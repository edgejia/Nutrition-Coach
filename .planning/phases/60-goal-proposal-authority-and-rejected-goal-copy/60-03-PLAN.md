---
phase: 60-goal-proposal-authority-and-rejected-goal-copy
plan: 03
type: execute
wave: 3
depends_on:
  - 60-02
files_modified:
  - server/orchestrator/index.ts
  - tests/unit/orchestrator.test.ts
  - tests/integration/chat-goal-update.integration.test.ts
autonomous: true
requirements:
  - GOAL-01
  - GOAL-02
  - GOAL-03
  - GOAL-04
must_haves:
  truths:
    - "Users receive backend-rendered proposal copy for vague goal-change recommendations, with daily targets unchanged."
    - "Users can confirm a valid latest active proposal with short consent, and consumed/expired/missing proposal confirmations fail closed."
    - "Users can cancel an active proposal with cancel terms and receive neutral backend copy without mutation or `goals_update`."
    - "Failed/cancel goal paths directly control the final reply with `finalReplySource: renderer` and no later LLM rewrite."
    - "Validation, guard, proposal failure, and cancel paths leave targets unchanged and publish no `goals_update`."
  artifacts:
    - path: "server/orchestrator/index.ts"
      provides: "Controlled goal reply short-circuit and pre-LLM cancel handling"
    - path: "tests/unit/orchestrator.test.ts"
      provides: "Renderer-owned final reply source and no later LLM rewrite proof"
    - path: "tests/integration/chat-goal-update.integration.test.ts"
      provides: "Fastify + real SQLite chat flow proof for proposal authority and rejected/cancel copy"
  key_links:
    - from: "server/orchestrator/tools.ts"
      to: "server/orchestrator/index.ts"
      via: "ToolExecutionResult.controlledReply"
      pattern: "controlledReply|finalReplySource: \"renderer\""
    - from: "server/orchestrator/index.ts"
      to: "server/routes/chat.ts"
      via: "OrchestratorResult.finalReplySource/finalReplyShape"
      pattern: "recordFinalReply"
    - from: "tests/integration/chat-goal-update.integration.test.ts"
      to: "server/realtime/publisher.ts"
      via: "publishGoalsUpdate spy"
      pattern: "goals_update"
---

<objective>
Finish Phase 60 by wiring controlled goal proposal/failure/cancel replies into the orchestrator final-reply path and proving the full Fastify chat behavior with real SQLite and mock LLM provider tests.

Purpose: Tool contracts alone are not enough; failed/cancel paths must not continue into LLM-authored final prose, and route metadata must show backend renderer ownership.
Output: orchestrator short-circuit behavior plus unit/integration proof for GOAL-01 through GOAL-04.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-CONTEXT.md
@.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-RESEARCH.md
@.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-PATTERNS.md
@.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-AI-SPEC.md
@.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-VALIDATION.md
@.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-01-SUMMARY.md
@.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-02-SUMMARY.md
@AGENTS.md

<interfaces>
From Plan 02:
- `executeTool()` returns normal mutation results for successful `update_goals` persistence and a controlled reply shape for non-mutating `propose_goals`, proposal/authority failure, validation failure, and cancel outcomes.
- `ToolDeps.goalProposalService` is wired through `server/app.ts` and `createOrchestrator`.
- `server/orchestrator/source-text-guard.ts` exports `isGoalProposalConsent(message)` and `isGoalProposalCancel(message)`; Plan 03 must import `isGoalProposalCancel` from this shared helper and must not duplicate cancel terms or define a local predicate.

Existing route behavior:
- `server/routes/chat.ts` already records `result.finalReplySource` and `result.finalReplyShape` through `llmTraceRecorderFactory` for JSON and SSE paths.
- JSON/SSE done payloads include `dailyTargets` only when `OrchestratorResult.dailyTargets` is set.

Execution skill rules:
- Use `nutrition-gen-test` patterns: Node `node:test`, real SQLite, `MockLLMProvider`, `app.inject()` or real `buildApp()` fixtures.
- Use `nutrition-verify-change` verification gates after edits: `yarn tsc --noEmit`, affected unit tests, and integration tests because `server/orchestrator/index.ts` and chat integration behavior are touched.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Short-circuit controlled goal replies in orchestrator</name>
  <files>server/orchestrator/index.ts, tests/unit/orchestrator.test.ts</files>
  <read_first>
    server/orchestrator/index.ts
    server/orchestrator/tools.ts
    server/orchestrator/source-text-guard.ts
    server/orchestrator/mutation-receipts.ts
    server/services/goal-proposals.ts
    tests/unit/orchestrator.test.ts
    tests/helpers/spy-hooks.ts
    .planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-CONTEXT.md
    server/orchestrator/index.ts
    tests/unit/orchestrator.test.ts
  </read_first>
  <behavior>
    - Test 1: a `propose_goals` tool call returns exact proposal copy, `didLogMeal: false`, `didMutateMeal: false`, `finalReplySource: "renderer"`, and does not call a second LLM reply round.
    - Test 2: an `update_goals` controlled validation failure returns exact validation copy, `finalReplySource: "renderer"`, unchanged targets, and no second LLM reply round.
    - Test 3: an `update_goals` missing/unavailable proposal outcome returns generic fail-closed copy, `finalReplySource: "renderer"`, and no second LLM reply round.
    - Test 4: a cancel term with an active pending proposal clears it before any LLM call and returns exact cancel copy with `finalReplySource: "renderer"`.
  </behavior>
  <action>
    Update `server/orchestrator/index.ts` so when `executeTool()` returns a controlled goal reply, `handleMessage()` immediately returns that reply with `didLogMeal: false`, `didMutateMeal: false`, `finalReplySource: "renderer"`, and `finalReplyShape: "plain_text"`, without appending a tool result for a later model round. Also add a narrow pre-LLM cancel check after user message persistence and before model completion: import `isGoalProposalCancel` from `server/orchestrator/source-text-guard.ts`, and if `isGoalProposalCancel(userMessage)` is true and `goalProposalService.getLatest(deviceId)` returns an active proposal, clear it and return `renderGoalCancelCopy()` with renderer metadata. Keep this preflight limited to the shared cancel predicate so consent still goes through explicit `update_goals` proposal mode. Do not define a local cancel term list in `index.ts`. This implements D-11, D-13, D-24, D-25, D-26, and D-28.
  </action>
  <acceptance_criteria>
    - Source assertion: controlled goal replies return before `messages.push({ role: "assistant", content: null, tool_calls: ... })` can drive a later model round.
    - Source assertion: `server/orchestrator/index.ts` imports `isGoalProposalCancel` from `server/orchestrator/source-text-guard.ts` and does not duplicate cancel terms.
    - Source assertion: cancel preflight calls `goalProposalService.clear(deviceId)` only after an active proposal exists, and returns cancel copy without `deviceService.updateGoals` or `publisher.publishGoalsUpdate`.
    - Behavior assertion: unit tests assert `mockLLM.chatCalls.length` does not increase for controlled proposal/failure/cancel final replies.
    - Behavior assertion: unit tests assert `finalReplySource === "renderer"` and `finalReplyShape === "plain_text"` for controlled goal replies.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/orchestrator.test.ts tests/unit/update-goals-contract.test.ts</automated>
  </verify>
  <done>Non-mutating proposal and rejected/cancel goal outcomes are backend-rendered final replies with no later LLM rewrite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove chat flow authority and no-publish invariants</name>
  <files>tests/integration/chat-goal-update.integration.test.ts</files>
  <read_first>
    tests/integration/chat-goal-update.integration.test.ts
    server/app.ts
    server/llm/mock.ts
    server/orchestrator/index.ts
    server/orchestrator/tools.ts
    server/realtime/publisher.ts
    server/routes/chat.ts
    .planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-VALIDATION.md
    tests/integration/chat-goal-update.integration.test.ts
  </read_first>
  <behavior>
    - Test 1: explicit numeric chat turn uses `update_goals` with `mode: "current_turn_values"`, persists targets, returns deterministic success receipt, and publishes `goals_update`.
    - Test 2: vague goal-change chat turn uses `propose_goals`, returns exact proposal copy, leaves targets unchanged, sends no `dailyTargets` in the response body, and publishes no `goals_update`.
    - Test 3: `好` with `update_goals` `mode: "latest_proposal"` applies the active proposal, clears it, returns success receipt, and a replayed `好` fails closed without mutation.
    - Test 4: missing/expired/unavailable proposal confirmation returns generic fail-closed copy, leaves targets unchanged, publishes no `goals_update`, and does not include LLM success-style text.
    - Test 5: cancel after a proposal clears pending state, returns exact cancel neutral copy, leaves targets unchanged, publishes no `goals_update`, and does not call the LLM on the cancel turn.
    - Test 6: validation range failure returns exact field-specific validation copy, leaves targets unchanged, publishes no `goals_update`, and does not call final reply generation.
  </behavior>
  <action>
    Rewrite the existing outdated confirmation tests in `tests/integration/chat-goal-update.integration.test.ts` so previous assistant prose is no longer an authority source. Use `buildApp({ onServicesReady })` to wrap the `publisher.publishGoalsUpdate` method exposed by Plan 02 `AppServices`, collect publish calls in memory, and assert rejected/cancel/proposal paths produce zero publish calls while successful current-turn and proposal confirmations publish once. Queue `MockLLMProvider` tool calls using explicit `mode` args. Preserve real Fastify app, signed cookie session, and real SQLite `:memory:` fixture patterns. Exact-copy assertions must cover proposal, generic fail-closed, validation range, and cancel paths per D-27. Negative assertions must cover unchanged targets, no success receipt text, no `goals_update` publish, and no later LLM rewrite per D-28.
  </action>
  <acceptance_criteria>
    - Behavior assertion: integration tests no longer expect a prompt-only recommendation followed by assistant-prose-authorized `update_goals`.
    - Behavior assertion: proposal creation and all rejected/cancel paths leave persisted targets at default or previously expected values.
    - Behavior assertion: rejected/cancel path replies equal backend renderer copy and do not match `/已更新每日目標|已經幫你更新|更新好了/`.
    - Behavior assertion: controlled rejected/cancel tests assert final reply generation is not called by checking `MockLLMProvider.chatCalls.length`.
    - Privacy assertion: tests do not write raw user messages, assistant final text, tool payloads, provider bodies, image data, session material, database snapshots, or target values to harness artifacts.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/chat-goal-update.integration.test.ts</automated>
  </verify>
  <done>Fastify chat behavior proves proposal creation, confirmation, replay failure, validation failure, and cancel invariants end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove renderer metadata and run phase gates</name>
  <files>tests/integration/chat-goal-update.integration.test.ts, tests/unit/orchestrator.test.ts</files>
  <read_first>
    tests/integration/chat-goal-update.integration.test.ts
    tests/unit/orchestrator.test.ts
    server/routes/chat.ts
    server/orchestrator/llm-trace.ts
    server/orchestrator/index.ts
    tests/unit/llm-chat-trace.test.ts
    .planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-VALIDATION.md
    tests/integration/chat-goal-update.integration.test.ts
    tests/unit/orchestrator.test.ts
  </read_first>
  <behavior>
    - Test 1: an integration test using `llmTraceRecorderFactory` records `summary.finalReply.source === "renderer"` and `shape === "plain_text"` for proposal failure or cancel.
    - Test 2: the same trace contains metadata-only timeline facts and does not contain raw user text, final reply text, tool arguments, provider bodies, session cookies, or database snapshots.
    - Test 3: the full targeted Phase 60 command set passes.
  </behavior>
  <action>
    Add metadata assertions to the Phase 60 integration or unit tests without changing the trace schema unless needed. Use the existing `llmTraceRecorderFactory` path in `buildApp()` or direct `createOrchestrator()` unit result metadata to prove renderer-owned final reply source. Keep evidence metadata-only: assertions may inspect `summary.finalReply.source`, `summary.finalReply.shape`, tool names, success/executed flags, failure reasons, updated field names, and published event names; assertions must not persist raw prompt, user text, assistant final text, tool payloads, provider body, image data, session material, or database snapshots. This implements D-25, D-26, D-28, GOAL-04, and PROOF-02 support required by Phase 60 validation.
  </action>
  <acceptance_criteria>
    - Behavior assertion: at least one rejected/cancel path has an automated assertion that final reply metadata is renderer-owned.
    - Privacy assertion: trace or test evidence checks do not include raw user message text or exact final reply text as persisted artifact content; exact-copy assertions may remain in test source only.
    - CLI assertion: `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/update-goals-contract.test.ts tests/unit/orchestrator.test.ts tests/integration/chat-goal-update.integration.test.ts` passes.
    - CLI assertion: `yarn tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>yarn tsc --noEmit && node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/goal-proposals.test.ts tests/unit/mutation-receipts.test.ts tests/unit/update-goals-contract.test.ts tests/unit/system-prompt.test.ts tests/unit/orchestrator.test.ts tests/integration/chat-goal-update.integration.test.ts</automated>
  </verify>
  <done>Phase 60 has renderer-owned metadata proof and targeted unit/integration gates passing with metadata-only evidence constraints preserved.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | Phase 60 | Users can only change daily targets through explicit current-turn numeric values or valid backend-persisted proposal; rejected updates produce deterministic backend copy. | 01-03 | COVERED | Service, tools, orchestrator, and tests all target this outcome. |
| REQ | GOAL-01 | Concrete proposal without target mutation until backend persists pending proposal. | 01, 02, 03 | COVERED | `propose_goals`, `goalProposalService`, proposal copy, integration proof. |
| REQ | GOAL-02 | `好` updates only with valid backend proposal or explicit current-turn numbers. | 02, 03 | COVERED | Explicit `current_turn_values` / `latest_proposal` modes and no previous-assistant authority. |
| REQ | GOAL-03 | Expired, consumed, mismatched, missing proposals fail closed with deterministic Traditional Chinese guidance. | 01, 02, 03 | COVERED | Generic fail-closed renderer plus lifecycle and replay/unavailable tests. |
| REQ | GOAL-04 | Validation/guard rejection has deterministic backend copy, no persistence, no publish, no LLM success-style text. | 01, 02, 03 | COVERED | Validation renderer, controlled reply short-circuit, publish spy tests. |
| RESEARCH | R-01 | Use existing custom TypeScript orchestrator, no new runtime dependency. | 02, 03 | COVERED | Plans modify existing `tools.ts`, `index.ts`, and tests only. |
| RESEARCH | R-02 | Use existing `turn_states` through a thin goal proposal wrapper, no schema migration. | 01, 02 | COVERED | `server/services/goal-proposals.ts`; schema diff gate. |
| RESEARCH | R-03 | Renderer-owned proposal/rejected/cancel copy near mutation receipts. | 01, 03 | COVERED | New renderers in `mutation-receipts.ts`, final source renderer. |
| RESEARCH | R-04 | Preserve metadata-only logs and proof. | 02, 03 | COVERED | Log summary and trace assertions exclude raw payloads. |
| CONTEXT | D-01 | Add separate `propose_goals` backend tool/contract. | 02 | COVERED | Plan 02 Task 2. |
| CONTEXT | D-02 | Replace prompt-only ambiguous recommendation with backend-owned proposal state. | 02, 03 | COVERED | Plan 02 prompt/tool change; Plan 03 integration rewrite. |
| CONTEXT | D-03 | `propose_goals` returns deterministic backend-rendered Traditional Chinese proposal copy. | 01, 02, 03 | COVERED | Proposal renderer and controlled reply tests. |
| CONTEXT | D-04 | Split proposal persistence from `update_goals` mutation path. | 02 | COVERED | Separate contracts and dependency wiring. |
| CONTEXT | D-05 | One active pending proposal per device. | 01 | COVERED | `turn_states` kind wrapper and overwrite test. |
| CONTEXT | D-06 | Newer proposal overwrites previous proposal. | 01 | COVERED | Goal proposal service tests. |
| CONTEXT | D-07 | Pending proposals expire after 30 minutes. | 01 | COVERED | `GOAL_PROPOSAL_TTL_MS` and expiry test. |
| CONTEXT | D-08 | Successful target persistence clears pending proposal before publish/recompute concerns. | 02 | COVERED | `update_goals` success path criteria. |
| CONTEXT | D-09 | Validation/guard/mismatch/execution failure no mutation/no publish and retryable failures do not consume pending proposal. | 02, 03 | COVERED | Tool tests and integration no-publish assertions. |
| CONTEXT | D-10 | Expired proposals unavailable and cannot mutate. | 01, 02 | COVERED | Service expiry plus latest proposal unavailable handling. |
| CONTEXT | D-11 | Cancel terms clear active pending proposal. | 02, 03 | COVERED | Tool cancel predicate and orchestrator preflight. |
| CONTEXT | D-12 | Consent belongs to backend predicate. | 02 | COVERED | Explicit consent term list in tool action. |
| CONTEXT | D-13 | Cancel terms excluded from confirmation. | 02, 03 | COVERED | Predicate criteria and preflight. |
| CONTEXT | D-14 | Proposal-path update requires current-turn consent plus active unexpired proposal. | 02 | COVERED | `latest_proposal` execution criteria. |
| CONTEXT | D-15 | Empty args must not mean proposal confirmation. | 02 | COVERED | Explicit mode schema and tests. |
| CONTEXT | D-16 | Hidden `proposal_id` preferred only if reliable. | 01, 02 | COVERED | Planning choice records hidden id is not proven reliable. |
| CONTEXT | D-17 | Use latest active proposal mode if hidden id reliability is not proven. | 01, 02 | COVERED | `mode: "latest_proposal"` selected. |
| CONTEXT | D-18 | Current-turn numeric updates override pending proposal and clear on success. | 02 | COVERED | `current_turn_values` behavior. |
| CONTEXT | D-19 | Mixed confirmation plus edits applies proposal with current-turn overrides. | 02 | COVERED | `latest_proposal` optional overrides. |
| CONTEXT | D-20 | Proposal copy lists exact proposed targets and instructions. | 01 | COVERED | Exact copy renderer. |
| CONTEXT | D-21 | Proposal copy avoids LLM rationale/success tone. | 01, 02 | COVERED | Renderer tests and prompt routing. |
| CONTEXT | D-22 | Proposal/authority failures share generic deterministic fail-closed copy. | 01, 02, 03 | COVERED | Single generic renderer and tests. |
| CONTEXT | D-23 | Validation range failures get field-specific deterministic copy. | 01, 02, 03 | COVERED | Validation renderer and tests. |
| CONTEXT | D-24 | Cancel is neutral, clears proposal, no mutation, no publish. | 01, 02, 03 | COVERED | Cancel renderer, tool predicate, preflight, integration proof. |
| CONTEXT | D-25 | Failed/cancel paths directly control final reply; no LLM rewrite. | 03 | COVERED | Controlled reply short-circuit. |
| CONTEXT | D-26 | Final reply metadata identifies backend/renderer ownership. | 03 | COVERED | `finalReplySource: "renderer"` tests. |
| CONTEXT | D-27 | Exact-copy assertions for generic failure, validation, and cancel. | 01, 03 | COVERED | Renderer and integration exact-copy tests. |
| CONTEXT | D-28 | Negative invariants: unchanged targets, no publish, renderer-owned, no LLM success prose, no forbidden terms. | 01, 02, 03 | COVERED | Unit and integration negative assertions. |
| CONTEXT | D-29 | Do not require distinct copy per internal reason code. | 01, 03 | COVERED | Single generic authority failure renderer and audit notes. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool result to final reply | Controlled backend outcomes cross into user-visible chat output. |
| Route trace recorder to artifacts | Final reply ownership metadata can be persisted as proof. |
| LLM provider to orchestrator | The model may attempt follow-up prose after a controlled failure unless orchestrator short-circuits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-08 | Spoofing | `finalReplySource` metadata | mitigate | Orchestrator sets renderer source only for backend controlled replies and tests assert trace metadata for rejected/cancel paths. |
| T-60-09 | Tampering | rejected/cancel final copy | mitigate | Controlled goal replies return before any later LLM round; tests assert no second chat call and exact backend copy. |
| T-60-10 | Information Disclosure | trace/artifact proof | mitigate | Metadata tests inspect final-reply source/shape and operational fields only; no raw user text, final reply text, tool payload, provider body, session, image, or DB snapshot is persisted. |
| T-60-11 | Repudiation | no-publish rejected paths | mitigate | Integration/unit tests assert unchanged targets and no `goals_update` publish for validation, guard, proposal failure, and cancel paths. |
</threat_model>

<verification>
Run the targeted command in Task 3. Because this phase touches `server/orchestrator/index.ts`, `server/app.ts`, `server/services/*.ts`, and integration tests, finish with `yarn tsc --noEmit`, `yarn test:unit`, and `yarn test:integration` if the targeted command passes.
</verification>

<success_criteria>
- Proposal creation is non-mutating and returns backend-rendered Traditional Chinese copy.
- Current-turn numeric values and valid latest active proposal confirmation are the only successful goal mutation paths.
- Expired, consumed, missing, replaced, validation, guard, proposal failure, and cancel paths do not mutate targets, do not publish `goals_update`, and return renderer-owned deterministic copy.
- Final reply metadata proves backend renderer ownership for failed/cancel paths, and tests preserve metadata-only proof constraints.
</success_criteria>

<output>
After completion, create `.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-03-SUMMARY.md`
</output>
