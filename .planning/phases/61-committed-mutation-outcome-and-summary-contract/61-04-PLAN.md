---
phase: 61-committed-mutation-outcome-and-summary-contract
plan: 04
type: execute
wave: 4
depends_on: [61-03]
files_modified:
  - server/routes/chat.ts
  - tests/integration/chat-api.test.ts
  - tests/integration/chat-streaming.test.ts
autonomous: true
requirements: [MUT-01, MUT-02, MUT-03]
user_setup: []
must_haves:
  truths:
    - "Chat JSON responses expose summaryOutcome for committed meal log, update, and delete mutations."
    - "Chat SSE terminal payloads expose summaryOutcome for committed meal mutations without changing publish failure semantics."
  artifacts:
    - path: "server/routes/chat.ts"
      provides: "JSON and SSE terminal payload projection for summaryOutcome"
      contains: "summaryOutcome"
    - path: "tests/integration/chat-api.test.ts"
      provides: "Fastify JSON proof for committed degraded mutation outcomes"
    - path: "tests/integration/chat-streaming.test.ts"
      provides: "SSE done/stopped proof for committed degraded mutation outcomes"
  key_links:
    - from: "server/routes/chat.ts"
      to: "server/orchestrator/index.ts"
      via: "OrchestratorResult.summaryOutcome"
      pattern: "summaryOutcome"
    - from: "server/routes/chat.ts"
      to: "publishSummarySafe"
      via: "publish only when dailySummaryFromOutcome produced usable summary"
      pattern: "publishSummarySafe"
---

<objective>
Project `summaryOutcome` through chat JSON and SSE terminal payloads while preserving existing SSE ordering and metadata-only publish failure handling.

Purpose: Users must receive committed chat outcomes for log/update/delete even when summary freshness degrades.
Output: Chat route response parity for JSON and streaming terminal events, with integration proof.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-RESEARCH.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-VALIDATION.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-PATTERNS.md
@server/routes/chat.ts
@server/orchestrator/index.ts
@tests/integration/chat-api.test.ts
@tests/integration/chat-streaming.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add chat JSON and SSE response parity tests</name>
  <files>tests/integration/chat-api.test.ts, tests/integration/chat-streaming.test.ts</files>
  <read_first>
    AGENTS.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-VALIDATION.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-PATTERNS.md
    server/routes/chat.ts
    tests/integration/chat-api.test.ts
    tests/integration/chat-streaming.test.ts
  </read_first>
  <behavior>
    - JSON `POST /api/chat` log response includes committed `loggedMeal`, `didLogMeal: true`, `didMutateMeal: true`, and `summaryOutcome`.
    - JSON update/delete responses include committed facts, `affectedDate`, `didMutateMeal: true`, and `summaryOutcome`.
    - SSE `done` and applicable `stopped` payloads include `summaryOutcome` for committed meal mutations.
    - `dailySummary` is included only for fresh/recovered and omitted for unavailable.
  </behavior>
  <action>
    Extend existing Fastify integration tests using the current `buildApp()` and `MockLLMProvider` patterns. Add degraded-summary fixtures by injecting or replacing summary recompute behavior through existing service hooks; do not call live OpenAI. Tests must assert D-05 parity across JSON and SSE terminal payloads, D-07 compatibility dailySummary projection, D-19-style committed success semantics for chat payloads, and D-21 that publish failure does not alter `summaryOutcome`. Preserve current status/chunk/done/stopped ordering assertions.
  </action>
  <acceptance_criteria>
    Integration tests fail before `server/routes/chat.ts` projection changes; tests cover log, update, and delete terminal payloads; tests assert no `publish_failed` status or reason; tests assert no top-level `dailySummary` for unavailable outcomes.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/chat-api.test.ts tests/integration/chat-streaming.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Chat route integration coverage proves response parity for committed degraded mutation outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project summaryOutcome from chat route results</name>
  <files>server/routes/chat.ts, tests/integration/chat-api.test.ts, tests/integration/chat-streaming.test.ts</files>
  <read_first>
    AGENTS.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-RESEARCH.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-PATTERNS.md
    server/routes/chat.ts
    server/orchestrator/index.ts
    tests/integration/chat-api.test.ts
    tests/integration/chat-streaming.test.ts
  </read_first>
  <action>
    Update `server/routes/chat.ts` so JSON responses and SSE `done`/`stopped` payloads include `summaryOutcome` whenever the orchestrator result committed a meal mutation. Remove the JSON invariant that throws when `didLogMeal` is true and `dailySummary` is missing; `summaryOutcome.status === "unavailable"` is valid. Keep top-level `dailySummary` only when the orchestrator result provides a usable summary from fresh/recovered outcome. Keep `publishSummarySafe()` non-fatal and call it only with usable `dailySummary`; publish failure remains warn-level metadata-only logging and does not enter the response body.
  </action>
  <acceptance_criteria>
    Chat JSON and SSE terminal payloads include `summaryOutcome` for committed meal mutations; unavailable summary does not throw or synthesize `dailySummary`; existing upload cleanup, status/chunk/done ordering, stop behavior, and fallback behavior are preserved; logs do not include raw user text, tool payloads, provider bodies, images, sessions, DB snapshots, or assistant final text.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/chat-api.test.ts tests/integration/chat-streaming.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Chat JSON and SSE consumers receive committed mutation facts plus summaryOutcome, with publish failure isolated from response status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to chat route | Multipart/SSE/JSON requests cross signed guest-session authorization and route validation. |
| orchestrator to SSE/JSON payload | Structured backend result becomes public response payload. |
| route to realtime publisher | Publish failure is logged but must not alter mutation response truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-13 | Spoofing | `server/routes/chat.ts` protected route | mitigate | Preserve signed guest-session resolution and cookie clearing behavior; do not accept raw `deviceId` query/header authority. |
| T-61-14 | Tampering | chat tool mutation path | mitigate | Preserve existing tool validation and service-scoped `deviceId`; Phase 62 stale revision checks remain out of scope. |
| T-61-15 | Information Disclosure | chat route logs/SSE/JSON | mitigate | Add only status/reason/summary fields allowed by `SummaryOutcome`; do not log raw user text, tool payloads, provider bodies, image data, sessions, DB snapshots, or assistant final text. |
| T-61-16 | Integrity | publish failure handling | mitigate | Keep `publish_failed` metadata-only and out of `summaryOutcome`; tests assert no response-body publish status. |
</threat_model>

<verification>
Targeted: `node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/chat-api.test.ts tests/integration/chat-streaming.test.ts`
Static: `yarn tsc --noEmit`
Wave gate after this plan if executed with dependencies: `yarn test:integration`
</verification>

<source_audit>
GOAL: Covered by public chat JSON/SSE response projection.
REQ: MUT-01/MUT-02/MUT-03 public chat response behavior.
RESEARCH: Implements chat JSON/SSE projection and preserves publishSummarySafe separation.
CONTEXT: Implements D-05, D-07, D-08, D-11, D-19, and D-21; excludes `/api/sse daily_summary` event freshness per D-06.
</source_audit>

<success_criteria>
Committed chat log/update/delete responses expose `summaryOutcome` and do not fail solely because summary recompute/recovery or publish degrades after commit.
</success_criteria>

<output>
After completion, create `.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-04-SUMMARY.md`.
</output>
