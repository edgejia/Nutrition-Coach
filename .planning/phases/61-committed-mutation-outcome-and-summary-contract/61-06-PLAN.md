---
phase: 61-committed-mutation-outcome-and-summary-contract
plan: 06
type: execute
wave: 5
depends_on: [61-04, 61-05]
files_modified:
  - client/src/types.ts
  - client/src/api.ts
  - client/src/components/MealEditScreen.tsx
  - client/src/components/SummaryDetailScreen.tsx
  - tests/unit/api-client.test.ts
  - tests/unit/meal-edit-screen.test.ts
autonomous: true
requirements: [MUT-04]
user_setup: []
must_haves:
  truths:
    - "Client HTTP and SSE consumers parse summaryOutcome safely."
    - "Direct meal edit/delete UI treats missing top-level dailySummary on committed unavailable outcomes as summary unavailable, not mutation failure."
    - "Final Phase 61 gates pass before closure."
    - "D-08: Parse and consume summaryOutcome safely without treating missing top-level dailySummary as mutation failure."
    - "D-11: Preserve summaryOutcome as the structured degraded-signal channel for direct HTTP and chat client tests."
    - "D-12: Do not add a visible stale-summary UI indicator in Phase 61."
  artifacts:
    - path: "client/src/types.ts"
      provides: "Public client SummaryOutcome and mutation response DTOs"
      contains: "SummaryOutcome"
    - path: "client/src/api.ts"
      provides: "Runtime parsing/normalization for summaryOutcome in chat and direct meal responses"
      contains: "isSummaryOutcome"
    - path: "client/src/components/MealEditScreen.tsx"
      provides: "Committed direct mutation UX that does not require dailySummary"
    - path: "client/src/components/SummaryDetailScreen.tsx"
      provides: "Historical/direct delete consumer that tolerates summaryOutcome.unavailable"
    - path: "tests/unit/api-client.test.ts"
      provides: "Client transport tests for unavailable summary outcomes"
    - path: "tests/unit/meal-edit-screen.test.ts"
      provides: "Source-contract tests for committed mutation side effects without dailySummary"
  key_links:
    - from: "client/src/api.ts"
      to: "client/src/types.ts"
      via: "SummaryOutcome type and guard"
      pattern: "SummaryOutcome"
    - from: "client/src/components/MealEditScreen.tsx"
      to: "client/src/api.ts"
      via: "updateMeal/deleteMeal responses with optional dailySummary"
      pattern: "summaryOutcome"
---

<objective>
Update client transport and direct mutation consumers to understand the public `summaryOutcome` contract, then run the Phase 61 final gate.

Purpose: D-08 requires HTTP consumers to parse and consume `summaryOutcome` safely and not treat missing top-level `dailySummary` as mutation failure.
Output: Client DTOs/parsers/components accept committed unavailable outcomes, with targeted tests and final phase verification.
</objective>

<decision_coverage>
- D-08: Parse and consume `summaryOutcome` safely without treating missing top-level `dailySummary` as mutation failure.
- D-11: Preserve `summaryOutcome` as the structured degraded-signal channel for direct HTTP and chat client tests.
- D-12: Do not add a visible stale-summary UI indicator in Phase 61.
</decision_coverage>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-RESEARCH.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-VALIDATION.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-PATTERNS.md
@client/src/types.ts
@client/src/api.ts
@client/src/components/MealEditScreen.tsx
@client/src/components/SummaryDetailScreen.tsx
@tests/unit/api-client.test.ts
@tests/unit/meal-edit-screen.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add client parsing and consumer tests</name>
  <files>tests/unit/api-client.test.ts, tests/unit/meal-edit-screen.test.ts</files>
  <read_first>
    AGENTS.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-VALIDATION.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-PATTERNS.md
    client/src/types.ts
    client/src/api.ts
    client/src/components/MealEditScreen.tsx
    client/src/components/SummaryDetailScreen.tsx
    tests/unit/api-client.test.ts
    tests/unit/meal-edit-screen.test.ts
  </read_first>
  <behavior>
    - `updateMeal()` and `deleteMeal()` resolve successfully for HTTP 200 bodies with `summaryOutcome.status === "unavailable"` and no top-level `dailySummary`.
    - Chat SSE `done` and `stopped` callback payloads preserve valid `summaryOutcome` and ignore invalid malformed summaryOutcome values.
    - `MealEditScreen` preserves `redactChatReceiptIdentity`, `recordMealMutation`, and navigation after committed update/delete even when `dailySummary` is absent.
    - `SummaryDetailScreen` direct delete handling does not dereference `dailySummary.date` unless a usable summary exists.
  </behavior>
  <action>
    Extend existing client unit/source-contract tests. Use the repo's current mocked `fetch` and source assertion patterns in `tests/unit/api-client.test.ts` and `tests/unit/meal-edit-screen.test.ts`. Add or update tests so missing `dailySummary` with committed `summaryOutcome.unavailable` is not interpreted as failed save/delete. Do not add visible stale-summary UI indicators, banners, or copy; D-12 defers that.
  </action>
  <acceptance_criteria>
    Tests fail before client implementation; tests cover direct update, direct delete, SSE terminal parsing, and component source contract; no test expects visible degraded-summary text; malformed `summaryOutcome` is ignored or safely omitted rather than throwing.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/api-client.test.ts tests/unit/meal-edit-screen.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Client tests describe safe parsing and committed direct mutation UX for unavailable summaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement client SummaryOutcome DTO parsing and consumers</name>
  <files>client/src/types.ts, client/src/api.ts, client/src/components/MealEditScreen.tsx, client/src/components/SummaryDetailScreen.tsx, tests/unit/api-client.test.ts, tests/unit/meal-edit-screen.test.ts</files>
  <read_first>
    AGENTS.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-RESEARCH.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-PATTERNS.md
    client/src/types.ts
    client/src/api.ts
    client/src/components/MealEditScreen.tsx
    client/src/components/SummaryDetailScreen.tsx
    tests/unit/api-client.test.ts
    tests/unit/meal-edit-screen.test.ts
  </read_first>
  <action>
    Add a client `SummaryOutcome` union in `client/src/types.ts` matching D-01 exactly. Update `UpdateMealResponse`, `DeleteMealResponse`, `ChatReply`, SSE callback payloads, and related DTOs to include `summaryOutcome` and make top-level `dailySummary` optional compatibility data. In `client/src/api.ts`, add `isSummaryOutcome()` or equivalent runtime guard: accept fresh/recovered only with a valid `DailySummary` and recovered/unavailable only with `reason: "recompute_failed"`. Update `updateMeal()`, `deleteMeal()`, SSE `done`, and `stopped` parsing to project `summaryOutcome` safely. Update `MealEditScreen` and `SummaryDetailScreen` so committed mutation side effects run after HTTP 200 even when `dailySummary` is missing; call `setDailySummary()` only when a usable summary exists.
  </action>
  <acceptance_criteria>
    Client types compile with `summaryOutcome`; direct HTTP consumers no longer require `dailySummary`; `MealEditScreen` and `SummaryDetailScreen` do not dereference `dailySummary.date` unless a summary exists; no visible degraded-summary UI text is added; malformed `summaryOutcome` does not crash parsing.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/api-client.test.ts tests/unit/meal-edit-screen.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Client DTOs, parsers, and direct mutation consumers handle committed unavailable summary outcomes safely.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 61 final verification gate</name>
  <files>client/src/types.ts, client/src/api.ts, client/src/components/MealEditScreen.tsx, client/src/components/SummaryDetailScreen.tsx, tests/unit/api-client.test.ts, tests/unit/meal-edit-screen.test.ts</files>
  <read_first>
    AGENTS.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-VALIDATION.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    After all previous Phase 61 plans are complete, run the final validation contract. Execute the targeted client command, then `yarn tsc --noEmit`, relevant unit and integration scripts, the full test suite, and `yarn release:check`. Do not push, merge, deploy, smoke-test Railway, or promote to staging/main; local release proof does not authorize promotion.
  </action>
  <acceptance_criteria>
    The executor records pass/fail status for every final command; `yarn release:check` is run only as local closure proof; no staging or main branch action occurs; generated harness artifacts are not hand-edited.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/api-client.test.ts tests/unit/meal-edit-screen.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
    <automated>yarn test:unit</automated>
    <automated>yarn test:integration</automated>
    <automated>yarn test</automated>
    <automated>yarn release:check</automated>
  </verify>
  <done>Phase 61 has targeted client proof plus full local type, test, and release-check closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server JSON/SSE to client parser | Public payloads are untrusted at the client boundary. |
| client transport to UI state | Parsed mutation responses affect local state and navigation. |
| local verification to release workflow | Release check proves local readiness but does not authorize deployment or promotion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-21 | Tampering | `client/src/api.ts` parser | mitigate | Runtime guard accepts only exact `summaryOutcome` statuses and `reason: "recompute_failed"`; malformed payloads are ignored safely. |
| T-61-22 | Information Disclosure | client tests/logs | mitigate | Tests use mocked payloads and do not persist raw prompts, user text, tool payloads, provider bodies, image data, sessions, or DB snapshots. |
| T-61-23 | Integrity | direct mutation UI | mitigate | Committed HTTP 200 responses trigger mutation side effects even without `dailySummary`; summary state updates only from usable summaries. |
| T-61-24 | Elevation of Privilege | client direct mutation calls | transfer | Authorization remains enforced by signed cookie guest-session routes from Plan 05; client sends same-origin credentials only. |
</threat_model>

<verification>
Targeted: `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/api-client.test.ts tests/unit/meal-edit-screen.test.ts`
Static: `yarn tsc --noEmit`
Phase gate: `yarn test:unit`, `yarn test:integration`, `yarn test`, `yarn release:check`
</verification>

<source_audit>
GOAL: Covered by client-safe consumption of committed outcomes.
REQ: MUT-04 client/public direct response consumption.
RESEARCH: Implements client parsing and direct mutation UX safeguards from common pitfall 4.
CONTEXT: Implements D-05, D-07, D-08, D-11, and D-12; does not add deferred visible degraded-summary UI.
</source_audit>

<success_criteria>
Client code accepts the new public contract, direct mutation UI does not fail committed unavailable outcomes, and Phase 61 passes local targeted and full gates.
</success_criteria>

<output>
After completion, create `.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-06-SUMMARY.md`.
</output>
