---
phase: 61-committed-mutation-outcome-and-summary-contract
plan: 02
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - server/orchestrator/mutation-effects.ts
  - server/orchestrator/mutation-receipts.ts
  - tests/unit/mutation-receipts.test.ts
autonomous: true
requirements: [MUT-01, MUT-02, MUT-03]
user_setup: []
must_haves:
  truths:
    - "Meal mutation receipt rendering depends on committed facts, not on mandatory committedSummary."
    - "Receipt copy remains unchanged for fresh, recovered, and unavailable summary outcomes."
  artifacts:
    - path: "server/orchestrator/mutation-effects.ts"
      provides: "Meal mutation effect types that carry summaryOutcome instead of requiring committedSummary"
      contains: "summaryOutcome"
    - path: "server/orchestrator/mutation-receipts.ts"
      provides: "Backend-rendered committed-facts receipt copy without freshness caveats"
      contains: "renderMutationReceipt"
    - path: "tests/unit/mutation-receipts.test.ts"
      provides: "Receipt copy invariants for summaryOutcome fresh/recovered/unavailable"
  key_links:
    - from: "server/orchestrator/mutation-effects.ts"
      to: "server/services/summary-outcome.ts"
      via: "SummaryOutcome type import"
      pattern: "SummaryOutcome"
    - from: "server/orchestrator/mutation-receipts.ts"
      to: "server/orchestrator/mutation-effects.ts"
      via: "MutationEffects union consumed by renderMutationReceipt"
      pattern: "renderMutationReceipt"
---

<objective>
Decouple meal mutation receipt types from mandatory fresh daily summaries while preserving backend-owned receipt copy.

Purpose: This removes the outdated `committedSummary` invariant for meal log/update/delete receipts before tool and route projections adopt `summaryOutcome`.
Output: Orchestrator mutation effect types and receipt tests compatible with fresh, recovered, and unavailable summary outcomes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-RESEARCH.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-PATTERNS.md
@.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-01-SUMMARY.md
@server/services/summary-outcome.ts
@server/orchestrator/mutation-effects.ts
@server/orchestrator/mutation-receipts.ts
@tests/unit/mutation-receipts.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove receipts ignore summary freshness status</name>
  <files>tests/unit/mutation-receipts.test.ts</files>
  <read_first>
    AGENTS.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-PATTERNS.md
    server/services/summary-outcome.ts
    server/orchestrator/mutation-effects.ts
    server/orchestrator/mutation-receipts.ts
    tests/unit/mutation-receipts.test.ts
  </read_first>
  <behavior>
    - Log receipt text is identical for `summaryOutcome.status` fresh, recovered, and unavailable.
    - Update receipt text is identical for fresh, recovered, and unavailable.
    - Delete receipt text is identical for fresh, recovered, and unavailable.
    - Receipt text does not contain `summaryOutcome`, `dailySummary`, `recompute_failed`, `publish_failed`, `PATCH`, `DELETE`, `/api`, or response internals.
  </behavior>
  <action>
    Extend `tests/unit/mutation-receipts.test.ts` to construct log, update, and delete `MutationEffects` with each D-01 summary outcome status. Assert exact existing Traditional Chinese receipt copy remains committed-facts only per D-09 and D-10. Keep Phase 60 goal proposal/rejection/cancel copy tests intact and do not migrate goal mutation effects to the Phase 61 public contract.
  </action>
  <acceptance_criteria>
    The unit tests fail against the old `committedSummary`-required meal effect shape; tests assert no freshness caveat and no protocol/internal terms appear in receipt text; Phase 60 goal copy tests continue to run unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/mutation-receipts.test.ts</automated>
  </verify>
  <done>Receipt tests prove backend-rendered meal mutation copy is independent of summary freshness status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update meal mutation effect contracts</name>
  <files>server/orchestrator/mutation-effects.ts, server/orchestrator/mutation-receipts.ts, tests/unit/mutation-receipts.test.ts</files>
  <read_first>
    AGENTS.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-RESEARCH.md
    server/services/summary-outcome.ts
    server/orchestrator/mutation-effects.ts
    server/orchestrator/mutation-receipts.ts
    tests/unit/mutation-receipts.test.ts
  </read_first>
  <action>
    Change `server/orchestrator/mutation-effects.ts` so log, update, and delete effects carry required `summaryOutcome: SummaryOutcome` and committed meal facts, and no longer require `committedSummary: DailySummary`. Keep goals effects compatible with existing Phase 60 behavior; do not expose Phase 61 `summaryOutcome` on `update_goals`. Update `mutation-receipts.ts` only as needed for the new type shape. Do not change exact receipt strings except where TypeScript requires fixture updates.
  </action>
  <acceptance_criteria>
    `MutationEffects` supports `kind: "log" | "update" | "delete"` with `summaryOutcome`; meal receipt rendering compiles without `committedSummary`; goal mutation effect behavior is not migrated; forbidden receipt terms still include internal/protocol terms; no visible degraded-summary UI copy is added.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/mutation-receipts.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Meal mutation effect and receipt contracts compile and pass copy invariants without mandatory fresh summary data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool result to renderer | Structured mutation facts become user-visible receipt text. |
| internal type to public route result | Summary freshness status must not leak raw implementation details. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05 | Information Disclosure | `mutation-receipts.ts` | mitigate | Tests assert no raw user text, tool payloads, provider bodies, image data, sessions, DB snapshots, `summaryOutcome`, `dailySummary`, or protocol terms appear in receipt copy. |
| T-61-06 | Spoofing | `mutation-effects.ts` | mitigate | Preserve committed facts as required typed fields; do not allow receipt rendering from assistant prose or unstructured tool strings. |
| T-61-07 | Integrity | `mutation-effects.ts` | mitigate | Keep `summaryOutcome` separate from committed meal facts so unavailable summary cannot erase committed log/update/delete facts. |
| T-61-08 | Scope Boundary | goal mutation effects | accept | D-20 excludes `update_goals` public summary-outcome migration; leave Phase 60 goal behavior intact. |
</threat_model>

<verification>
Targeted: `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/mutation-receipts.test.ts`
Static: `yarn tsc --noEmit`
</verification>

<source_audit>
GOAL: Covered by removing receipt dependency on mandatory fresh summaries.
REQ: MUT-01/MUT-02/MUT-03 receipt contract foundation.
RESEARCH: Addresses pitfall "Keeping committedSummary Required In Mutation Effects".
CONTEXT: Implements D-09 through D-12 and respects D-20 deferred goal migration.
</source_audit>

<success_criteria>
Meal mutation receipt copy is committed-facts only and can render with `summaryOutcome.unavailable` without a `DailySummary`.
</success_criteria>

<output>
After completion, create `.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-02-SUMMARY.md`.
</output>
