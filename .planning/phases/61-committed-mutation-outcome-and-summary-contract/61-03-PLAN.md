---
phase: 61-committed-mutation-outcome-and-summary-contract
plan: 03
type: execute
wave: 3
depends_on: [61-02]
files_modified:
  - server/orchestrator/tools.ts
  - server/orchestrator/index.ts
  - tests/unit/tools.test.ts
  - tests/unit/orchestrator.test.ts
autonomous: true
requirements: [MUT-01, MUT-02, MUT-03]
user_setup: []
must_haves:
  truths:
    - "Chat tool results for log, update, and delete expose committed mutation facts plus summaryOutcome."
    - "The orchestrator returns renderer-owned committed receipts even when summaryOutcome is unavailable."
  artifacts:
    - path: "server/orchestrator/tools.ts"
      provides: "log_food, update_meal, delete_meal tool result projection with summaryOutcome"
      contains: "summaryOutcome"
    - path: "server/orchestrator/index.ts"
      provides: "OrchestratorResult summaryOutcome propagation and no post-commit dailySummary gate for meal mutations"
      contains: "summaryOutcome"
    - path: "tests/unit/tools.test.ts"
      provides: "Tool contract proof for fresh/recovered/unavailable summary outcomes"
    - path: "tests/unit/orchestrator.test.ts"
      provides: "Renderer-owned receipt proof when summary recompute/recovery degrades"
  key_links:
    - from: "server/orchestrator/tools.ts"
      to: "server/services/summary-outcome.ts"
      via: "shared helper for log_food and service-provided update/delete outcomes"
      pattern: "summaryOutcome"
    - from: "server/orchestrator/index.ts"
      to: "server/orchestrator/mutation-receipts.ts"
      via: "renderCheckedMutationReceipt from committed facts and summaryOutcome"
      pattern: "renderCheckedMutationReceipt"
---

<objective>
Propagate the shared summary outcome contract through chat tools and orchestrator receipt generation.

Purpose: MUT-01, MUT-02, and MUT-03 require authoritative committed chat receipts after SQLite persistence, regardless of summary recompute/recovery degradation.
Output: Tool result DTOs and `OrchestratorResult` carry `summaryOutcome`; renderer-owned receipts no longer throw after commit because `dailySummary` is missing.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-RESEARCH.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-VALIDATION.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-PATTERNS.md
@.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-03-SUMMARY.md
@server/services/summary-outcome.ts
@server/orchestrator/tools.ts
@server/orchestrator/index.ts
@server/orchestrator/mutation-effects.ts
@server/orchestrator/mutation-receipts.ts
@tests/unit/tools.test.ts
@tests/unit/orchestrator.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend tool and orchestrator unit coverage</name>
  <files>tests/unit/tools.test.ts, tests/unit/orchestrator.test.ts</files>
  <read_first>
    AGENTS.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-VALIDATION.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-PATTERNS.md
    server/orchestrator/tools.ts
    server/orchestrator/index.ts
    tests/unit/tools.test.ts
    tests/unit/orchestrator.test.ts
  </read_first>
  <behavior>
    - `log_food` returns `summaryOutcome.status === "recovered"` when recompute fails and persisted-meal recovery succeeds.
    - `log_food` returns `summaryOutcome.status === "unavailable"` when recompute and recovery fail, while still returning logged meal facts.
    - `update_meal` and `delete_meal` tool results include service-provided `summaryOutcome`, committed facts, `affectedDate`, and compatibility `dailySummary` only for fresh/recovered.
    - `handleMessage()` returns renderer-owned log/update/delete receipt text and `didMutateMeal: true` when `summaryOutcome.status === "unavailable"`.
  </behavior>
  <action>
    Extend existing unit tests rather than adding a new framework. Update `tests/unit/tools.test.ts` around current log/update/delete tests and `tests/unit/orchestrator.test.ts` around committed receipt tests. Assertions must use exact statuses `"fresh"`, `"recovered"`, `"unavailable"` and exact reason `"recompute_failed"`. Add assertions that unavailable responses omit top-level `dailySummary` while retaining `loggedMeal`, `deletedMeal`, or updated meal facts. Preserve Phase 60 controlled goal reply assertions and explicit `update_goals` mode fixtures.
  </action>
  <acceptance_criteria>
    Targeted tests fail before production edits; tests cover MUT-01, MUT-02, and MUT-03 degraded chat behavior; tests do not expect `publish_failed` in `summaryOutcome`; tests do not assert visible summary caveats in receipt copy.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/tools.test.ts tests/unit/orchestrator.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Unit coverage describes chat tool and orchestrator committed-outcome behavior for log, update, and delete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire summaryOutcome through tools and orchestrator</name>
  <files>server/orchestrator/tools.ts, server/orchestrator/index.ts, tests/unit/tools.test.ts, tests/unit/orchestrator.test.ts</files>
  <read_first>
    AGENTS.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-RESEARCH.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-PATTERNS.md
    server/services/summary-outcome.ts
    server/orchestrator/tools.ts
    server/orchestrator/index.ts
    server/orchestrator/mutation-effects.ts
    server/orchestrator/mutation-receipts.ts
    tests/unit/tools.test.ts
    tests/unit/orchestrator.test.ts
  </read_first>
  <action>
    Update `server/orchestrator/tools.ts` so `ToolExecutionResult` and contract results for `log_food`, `update_meal`, and `delete_meal` include `summaryOutcome`. Replace the private log-only recovery path with the shared Plan 01 helper. Keep `dailySummary` as compatibility data only via `dailySummaryFromOutcome(summaryOutcome)`. Update `server/orchestrator/index.ts` so `OrchestratorResult` carries optional `summaryOutcome`, removes or stops using `requireDailySummaryForLoggedMeal()` as a post-commit gate for meal mutations, and builds log/update/delete `MutationEffects` from committed facts plus `summaryOutcome`. Preserve final-reply renderer short-circuiting from Phase 60 and preserve current stale/conflict/validation behavior.
  </action>
  <acceptance_criteria>
    `log_food`, `update_meal`, and `delete_meal` tool projections include `summaryOutcome`; unavailable summary does not throw after commit; compatibility `dailySummary` is present only for fresh/recovered; `publish_failed` is not a status or reason; `update_goals` public behavior is not migrated; Phase 60 goal tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/tools.test.ts tests/unit/orchestrator.test.ts tests/unit/mutation-receipts.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Chat tool and orchestrator layers return committed mutation receipts with explicit summaryOutcome for log, update, and delete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool call to backend tool executor | Untrusted model arguments are validated before persistence. |
| tool executor to renderer | Committed mutation facts become user-visible deterministic copy. |
| orchestrator to route | Structured result metadata is projected to JSON/SSE without raw prompt or payload leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-09 | Tampering | `tools.ts` tool contracts | mitigate | Preserve existing zod-backed tool validation and stale/conflict behavior; only change response projection after successful service commit. |
| T-61-10 | Repudiation | `index.ts` committed receipt path | mitigate | Return renderer-owned final replies with `didMutateMeal`, committed facts, `affectedDate`, and `summaryOutcome` after SQLite commit. |
| T-61-11 | Information Disclosure | orchestrator logs/results | mitigate | Do not log raw user text, tool payloads, provider bodies, image data, sessions, DB snapshots, or final assistant text while adding degraded-summary observability. |
| T-61-12 | Integrity | realtime publish separation | mitigate | Keep publish status outside `summaryOutcome`; D-04 forbids `publish_failed` in the union. |
</threat_model>

<verification>
Targeted: `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/tools.test.ts tests/unit/orchestrator.test.ts tests/unit/mutation-receipts.test.ts`
Static: `yarn tsc --noEmit`
Wave gate after this plan if executed with dependencies: `yarn test:unit`
</verification>

<source_audit>
GOAL: Covered by chat tool/orchestrator committed-outcome propagation.
REQ: MUT-01 log, MUT-02 update, MUT-03 delete.
RESEARCH: Implements tool/orchestrator projection and removes the `requireDailySummaryForLoggedMeal` failure mode for meal mutations.
CONTEXT: Implements D-01 through D-05, D-07, D-09 through D-18, D-20, and D-21; excludes `/api/sse daily_summary` freshness per D-06.
</source_audit>

<success_criteria>
Chat log/update/delete tool execution and orchestrator receipt generation succeed after commit even when summary recompute/recovery is unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-03-SUMMARY.md`.
</output>
