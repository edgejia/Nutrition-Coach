---
phase: 66-numeric-correction-provenance-guard
plan: 04
type: execute
wave: 2
depends_on:
  - 66-01
  - 66-02
  - 66-03
files_modified:
  - server/orchestrator/index.ts
  - server/app.ts
  - tests/unit/orchestrator.test.ts
  - tests/integration/chat-goal-update.integration.test.ts
autonomous: true
requirements:
  - CORR-01
  - CORR-02
  - CORR-03
must_haves:
  truths:
    - "D-20/D-22/D-23: approval commits only an active backend-owned meal proposal through the existing expected revision path, and cancel takes precedence."
    - "D-28/D-29/D-30: bare approval fails closed when goal and meal proposals coexist; kind-specific approval selects only the active proposal kind."
    - "D-31/D-32: creating one proposal kind does not clear the other kind, while broad cancel clears all active approvable proposal kinds."
  artifacts:
    - path: "server/orchestrator/index.ts"
      provides: "Pre-model proposal decision router for meal proposal approval, cancel, and cross-kind ambiguity"
    - path: "server/app.ts"
      provides: "Composition-root mealNumericProposalService wiring"
    - path: "tests/unit/orchestrator.test.ts"
      provides: "Renderer-owned no-second-LLM proof for meal proposal decisions"
  key_links:
    - from: "server/orchestrator/index.ts"
      to: "server/services/meal-numeric-proposals.ts"
      via: "active proposal decision router before model calls"
      pattern: "mealNumericProposalService"
---

<objective>
Route proposal approval, cancellation, and cross-kind ambiguity before the model loop so stored backend proposals cannot be confused with LLM prose.

Purpose: CORR-01 permits approved backend proposals; D-28 through D-32 require fail-closed behavior when multiple proposal kinds are active.
Output: App wiring plus orchestrator pre-model decision handling that applies or clears active proposals with renderer-owned terminal replies.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/66-numeric-correction-provenance-guard/66-CONTEXT.md
@.planning/phases/66-numeric-correction-provenance-guard/66-RESEARCH.md
@.planning/phases/66-numeric-correction-provenance-guard/66-VALIDATION.md
@.planning/phases/66-numeric-correction-provenance-guard/66-PATTERNS.md
@.planning/phases/66-numeric-correction-provenance-guard/66-01-SUMMARY.md
@.planning/phases/66-numeric-correction-provenance-guard/66-02-SUMMARY.md
@.planning/phases/66-numeric-correction-provenance-guard/66-03-SUMMARY.md
@server/orchestrator/index.ts
@server/app.ts
@server/services/goal-proposals.ts
@server/services/meal-numeric-proposals.ts
@tests/unit/orchestrator.test.ts
@tests/integration/chat-goal-update.integration.test.ts
</context>

<decision_coverage>
Covers D-20, D-21, D-22, D-23, D-27a, D-28, D-29, D-30, D-31, and D-32.
Uses the existing Phase 62 meal revision precondition path for stale proposal approval rather than adding proposal-specific stale write semantics.
</decision_coverage>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add proposal decision router proof</name>
  <files>tests/unit/orchestrator.test.ts, tests/integration/chat-goal-update.integration.test.ts</files>
  <read_first>
    - tests/unit/orchestrator.test.ts
    - tests/integration/chat-goal-update.integration.test.ts
    - server/orchestrator/index.ts
    - server/services/goal-proposals.ts
    - server/services/meal-numeric-proposals.ts
  </read_first>
  <action>Add failing tests that seed active meal numeric and goal proposals through their services. Prove broad cancel text `不要` / `取消` clears both proposal kinds and returns renderer-owned no-update copy without starting another LLM round; kind-specific cancel clears only that kind; bare approval `好` with both kinds active returns `renderProposalKindAmbiguityCopy` and mutates neither; kind-specific `套用餐點修改` applies only the active meal proposal; kind-specific `套用目標更新` leaves the meal proposal untouched and allows the existing goal path to proceed. Add stale meal proposal approval proof that the stored `expectedMealRevisionId` reaches the existing meal revision precondition path.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/orchestrator.test.ts tests/integration/chat-goal-update.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: both-active bare approval produces a renderer-owned disambiguation reply and no mutation.
    - Behavior assertion: broad cancel clears meal and goal proposals before model execution and returns no-update copy.
    - Behavior assertion: kind-specific meal approval commits only the stored backend-computed proposal and clears it after success.
    - Behavior assertion: stale proposal approval does not create a new revision and surfaces the existing revision precondition failure path.
  </acceptance_criteria>
  <done>All acceptance criteria pass, the automated verification command is green, and the implementation remains scoped to the files listed in this task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire meal proposal service and pre-model decisions</name>
  <files>server/orchestrator/index.ts, server/app.ts, tests/unit/orchestrator.test.ts, tests/integration/chat-goal-update.integration.test.ts</files>
  <read_first>
    - server/orchestrator/index.ts
    - server/app.ts
    - server/orchestrator/source-text-guard.ts
    - server/orchestrator/mutation-receipts.ts
    - server/services/meal-numeric-proposals.ts
    - server/services/meal-correction.ts
  </read_first>
  <action>Instantiate `createMealNumericProposalService(db)` in `server/app.ts`, expose it through `AppServices`, and pass it into `createOrchestrator`. Extend `OrchestratorDeps` with `mealNumericProposalService`. In `createOrchestrator().handleMessage`, after saving the user message and before the first LLM call, load active goal and meal proposals. Handle broad cancel first using shared cancel vocabulary plus meal-specific kind phrases, then bare approval ambiguity when more than one proposal kind is active, then kind-specific meal approval by calling `mealCorrectionService.updateMeal(deviceId, proposal.mealId, proposal.updateInput, proposal.expectedMealRevisionId)`. Return renderer-owned terminal replies for cancel, ambiguity, successful proposal apply, and stale apply failure; do not reconstruct proposal values from user prose. Preserve existing goal-only behavior unless needed for cross-kind fail-closed routing.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/orchestrator.test.ts tests/integration/chat-goal-update.integration.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `server/app.ts` creates and passes `mealNumericProposalService`.
    - Source assertion: proposal decision routing runs before model provider chat calls in `server/orchestrator/index.ts`.
    - Source assertion: meal approval uses `proposal.updateInput` and `proposal.expectedMealRevisionId`; it does not parse new numeric values from approval text.
    - Behavior assertion: renderer-owned proposal decisions return without a second model rewrite and with `didMutateMeal` true only after a successful stored meal proposal commit.
    - CLI assertion: targeted orchestrator tests and `yarn tsc --noEmit` pass.
  </acceptance_criteria>
  <done>All acceptance criteria pass, the automated verification command is green, and the implementation remains scoped to the files listed in this task.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user approval text -> proposal router | Short approval/cancel text is ambiguous and must be resolved against active backend proposal state. |
| active proposal state -> meal write | Stored proposal values are the only approved numeric payload; stale revisions must fail through existing preconditions. |
| app composition -> orchestrator deps | Runtime and tests must receive the same concrete service wiring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-14 | Tampering | bare approval with multiple proposal kinds | mitigate | Fail closed and return renderer-owned disambiguation when goal and meal proposals coexist. |
| T-66-15 | Elevation of Privilege | kind-specific approval | mitigate | Select only the active backend-owned proposal of the named kind; do not reconstruct or alter proposal values from prose. |
| T-66-16 | Tampering | stale proposal approval | mitigate | Apply through `mealCorrectionService.updateMeal` with stored `expectedMealRevisionId` so Phase 62 stale checks block revision writes. |
| T-66-17 | Repudiation | cancel behavior | mitigate | Broad cancel clears all active approvable proposal kinds and returns renderer-owned no-update copy. |
| T-66-18 | Information Disclosure | router replies and tests | mitigate | Replies avoid internal IDs/tool names; tests do not persist raw traces or DB snapshots. |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | No package installation is planned; Package Legitimacy Audit lists none. |
</threat_model>

<verification>
Task-level:
- `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/orchestrator.test.ts tests/integration/chat-goal-update.integration.test.ts`
- `yarn tsc --noEmit`

Wave-level:
- `yarn test:unit`
- `yarn test:integration`
</verification>

<success_criteria>
Approved meal numeric proposals commit only through stored backend state and expected revision checks.
Cross-kind proposal ambiguity fails closed before the model can choose a mutation path.
Broad and kind-specific cancel behavior clears proposal state without meal revisions, `daily_summary` publish, or success-style copy.
</success_criteria>

<output>
Create `.planning/phases/66-numeric-correction-provenance-guard/66-04-SUMMARY.md` when done.
</output>
