---
phase: 63-sse-meal-row-freshness-and-affected-date-invalidation
plan: 05
type: execute
wave: 3
depends_on: [63-04]
files_modified:
  - client/src/components/HistoryDayDetailScreen.tsx
  - tests/unit/history-day-detail-source-contract.test.ts
  - tests/unit/history-screen-contract.test.ts
autonomous: true
requirements: [REAL-03]
requirements_addressed: [REAL-03]
user_setup: []
must_haves:
  truths:
    - "Historical affected-date SSE invalidation refreshes an open Day Detail only when its `dateKey` matches `affectedDate`."
    - "The active History screen refreshes only the selected day or currently visible week, preserving the existing gated invalidation behavior."
    - "Historical affected-date events never refresh today's rows and never commit today's `dailySummary`."
    - "No new stale/error/freshness UI is added for historical invalidation."
  artifacts:
    - path: "client/src/components/HistoryDayDetailScreen.tsx"
      provides: "Open matching Day Detail refresh on `lastMealMutation`"
      contains: "lastMealMutation"
    - path: "tests/unit/history-day-detail-source-contract.test.ts"
      provides: "Source proof for matching-date Day Detail invalidation and existing visual contract"
    - path: "tests/unit/history-screen-contract.test.ts"
      provides: "Source proof preserving selected-day/current-week-only History invalidation"
  key_links:
    - from: "client/src/sse-summary-coordinator.ts"
      to: "client/src/store.ts"
      via: "`recordMealMutation(affectedDate)` updates `lastMealMutation`"
      pattern: "recordMealMutation"
    - from: "client/src/components/HistoryDayDetailScreen.tsx"
      to: "client/src/store.ts"
      via: "observes `lastMealMutation` and matches `dateKey`"
      pattern: "lastMealMutation.*affectedDate"
    - from: "client/src/components/HistoryScreen.tsx"
      to: "client/src/store.ts"
      via: "existing selected-day/current-week invalidation"
      pattern: "affectedWeekStartKey === weekStartKey"
---

<objective>
Complete historical affected-date invalidation by refreshing the matching visible historical surface and preserving passive invalidation elsewhere.

Purpose: The coordinator can record historical invalidation, but Phase 63 also requires visible historical surfaces to refresh only when the affected date is actually visible.
Output: Day Detail `lastMealMutation` refresh wiring and source-contract tests preserving History gating and UI constraints.
</objective>

<decision_coverage>
- D-26: Historical events never update today's summary, refresh today's rows, or invoke same-day reconcile; that remains coordinator responsibility from 63-04.
- D-27, D-29: Historical invalidation travels through `recordMealMutation(affectedDate)` / `lastMealMutation` and matching visible surfaces refresh.
- D-30, D-31: "Currently visible" means matching Day Detail or active History selected day/current week; History tab presence alone is not enough.
- D-32: Historical summary payload is not consumed as the historical data source; surfaces refetch.
- D-33: Visible historical refreshes use latest-wins cleanup/cancellation per affected date.
- D-34: No explicit event coalescing is required.
- UI-SPEC: Reuse existing loading/error/empty presentation only; no new freshness indicator, stale label, toast, banner, or redesign.
</decision_coverage>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | - | Users cannot see fresher daily totals beside stale same-day meal rows after realtime summary updates. | 63-01..63-05 | COVERED | This plan closes the historical non-today part of REAL-03 without touching today.
REQ | REAL-03 | Malformed, stale-date, or historical events preserve existing guards and do not overwrite current-day rows incorrectly. | 63-03, 63-04, 63-05 | COVERED | Matching historical visible refresh and passive invalidation are covered here.
RESEARCH | Day Detail does not observe historical mutation nonce pitfall | Open Day Detail must observe `lastMealMutation` for matching date. | 63-05 | COVERED | This plan wires it.
RESEARCH | Existing History selected-day/week invalidation | Preserve current gated History behavior; do not refresh because tab exists. | 63-05 | COVERED | Source-contract test guards it.
CONTEXT | D-26..D-34 | Historical affected-date invalidation and visible-surface refresh semantics. | 63-05 | COVERED | Future date and date-moving mutations remain deferred.
CONTEXT | UI-SPEC | No new visible stale/error UI or freshness indicator. | 63-05 | COVERED | Existing loading/error copy only.
</source_coverage_audit>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.codex/skills/nutrition-gen-test/SKILL.md
@.codex/skills/nutrition-verify-change/SKILL.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-CONTEXT.md
@.planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-RESEARCH.md
@.planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-VALIDATION.md
@.planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-PATTERNS.md
@.planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-UI-SPEC.md
@.planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-04-SUMMARY.md
@client/src/components/HistoryScreen.tsx
@client/src/components/HistoryDayDetailScreen.tsx
@client/src/store.ts
@client/src/api.ts
@tests/unit/history-screen-contract.test.ts
@tests/unit/history-day-detail-source-contract.test.ts

<interfaces>
From `client/src/store.ts`:
- State field: `lastMealMutation: MealMutationNotice | null`
- Action: `recordMealMutation(affectedDate: string)` increments `lastMealMutation.nonce`.

From `client/src/components/HistoryScreen.tsx`:
- Existing effect gates refresh by `affectedDate === selectedDateKey` and `affectedWeekStartKey === weekStartKey`.

From `client/src/components/HistoryDayDetailScreen.tsx`:
- Existing load source: `getHistoryDaySnapshot(dateKey)`.
- Existing cancellation pattern uses a local `cancelled` flag.
- Existing error copy: `當日詳情暫時載入失敗。請稍後再試。`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source-contract proof for historical visible refresh boundaries</name>
  <files>tests/unit/history-day-detail-source-contract.test.ts, tests/unit/history-screen-contract.test.ts</files>
  <read_first>
    AGENTS.md
    .codex/skills/nutrition-gen-test/SKILL.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-CONTEXT.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-PATTERNS.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-UI-SPEC.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-04-SUMMARY.md
    client/src/components/HistoryScreen.tsx
    client/src/components/HistoryDayDetailScreen.tsx
    client/src/store.ts
    tests/unit/history-screen-contract.test.ts
    tests/unit/history-day-detail-source-contract.test.ts
  </read_first>
  <action>Update `tests/unit/history-day-detail-source-contract.test.ts` to assert `HistoryDayDetailScreen.tsx` reads `lastMealMutation` from `useStore`, checks `lastMealMutation.affectedDate === dateKey`, includes `lastMealMutation?.nonce` or an equivalent nonce dependency in the refresh effect dependencies, and refetches through `getHistoryDaySnapshot(dateKey)` while preserving the existing cancellation guard. Assert the source does not call `setDailySummary`, `getMeals`, `setMeals`, or render new stale/freshness indicator copy. Update `tests/unit/history-screen-contract.test.ts` to retain existing selected-day/current-week gating assertions and add a negative assertion that History does not refresh solely because the History tab exists; require source patterns for `shouldRefreshDay`, `shouldRefreshWeek`, and early return when neither matches.</action>
  <acceptance_criteria>
    - Source-contract tests fail before Day Detail observes `lastMealMutation`.
    - Tests prove Day Detail refresh is keyed to exact `affectedDate === dateKey`.
    - Tests prove History retains selected-day/current-week-only invalidation.
    - Tests reject calls to current-day summary/meal-row APIs from historical surface refresh code.
    - Tests reject new visible stale/freshness UI strings or markers.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/history-day-detail-source-contract.test.ts tests/unit/history-screen-contract.test.ts</automated>
  </verify>
  <done>Historical visible refresh boundaries are captured before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire matching Day Detail refresh through `lastMealMutation`</name>
  <files>client/src/components/HistoryDayDetailScreen.tsx</files>
  <read_first>
    AGENTS.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-CONTEXT.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-RESEARCH.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-PATTERNS.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-UI-SPEC.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-04-SUMMARY.md
    client/src/components/HistoryDayDetailScreen.tsx
    client/src/components/HistoryScreen.tsx
    client/src/store.ts
    client/src/api.ts
    tests/unit/history-day-detail-source-contract.test.ts
    tests/unit/history-screen-contract.test.ts
  </read_first>
  <action>Update `HistoryDayDetailScreen.tsx` so it selects `lastMealMutation` from `useStore`. Add a matching-date refresh effect or refactor the existing date-key load into a reusable local loader so both initial load and invalidation refresh use `getHistoryDaySnapshot(dateKey)`, preserve `UNAUTHORIZED` recovery through `recoverGuestSession`, preserve existing loading/error/empty copy, and use cancellation/latest-wins cleanup so older in-flight refresh results cannot overwrite newer results for the same visible date. The invalidation effect must return immediately unless `lastMealMutation?.affectedDate === dateKey`. It must not call `setDailySummary`, `getMeals`, `setMeals`, same-day reconcile helpers, or consume the historical SSE `summary` as data. Do not add a toast, banner, stale label, freshness badge, new spinner specifically for SSE, or visual redesign.</action>
  <acceptance_criteria>
    - Open Day Detail refreshes only when `lastMealMutation.affectedDate` exactly matches its `dateKey`.
    - Day Detail uses `getHistoryDaySnapshot(dateKey)` as the data source and never consumes pushed historical `summary`.
    - Older in-flight Day Detail refresh results are dropped by cleanup/cancellation when superseded.
    - Existing loading/error/empty visual copy remains unchanged except for unavoidable refactor movement.
    - No current-day summary or meal-row state action is called by Day Detail invalidation.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/history-day-detail-source-contract.test.ts tests/unit/history-screen-contract.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Matching open Day Detail refreshes on historical affected-date invalidation without touching today state or adding UI.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 63 client and integration closure checks</name>
  <files>client/src/components/HistoryDayDetailScreen.tsx, tests/unit/history-day-detail-source-contract.test.ts, tests/unit/history-screen-contract.test.ts</files>
  <read_first>
    AGENTS.md
    .codex/skills/nutrition-verify-change/SKILL.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-VALIDATION.md
    client/src/components/HistoryDayDetailScreen.tsx
    tests/unit/history-day-detail-source-contract.test.ts
    tests/unit/history-screen-contract.test.ts
  </read_first>
  <action>Apply the Nutrition Coach verification matrix for this plan and the completed Phase 63 file set. Run the targeted Day Detail/History source-contract command, then `yarn tsc --noEmit` because TypeScript changed, `yarn test:unit` because unit tests and client helpers changed, and `yarn test:integration` because Phase 63 backend plans changed route/publisher behavior. Do not run Railway smoke, do not run `yarn release:check`, do not promote to `staging` or `main`, and do not edit generated harness artifacts.</action>
  <acceptance_criteria>
    - Targeted Day Detail/History tests pass.
    - `yarn tsc --noEmit` passes.
    - `yarn test:unit` passes.
    - `yarn test:integration` passes.
    - No staging/main promotion, deployed-domain smoke, release check, dependency installation, or generated harness artifact edit occurs.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/history-day-detail-source-contract.test.ts tests/unit/history-screen-contract.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
    <automated>yarn test:unit</automated>
    <automated>yarn test:integration</automated>
  </verify>
  <done>Historical visible refresh behavior and full Phase 63 local gates are verified without promotion actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `lastMealMutation` -> historical UI | Store invalidation nonce triggers visible historical refetch. |
| historical UI -> REST API | Day Detail and History fetch historical snapshots/trends. |
| async fetch -> component state | Historical refresh promises can resolve out of order. |
| tests/logs -> proof artifacts | Source-contract tests must stay metadata-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-05-01 | Tampering | `HistoryDayDetailScreen.tsx` | mitigate | Refresh only when `lastMealMutation.affectedDate === dateKey`; never call today summary/meal state actions. |
| T-63-05-02 | Tampering | historical fetch effects | mitigate | Use cancellation/latest-wins cleanup so older fetches cannot overwrite newer visible-date results. |
| T-63-05-03 | Tampering | `HistoryScreen.tsx` existing invalidation | mitigate | Source-contract tests preserve selected-day/current-week gating and early return when neither matches. |
| T-63-05-04 | Information Disclosure | historical tests | mitigate | Tests inspect source patterns and synthetic strings only; no raw prompts, provider bodies, images, sessions, or DB snapshots. |
| T-63-05-05 | Denial of Service | repeated historical invalidations | accept | One visible-surface refetch per matching nonce is bounded and D-34 explicitly does not require coalescing. |
</threat_model>

<verification>
- `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/history-day-detail-source-contract.test.ts tests/unit/history-screen-contract.test.ts`
- `yarn tsc --noEmit`
- `yarn test:unit`
- `yarn test:integration`
</verification>

<success_criteria>
- Matching open Day Detail refreshes after historical affected-date invalidation.
- Non-matching historical dates remain passive invalidation only.
- Existing History selected-day/current-week behavior is preserved.
- Historical refresh does not update today rows or summary.
- Targeted, TypeScript, unit-suite, and integration-suite checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-05-SUMMARY.md`
</output>
