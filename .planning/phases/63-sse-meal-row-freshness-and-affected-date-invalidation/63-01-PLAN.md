---
phase: 63-sse-meal-row-freshness-and-affected-date-invalidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/realtime/publisher.ts
  - server/routes/sse.ts
  - tests/integration/sse.test.ts
autonomous: true
requirements: [REAL-01, REAL-03]
requirements_addressed: [REAL-01, REAL-03]
user_setup: []
must_haves:
  truths:
    - "A new `/api/sse` connection receives a `daily_summary` frame shaped as `{ summary, affectedDate, source: \"initial\" }`."
    - "The initial envelope has `affectedDate` equal to `summary.date`."
    - "The realtime publisher remains fan-out only and performs no database or summary reads."
  artifacts:
    - path: "server/realtime/publisher.ts"
      provides: "Fan-out-only `publishDailySummary` envelope boundary"
      contains: "DailySummarySSEPayload"
    - path: "server/routes/sse.ts"
      provides: "Authenticated initial `daily_summary` envelope with `source: \"initial\"`"
      contains: "source: \"initial\""
    - path: "tests/integration/sse.test.ts"
      provides: "Integration proof for initial SSE envelope and existing auth guard"
  key_links:
    - from: "server/routes/sse.ts"
      to: "server/realtime/publisher.ts"
      via: "shared `DailySummarySSEPayload` shape"
      pattern: "affectedDate.*summary\\.date|summary\\.date.*affectedDate"
    - from: "tests/integration/sse.test.ts"
      to: "/api/sse"
      via: "Fastify SSE connection test"
      pattern: "source.*initial"
---

<objective>
Create the backend envelope contract for `daily_summary` SSE frames and prove the authenticated initial snapshot uses it.

Purpose: Phase 63 cannot make client-side freshness decisions until every `daily_summary` frame has the strict metadata envelope required by D-01 through D-05.
Output: realtime publisher envelope type with a compile-safe migration bridge, initial SSE envelope emission, and integration proof for the initial connection frame.
</objective>

<decision_coverage>
- D-01, D-02, D-03: Publisher and initial route emit `{ summary, affectedDate, source }`.
- D-04: No `summaryOutcome.status`, changed ids, changed revision ids, or high-watermark is added.
- D-05: Initial route sets `affectedDate` from `summary.date`.
- D-14: Server still sends `source: "initial"` on every SSE connection.
- D-20: Server only emits the envelope; client parsing/refetch behavior is planned separately.
- D-35, D-36: Malformed client handling is planned in 63-03; this plan preserves server-side valid shape.
- D-44, D-46: Publisher remains fan-out only; route supplies metadata.
</decision_coverage>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | - | Users cannot see fresher daily totals beside stale same-day meal rows after realtime summary updates. | 63-01..63-05 | COVERED | This plan establishes metadata needed for client reconciliation.
REQ | REAL-01 | Same-day `daily_summary` SSE events include enough freshness metadata for clients to refresh or invalidate meal rows. | 63-01, 63-02 | COVERED | Initial envelope is covered here; mutation envelope is covered in 63-02.
REQ | REAL-03 | Malformed, stale-date, or historical events preserve guards and do not overwrite current-day rows incorrectly. | 63-01, 63-03, 63-04, 63-05 | COVERED | This plan keeps initial source/date shape valid; client rejection/routing is covered later.
RESEARCH | Route-owned envelope, publisher-owned fan-out | Routes pass computed summary/date/source and publisher only serializes/fans out. | 63-01, 63-02 | COVERED | No DB reads in publisher.
RESEARCH | Preserve cookie-backed SSE auth | `/api/sse` must keep `resolveGuestSession`; no device query/header trust. | 63-01 | COVERED | Initial route auth guard remains unchanged.
CONTEXT | D-01..D-05, D-14, D-20, D-44, D-46 | Strict envelope and fan-out-only initial emission. | 63-01 | COVERED | Deferred date-moving and future-date mutation policy are not planned.
</source_coverage_audit>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.codex/skills/nutrition-gen-test/SKILL.md
@.codex/skills/nutrition-verify-change/SKILL.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-CONTEXT.md
@.planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-RESEARCH.md
@.planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-VALIDATION.md
@.planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-PATTERNS.md
@server/realtime/publisher.ts
@server/routes/sse.ts
@tests/integration/sse.test.ts

<interfaces>
From `server/realtime/publisher.ts`:
- `export class RealtimePublisher`
- Current method to widen without breaking existing route call sites in this plan: `publishDailySummary(deviceId: string, summary: DailySummary)`

From `server/routes/sse.ts`:
- `export function registerSSERoutes(app: FastifyInstance, deps: Deps)`
- Existing auth boundary: `resolveGuestSession(request, { deviceService, guestSessionService })`
- Existing initial frame source: `summaryService.getDailySummary(deviceId, currentAppDate())`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove initial SSE frame uses the strict envelope</name>
  <files>tests/integration/sse.test.ts</files>
  <read_first>
    AGENTS.md
    .codex/skills/nutrition-gen-test/SKILL.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-CONTEXT.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-PATTERNS.md
    server/routes/sse.ts
    server/realtime/publisher.ts
    tests/integration/sse.test.ts
  </read_first>
  <action>Add or update a focused integration assertion in `tests/integration/sse.test.ts` for `GET /api/sse` using the existing Fastify app fixture and real SQLite setup. Parse the first `event: daily_summary` frame and assert the JSON body is an object with exactly the Phase 63 envelope fields `summary`, `affectedDate`, and `source`; assert `source === "initial"`, `affectedDate === summary.date`, and the nested `summary` retains valid numeric daily summary fields. Keep existing unauthorized SSE subscription coverage intact and do not add any device id query/header auth path.</action>
  <acceptance_criteria>
    - `tests/integration/sse.test.ts` fails against the current raw `DailySummary` payload because it lacks `summary`, `affectedDate`, and `source`.
    - The test asserts `source === "initial"` and `affectedDate === summary.date`.
    - The test keeps `/api/sse` authorization based on guest-session cookies and does not introduce raw `deviceId` trust.
    - Test fixtures do not log or persist raw user text, provider bodies, image data, session material, or database snapshots.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/sse.test.ts</automated>
  </verify>
  <done>Initial SSE envelope proof exists and fails before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement fan-out-only initial `daily_summary` envelope</name>
  <files>server/realtime/publisher.ts, server/routes/sse.ts</files>
  <read_first>
    AGENTS.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-CONTEXT.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-RESEARCH.md
    .planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-PATTERNS.md
    server/realtime/publisher.ts
    server/routes/sse.ts
    tests/integration/sse.test.ts
  </read_first>
  <action>In `server/realtime/publisher.ts`, define and export a server-side `DailySummarySSESource` type with the exact literals `"initial" | "meal_mutation"` and a `DailySummarySSEPayload` type containing `summary: DailySummary`, `affectedDate: string`, and `source: DailySummarySSESource`. Change `RealtimePublisher.publishDailySummary` to accept either `DailySummarySSEPayload` or the existing raw `DailySummary` input so 63-01 remains independently type-checkable before chat and meals route call sites move in 63-02. For raw `DailySummary` inputs, preserve the existing raw publish behavior in this plan; for `DailySummarySSEPayload`, pass the envelope unchanged to the private `publish()` helper. Do not add DB reads, summary-service imports, route knowledge, event coalescing, revision fields, summaryOutcome fields, or logging inside the publisher. In `server/routes/sse.ts`, keep `resolveGuestSession`, headers, keepalive, cookie handling, and cleanup unchanged; after `summaryService.getDailySummary(deviceId, currentAppDate())`, write `event: daily_summary` with `JSON.stringify({ summary, affectedDate: summary.date, source: "initial" })`. Do not use `currentAppDate()` separately for `affectedDate`; it must equal `summary.date` per D-05.</action>
  <acceptance_criteria>
    - `publishDailySummary` exports the strict envelope type and accepts both the new envelope and existing raw `DailySummary` call sites so `yarn tsc --noEmit` passes at the end of 63-01.
    - `server/routes/sse.ts` initial frame emits `{ summary, affectedDate: summary.date, source: "initial" }`.
    - No `summaryOutcome`, changed meal ids, changed revision ids, `mealRevisionId`, high-watermark, schema migration, or new dependency is introduced.
    - `/api/sse` still rejects unauthorized requests through `resolveGuestSession` and does not trust query params or headers for ownership.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/sse.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Initial `daily_summary` SSE frames use the strict envelope while the publisher remains fan-out only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `/api/sse` | EventSource connects with ambient same-origin cookies. |
| `/api/sse` -> `RealtimePublisher` | Authenticated route creates initial SSE frame and publisher subscriptions. |
| publisher -> browser | Process-local fan-out serializes payloads to long-lived replies. |
| tests/logs -> proof artifacts | Integration tests can accidentally capture sensitive runtime material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01-01 | Spoofing | `server/routes/sse.ts` | mitigate | Preserve `resolveGuestSession`; do not add `deviceId` query/header auth for EventSource. |
| T-63-01-02 | Tampering | `server/routes/sse.ts` initial frame | mitigate | Emit `affectedDate: summary.date` and `source: "initial"` so client can reject mismatched envelopes later. |
| T-63-01-03 | Information Disclosure | `server/realtime/publisher.ts` | mitigate | Keep publisher fan-out only and avoid logs containing raw user/provider/session/database data. |
| T-63-01-04 | Denial of Service | `RealtimePublisher.publish()` | accept | Existing stale-reply cleanup remains unchanged; this plan only changes payload shape. |
| T-63-01-05 | Information Disclosure | integration tests | mitigate | Assertions inspect metadata and summary numbers only; no raw prompts, provider bodies, image data, cookies, or DB snapshots are persisted. |
</threat_model>

<verification>
- `node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/sse.test.ts`
- `yarn tsc --noEmit`
</verification>

<success_criteria>
- Initial `daily_summary` frames use the strict Phase 63 envelope.
- Publisher exposes the strict envelope contract while preserving compile compatibility for unmigrated route call sites until 63-02 removes raw call-site usage.
- Publisher does no route-specific logic and no persistence reads.
- Authenticated SSE subscription behavior is unchanged.
- Targeted integration and TypeScript checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/63-sse-meal-row-freshness-and-affected-date-invalidation/63-01-SUMMARY.md`
</output>
