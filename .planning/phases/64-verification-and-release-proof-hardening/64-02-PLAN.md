---
phase: 64-verification-and-release-proof-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 64-01
files_modified:
  - tests/unit/phase64-metadata-sweep.test.ts
  - .planning/phases/64-verification-and-release-proof-hardening/64-VERIFICATION.md
autonomous: true
requirements:
  - PROOF-02
must_haves:
  truths:
    - "PROOF-02 sweep runs before any PROOF-01 behavior-test additions."
    - "Generated artifacts, structured trace/log evidence, and route/orchestrator evidence paths are checked against Tier 1 and Tier 2 metadata-only policy."
    - "`64-VERIFICATION.md` records sweep metadata only and stores no raw matches or raw evidence payloads."
  artifacts:
    - path: "tests/unit/phase64-metadata-sweep.test.ts"
      provides: "Phase 64 artifact enumeration and denylist sweep"
      exports: []
    - path: ".planning/phases/64-verification-and-release-proof-hardening/64-VERIFICATION.md"
      provides: "PROOF-02 Metadata-Only Sweep report"
      contains: "PROOF-02 Metadata-Only Sweep"
  key_links:
    - from: "tests/unit/phase64-metadata-sweep.test.ts"
      to: "tests/harness/artifacts/**"
      via: "recursive metadata-only artifact enumeration"
      pattern: "tests/harness/artifacts"
    - from: "tests/unit/verification-artifacts.test.ts"
      to: "tests/harness/artifacts.ts"
      via: "artifact writer redaction contracts"
      pattern: "writeScenarioArtifacts"
    - from: "tests/unit/llm-chat-trace.test.ts"
      to: "server/orchestrator/llm-trace.ts"
      via: "trace/log metadata-only contracts"
      pattern: "createLlmTraceRecorder"
---

<objective>
Add and run the Phase 64 PROOF-02 metadata-only sweep before any behavior-test expansion.

Purpose: Locked decisions D-02, D-18 through D-31, D-36 through D-39, and D-45 require a cross-phase privacy sweep over persisted and emitted evidence surfaces, including all local harness artifacts.
Output: A focused unit sweep plus `64-VERIFICATION.md` tables for inspected surfaces, denylist coverage, command results, and escalations.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/64-verification-and-release-proof-hardening/64-CONTEXT.md
@.planning/phases/64-verification-and-release-proof-hardening/64-RESEARCH.md
@.planning/phases/64-verification-and-release-proof-hardening/64-VALIDATION.md
@.planning/phases/64-verification-and-release-proof-hardening/64-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md
@tests/unit/verification-artifacts.test.ts
@tests/unit/llm-chat-trace.test.ts
@tests/harness/artifacts.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase 64 metadata sweep test</name>
  <files>tests/unit/phase64-metadata-sweep.test.ts</files>
  <read_first>
    - `tests/unit/phase64-metadata-sweep.test.ts` if it already exists
    - `tests/unit/verification-artifacts.test.ts`
    - `tests/unit/llm-chat-trace.test.ts`
    - `tests/harness/artifacts.ts`
    - `.planning/phases/64-verification-and-release-proof-hardening/64-CONTEXT.md`
    - `.planning/phases/64-verification-and-release-proof-hardening/64-PATTERNS.md`
    - `.codex/skills/nutrition-gen-test/SKILL.md`
  </read_first>
  <behavior>
    - Test 1: recursively enumerates every on-disk file under `tests/harness/artifacts/**` and fails if no enumeration occurs when the directory exists.
    - Test 2: checks text artifacts for Tier 1 and Tier 2 forbidden evidence without printing raw matched content in assertion messages.
    - Test 3: classifies binary artifacts separately by path, extension/type, and byte size; binary screenshots are not treated as text, and raw uploaded/base64/provider image payloads remain forbidden text evidence.
    - Test 4: confirms existing trace and artifact redaction contract tests are the required companion proof for structured logs, trace facts, and route/orchestrator evidence paths.
  </behavior>
  <action>Create `tests/unit/phase64-metadata-sweep.test.ts` using Node built-in `node:test`, `node:assert/strict`, `node:fs`, and `node:path`, with explicit `.js` local imports only if needed. Define a Tier 1 registry from D-22: raw prompts, user text, assistant final text, tool payloads, provider bodies, image data, session material, and database snapshots. Define a Tier 2 registry from D-23 using existing operational denylist terms in `verification-artifacts.test.ts`, `llm-chat-trace.test.ts`, and `tests/harness/artifacts.ts`: API keys, bearer/auth headers, cookies, device/session identifiers, upload paths, error stacks, internal schema, raw tool args/results, raw messages, and provider request/body/header material. The test must report only file path, tier, file count, binary count, and match count metadata in failure messages; it must not include the matched substring or surrounding content. Follow D-24 by allowing additional Tier 2 terms if the sweep exposes a new risk, but do not remove Tier 2 entries without escalation. Do not create a harness scenario or release-proof bundle in this task, per D-32 through D-35. Do not hand-edit generated harness artifacts under `tests/harness/artifacts/**`; D-41 requires regeneration through the matching harness command if artifact remediation later becomes necessary. After creating or editing this TypeScript unit test, run the changed-file gate plus AGENTS.md-required TypeScript and unit gates: `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/phase64-metadata-sweep.test.ts`, `yarn tsc --noEmit`, and `yarn test:unit`.</action>
  <acceptance_criteria>
    - Source assertion: `tests/unit/phase64-metadata-sweep.test.ts` imports `node:test` and `node:assert/strict`.
    - Source assertion: the test includes Tier 1 labels from D-22 and Tier 2 labels from D-23.
    - Source assertion: assertion failure messages include path/count/tier metadata but do not include raw matched snippets.
    - Source assertion: binary artifact handling records path/type/size metadata separately from text matching, per D-36b.
    - Decision assertion: D-02, D-18, D-20, D-21, D-22, D-23, D-24, D-26, D-27, D-32, D-33, D-34, D-35, D-36, D-36a, D-36b, D-37, D-38, D-39, D-41, and D-45 are represented in test names, comments, or verification notes.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/phase64-metadata-sweep.test.ts && yarn tsc --noEmit && yarn test:unit</automated>
  </verify>
  <done>The Phase 64 metadata sweep test exists and can enumerate/sweep harness artifacts without leaking raw matched content through its own failure output.</done>
</task>

<task type="auto">
  <name>Task 2: Run PROOF-02 companion tests and record sweep metadata</name>
  <files>.planning/phases/64-verification-and-release-proof-hardening/64-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/64-verification-and-release-proof-hardening/64-VERIFICATION.md`
    - `tests/unit/phase64-metadata-sweep.test.ts`
    - `tests/unit/verification-artifacts.test.ts`
    - `tests/unit/llm-chat-trace.test.ts`
    - `.planning/phases/64-verification-and-release-proof-hardening/64-CONTEXT.md`
  </read_first>
  <action>Run `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/phase64-metadata-sweep.test.ts tests/unit/verification-artifacts.test.ts tests/unit/llm-chat-trace.test.ts`. Update `64-VERIFICATION.md` with a section named exactly `PROOF-02 Metadata-Only Sweep`, per D-29. Include structured tables for inspected surfaces, Tier 1/Tier 2 denylist coverage, command results, artifact text-file count, artifact binary-file count, match count, pass/fail/escalation status, and facts proven. Per D-25, treat Tier 1 or Tier 2 matches in persisted or emitted evidence as blockers. Per D-39, if persisted matches are found, PROOF-02 cannot close until generated artifacts are cleaned through the proper harness command where applicable and the producing redaction/emission path is fixed or verified unreachable. Delete-only remediation is explicitly forbidden when a producer can recreate the leak. Per D-28, escalate gray-zone emission paths such as request logging middleware, production trace callbacks, or CI stdout capture with metadata only. Per D-30, never store raw matched content, raw evidence payloads, raw screenshots, prompt text, user text, assistant final text, tool payloads, provider bodies, image data payloads, session material, database snapshots, stack traces, headers, cookies, or upload paths in `64-VERIFICATION.md`. Per D-31, do not add machine-readable JSON unless Markdown cannot avoid false-pass; if that becomes true, stop and ask for replanning before writing JSON.</action>
  <acceptance_criteria>
    - CLI assertion: the PROOF-02 command runs with `phase64-metadata-sweep.test.ts`, `verification-artifacts.test.ts`, and `llm-chat-trace.test.ts`.
    - Source assertion: `64-VERIFICATION.md` includes `PROOF-02 Metadata-Only Sweep`.
    - Source assertion: `64-VERIFICATION.md` records inspected surfaces, denylist tiers, counts, command status, and facts proven, not raw content.
    - Decision assertion: D-19, D-25, D-28, D-29, D-30, and D-31 are represented in sweep notes or escalation policy.
    - Behavior assertion: any persisted/emitted Tier 1 or Tier 2 match is recorded as a blocker or escalation, not ignored.
    - Remediation assertion: if persisted matches are found, PROOF-02 remains open until artifacts are cleaned through the proper harness command where applicable and the producing redaction/emission path is fixed or verified unreachable; delete-only cleanup is not acceptable when a producer can recreate the leak.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'PROOF-02 Metadata-Only Sweep' .planning/phases/64-verification-and-release-proof-hardening/64-VERIFICATION.md && node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/phase64-metadata-sweep.test.ts tests/unit/verification-artifacts.test.ts tests/unit/llm-chat-trace.test.ts</automated>
  </verify>
  <done>PROOF-02 sweep evidence is recorded as metadata-only tables, and any blocker/escalation includes D-39 producer-path remediation before PROOF-01 behavior proof decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness artifacts -> unit test output | Persisted local evidence is inspected by test code and may contain sensitive strings. |
| test output -> verification report | Sweep results cross into a durable planning artifact. |
| route/orchestrator/log evidence -> privacy proof | Metadata-only trace/log facts must not become raw payload release proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-04 | Information Disclosure | `tests/unit/phase64-metadata-sweep.test.ts` | mitigate | Failure messages include only path, tier, and counts; never raw matches or surrounding content. |
| T-64-05 | Information Disclosure | `tests/harness/artifacts/**` | mitigate | Enumerate all text and binary artifacts; classify binary screenshots separately; block raw image payload, session, prompt, provider, tool, and database evidence. |
| T-64-06 | Repudiation | `64-VERIFICATION.md` PROOF-02 tables | mitigate | Record command, inspected surface, counts, and status so later review can prove what was swept without storing sensitive payloads. |
| T-64-07 | Tampering | Denylist registry | mitigate | Treat Tier 1 as the non-negotiable policy floor and require escalation before removing Tier 2 terms. |
</threat_model>

<verification>
Run the PROOF-02 companion command and confirm `64-VERIFICATION.md` contains only metadata tables for the sweep.
</verification>

<success_criteria>
- The sweep test exists and passes or fails safely without raw matched content.
- All `tests/harness/artifacts/**` files are enumerated.
- Binary artifacts are classified separately.
- Existing trace/artifact privacy tests remain part of PROOF-02 proof.
- No behavior tests are added before this sweep completes.
</success_criteria>

<output>
After completion, create `.planning/phases/64-verification-and-release-proof-hardening/64-02-SUMMARY.md`
</output>
