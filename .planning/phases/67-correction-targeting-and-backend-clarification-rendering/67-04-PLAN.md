---
phase: 67-correction-targeting-and-backend-clarification-rendering
plan: 04
type: execute
wave: 3
depends_on:
  - 67-03
files_modified:
  - server/orchestrator/index.ts
  - server/orchestrator/system-prompt.ts
  - tests/unit/orchestrator.test.ts
  - tests/unit/system-prompt.test.ts
autonomous: true
requirements:
  - TARGET-01
  - TARGET-02
user_setup: []
must_haves:
  truths:
    - "The orchestrator returns renderer-owned correction clarification without asking the LLM for a second final answer."
    - "Legacy raw-user-message correction clarification rendering is removed or unreachable for update/delete targeting."
    - "Prompt guidance supports backend target authority without becoming the enforcement mechanism."
  artifacts:
    - path: "server/orchestrator/index.ts"
      provides: "Controlled reply terminal behavior and removal of raw correction-target fallback rendering"
    - path: "server/orchestrator/system-prompt.ts"
      provides: "Support-only model guidance aligned with backend-owned resolver policy"
    - path: "tests/unit/orchestrator.test.ts"
      provides: "Proof that backend clarification is final and LLM rewrite is not called"
  key_links:
    - from: "server/orchestrator/tools.ts"
      to: "server/orchestrator/index.ts"
      via: "ToolExecutionResult.controlledReply"
      pattern: "controlledReply"
    - from: "server/orchestrator/system-prompt.ts"
      to: "server/orchestrator/tools.ts"
      via: "Prompt instructs model to call find_meals and preserve target query, not select candidates"
      pattern: "find_meals"
---

<objective>
Wire renderer-owned correction clarification through the orchestrator and align prompt support copy.

Purpose: ensure backend-rendered clarification is the final reply for ambiguous update/delete target resolution and no legacy raw correction echo path remains.
Output: green orchestrator and prompt tests proving one-round terminal renderer behavior.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-CONTEXT.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-RESEARCH.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-PATTERNS.md
@.planning/phases/66-numeric-correction-provenance-guard/66-VERIFICATION.md
@.codex/skills/nutrition-gen-test/SKILL.md
@.codex/skills/nutrition-verify-change/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove legacy raw correction clarification rendering from the orchestrator path</name>
  <files>server/orchestrator/index.ts, tests/unit/orchestrator.test.ts</files>
  <read_first>
    @server/orchestrator/index.ts
    @tests/unit/orchestrator.test.ts
    @server/orchestrator/tools.ts
    @server/orchestrator/mutation-receipts.ts
    @server/llm/mock.ts
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-CONTEXT.md
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-PATTERNS.md
  </read_first>
  <action>Update `server/orchestrator/index.ts` so correction-target `controlledReply` from `find_meals` is terminal through the existing controlled-reply short-circuit. Remove or make unreachable the legacy `buildCorrectionClarificationReply()` path that derives labels from `userMessage` via `extractUserCorrectionTarget()`. Strengthen `tests/unit/orchestrator.test.ts` so a queued second LLM response attempting to echo `中午雞腿便當` or claim `已更新` is never consumed; assert `finalReplySource === "renderer"`, `didMutateMeal === false`, `finalReplyShape === "plain_text"`, and `mockLLM.chatCalls.length === 1`. Implement D-26, D-27, D-28, D-32, and D-33.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `index.ts` no longer builds correction target labels from raw `userMessage` for `find_meals` clarification.
    - Behavior assertion: ambiguous correction with rendered options returns the exact backend renderer copy and consumes only one LLM response.
    - Behavior assertion: orchestrator result has `didLogMeal: false`, `didMutateMeal: false`, and no success-style update/delete copy on clarification.
  </acceptance_criteria>
  <done>Orchestrator-controlled correction clarification is terminal and legacy raw-message rendering is removed or unreachable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align model-facing correction prompt with backend resolver authority</name>
  <files>server/orchestrator/system-prompt.ts, tests/unit/system-prompt.test.ts</files>
  <read_first>
    @server/orchestrator/system-prompt.ts
    @tests/unit/system-prompt.test.ts
    @server/orchestrator/tools.ts
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-CONTEXT.md
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-RESEARCH.md
  </read_first>
  <action>Update the meal correction prompt section as support-only guidance. It must tell the model to call `find_meals` for update/delete targeting, preserve user-provided food/item/date/period words in `find_meals.query`, never choose one meal from a candidate list, never rewrite backend-rendered clarification, and call mutators only after `find_meals` resolves one target. Keep Phase 66 numeric authority guidance intact: explicit current-turn numbers or computable proposal operators only. Add/update `tests/unit/system-prompt.test.ts` assertions for D-10, D-11, D-12, D-18, D-19, D-32, D-33, D-42, and D-43.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/system-prompt.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: prompt includes `find_meals` before mutation guidance and no wording that authorizes the model to select a candidate by prose.
    - Source assertion: prompt preserves Phase 66 wording that vague numeric intent such as `合理一點` cannot directly call `update_meal`.
    - Behavior assertion: system prompt tests pass without requiring any external LLM call.
  </acceptance_criteria>
  <done>Prompt guidance supports backend resolver and numeric authority boundaries without acting as the enforcement layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool result -> orchestrator final reply | Controlled renderer text crosses into user-visible response. |
| prompt -> model behavior | Support guidance can influence but must not enforce target authority. |
| model response -> mutation claims | Model-authored final text may attempt to claim mutation without backend commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Elevation of privilege | orchestrator target handling | mitigate | Mutators remain gated on resolver-owned `resolvedMealTargets`; this plan does not accept model-selected ids. |
| T-67-02 | Tampering | LLM paraphrase/retargeting | mitigate | Controlled correction clarification returns immediately; tests prove second LLM response is not consumed. |
| T-67-03 | Tampering | stale overwrite | transfer | Stale mutation checks remain in service/tool plans; orchestrator must preserve no-mutation fields when controlled reply is returned. |
| T-67-04 | Information disclosure | legacy raw user target renderer | mitigate | Remove or bypass raw `userMessage` target-label extraction for correction clarification. |
| T-67-05 | Tampering | SQL/query safety | accept | No database query changes in this plan. |
| T-67-SC | Tampering | package installs | mitigate | No dependency additions. |
</threat_model>

<verification>
Run `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/orchestrator.test.ts tests/unit/system-prompt.test.ts`, then `yarn tsc --noEmit`.
</verification>

<success_criteria>
Ambiguous update/delete target clarification cannot be rewritten by the model, and prompt guidance matches backend-owned resolver and Phase 66 numeric authority contracts.
</success_criteria>

<output>
Create `.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-04-SUMMARY.md` when done.
</output>
