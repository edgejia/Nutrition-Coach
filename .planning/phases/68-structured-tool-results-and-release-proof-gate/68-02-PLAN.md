---
phase: 68-structured-tool-results-and-release-proof-gate
plan: 02
type: execute
wave: 1
depends_on:
  - 68-01
files_modified:
  - server/orchestrator/tools.ts
  - server/orchestrator/mutation-receipts.ts
  - tests/unit/tools.test.ts
  - tests/unit/orchestrator.test.ts
autonomous: true
requirements:
  - TARGET-03
  - PROOF-01
user_setup: []
must_haves:
  truths:
    - "`executeTool()` remains the adapter seam and raw `contractResult` does not leak into `server/orchestrator/index.ts`."
    - "`ToolExecutionResult` carries a narrow discriminated clarification union for unresolved `find_meals`, historical `log_food`, and historical `get_daily_summary`."
    - "Renderer/copy helpers own terminal clarification copy; `server/orchestrator/index.ts` does not render or parse clarification results."
    - "Historical terminal clarifications have no mutation, no summary, no success receipt, no success-style copy, and no second LLM pass."
  artifacts:
    - path: "server/orchestrator/tools.ts"
      provides: "Typed ToolExecutionResult clarification union and executeTool adapter mapping"
      exports: ["ToolExecutionResult", "executeTool"]
    - path: "server/orchestrator/mutation-receipts.ts"
      provides: "Renderer-owned historical clarification copy helpers"
    - path: "tests/unit/tools.test.ts"
      provides: "Green unit proof for structured clarification facts"
    - path: "tests/unit/orchestrator.test.ts"
      provides: "Green unit proof for terminal renderer ownership and source guard"
  key_links:
    - from: "server/orchestrator/tools.ts"
      to: "server/orchestrator/mutation-receipts.ts"
      via: "historical clarification renderer helper imports"
      pattern: "renderHistorical"
    - from: "server/orchestrator/index.ts"
      to: "server/orchestrator/tools.ts"
      via: "ToolExecutionResult.controlledReply only"
      pattern: "controlledReply"
---

<objective>
Implement the structured tool-result boundary for TARGET-03.

Purpose: replace brittle serialized clarification-result behavior with typed renderer-ready facts while preserving `executeTool()` as the adapter seam.
Output: production code and green unit tests for typed clarification facts, renderer terminal copy, no second LLM pass, and source-guard cleanup.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/codex.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/68-structured-tool-results-and-release-proof-gate/68-CONTEXT.md
@.planning/phases/68-structured-tool-results-and-release-proof-gate/68-RESEARCH.md
@.planning/phases/68-structured-tool-results-and-release-proof-gate/68-VALIDATION.md
@.planning/phases/68-structured-tool-results-and-release-proof-gate/68-PATTERNS.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
@.planning/phases/66-numeric-correction-provenance-guard/66-CONTEXT.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-CONTEXT.md
@.codex/skills/nutrition-gen-test/SKILL.md
@.codex/skills/nutrition-verify-change/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define renderer-ready clarification facts and copy helpers</name>
  <files>server/orchestrator/tools.ts, server/orchestrator/mutation-receipts.ts, tests/unit/tools.test.ts</files>
  <read_first>
    @server/orchestrator/tools.ts
    @server/orchestrator/mutation-receipts.ts
    @server/orchestrator/tool-contract.ts
    @server/services/meal-correction.ts
    @server/lib/historical-date.ts
    @tests/unit/tools.test.ts
    @.planning/phases/68-structured-tool-results-and-release-proof-gate/68-CONTEXT.md
    @.planning/phases/68-structured-tool-results-and-release-proof-gate/68-PATTERNS.md
  </read_first>
  <behavior>
    - The public adapter type includes a narrow discriminated union, for example `ToolClarificationFact`, assigned to `ToolExecutionResult.clarification`.
    - Union variants cover `find_meals` target clarification, historical `log_food` date clarification, `get_daily_summary` date clarification, and `get_daily_summary` multiple targets with `dateKeys`.
    - Candidate facts use an allowlisted projection only: stable option number, `dateKey`, display time, display label, and explicit meal-period facts when source is explicit.
  </behavior>
  <action>In `server/orchestrator/tools.ts`, extend `ToolExecutionResult` with a typed clarification/status field per D-02, D-03, and D-04. Use a discriminant name that is stable and readable, such as `kind`, with variants for `meal_target`, `historical_log_food`, and `historical_summary`; represent summary `multiple_targets` as its own status carrying `dateKeys`. Keep raw `contractResult`, full `MealCorrectionCandidate`, meal id, and meal revision out of the renderer/proof projection by default per D-01, D-05, and D-06. In `server/orchestrator/mutation-receipts.ts`, add renderer-owned helpers for historical log-food clarification, summary clarification, and summary multiple-target narrow-to-one-date copy per D-07, D-09, D-15, and D-16. The `multiple_targets` helper must ask the user to pick one date from typed `dateKeys`, must not imply multi-date aggregation per D-14, and must be shaped so `extractPreviousHistoricalDateKey()` does not parse it as a single resolved historical date per D-18a. Keep helper copy concise Traditional Chinese and include no internal terms such as `log_food`, `get_daily_summary`, `summaryOutcome`, `JSON`, `tool`, `contractResult`, `daily_summary`, or provider/debug terms.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/tools.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `ToolExecutionResult` has a typed clarification field with variants for unresolved `find_meals`, historical `log_food`, `get_daily_summary needs_clarification`, and `get_daily_summary multiple_targets`.
    - Source assertion: candidate projection type in `tools.ts` does not expose full `MealCorrectionCandidate` or raw service objects as the renderer/proof contract.
    - Source assertion: historical clarification copy helpers live in `server/orchestrator/mutation-receipts.ts`, not in `server/orchestrator/index.ts`.
    - Behavior assertion: `multiple_targets` copy lists multiple date choices but `extractPreviousHistoricalDateKey()` returns `undefined` for that copy.
    - Security assertion: renderer helper tests or assertions reject internal protocol/tool names and success-style mutation wording.
  </acceptance_criteria>
  <done>Typed clarification facts and renderer helper APIs exist, and tool unit tests for fact shape and copy safety pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map historical clarification paths to terminal controlled replies</name>
  <files>server/orchestrator/tools.ts, tests/unit/tools.test.ts, tests/unit/orchestrator.test.ts</files>
  <read_first>
    @server/orchestrator/tools.ts
    @server/orchestrator/index.ts
    @server/orchestrator/mutation-receipts.ts
    @tests/unit/tools.test.ts
    @tests/unit/orchestrator.test.ts
    @server/lib/historical-date.ts
    @.planning/phases/68-structured-tool-results-and-release-proof-gate/68-CONTEXT.md
    @.planning/phases/68-structured-tool-results-and-release-proof-gate/68-PATTERNS.md
  </read_first>
  <behavior>
    - `executeTool(log_food)` historical `needs_clarification` returns `success:false`, `executed:false`, `failureReason:"guard"`, `clarification`, and renderer `controlledReply`.
    - `executeTool(get_daily_summary)` `needs_clarification` and `multiple_targets` return terminal renderer `controlledReply` plus typed facts.
    - `executeTool(find_meals)` keeps Phase 67 controlled reply behavior and also populates the new typed clarification fact.
    - `server/orchestrator/index.ts` remains a consumer of `controlledReply`; it does not import historical clarification helpers or inspect raw contract results.
  </behavior>
  <action>Update `executeTool()` in `server/orchestrator/tools.ts` so historical `log_food` and `get_daily_summary` clarification statuses are terminal controlled replies per D-08, D-10, D-11, D-12, D-13, D-17, and D-18. Preserve the existing successful log and summary result behavior, summary history facts, mutation receipts, and Phase 67 non-resolved `find_meals` behavior. Stop relying on `buildHistoricalToolMessage()` for orchestrator-facing behavior; remove it if no longer needed, or leave it only as contract-internal compatibility that no orchestrator test can depend on. Do not add any historical clarification rendering, `contractResult` branching, or serialized-result parsing to `server/orchestrator/index.ts` per D-01 and D-07. Make the Plan 68-01 tests green and keep existing Phase 65-67 tool/orchestrator tests passing.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/tools.test.ts tests/unit/orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: historical `log_food` terminal clarification returns no `loggedMeal`, no `summaryOutcome`, no `dailySummary`, `success:false`, `executed:false`, and renderer `controlledReply`.
    - Behavior assertion: `get_daily_summary multiple_targets` returns typed `dateKeys`, renderer copy asking for one date, no aggregation, no `dailySummary`, and no `summaryHistoryFacts`.
    - Behavior assertion: orchestrator terminal clarification tests observe one LLM call, `finalReplySource === "renderer"`, `didLogMeal:false`, `didMutateMeal:false`, and no success-style copy.
    - Source assertion: `server/orchestrator/index.ts` contains no raw `contractResult` usage and no serialized clarification-result parsing path.
    - Verification assertion: targeted unit command exits 0 after implementation.
  </acceptance_criteria>
  <done>Structured clarification facts flow through `executeTool()`, terminal historical clarifications stop before a second model pass, and unit proof is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool call -> `runContract()` | Untrusted model arguments become validated contract input. |
| `runContract()` -> `executeTool()` | Raw contract-specific result must be adapted before orchestration. |
| `executeTool()` -> renderer helpers | Backend facts become user-visible clarification copy. |
| `executeTool()` -> `server/orchestrator/index.ts` | Typed adapter fields and `controlledReply` cross into the LLM loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-05 | Tampering | `executeTool()` historical status mapping | mitigate | Map `needs_clarification` and `multiple_targets` to typed facts and terminal `controlledReply` before the LLM can rewrite them. |
| T-68-06 | Information Disclosure | `ToolExecutionResult.clarification` candidate projection | mitigate | Use allowlisted renderer-ready projection only; exclude raw contract payload, raw user text, meal ids/revisions by default, provider data, and full service candidates. |
| T-68-07 | Repudiation | terminal historical no-side-effect proof | mitigate | Unit tests assert no mutation fields, no summary fields, and no second LLM pass for terminal clarification results. |
| T-68-08 | Elevation of Privilege | raw `contractResult` in `index.ts` | mitigate | Source guard keeps `server/orchestrator/index.ts` out of contract-specific raw result handling. |
| T-68-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned; use existing TypeScript, Zod, Fastify, SQLite, and node:test stack. |
</threat_model>

<verification>
Use `nutrition-gen-test` for unit test updates and `nutrition-verify-change` for final command selection. Run:
<automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/tools.test.ts tests/unit/orchestrator.test.ts</automated>
<automated>yarn tsc --noEmit</automated>
</verification>

<success_criteria>
Plan 68-02 is complete when `executeTool()` exposes structured renderer-ready clarification facts for all Phase 68 paths, historical clarifications are terminal controlled replies, `index.ts` remains adapter-clean, and targeted unit plus TypeScript gates pass.
</success_criteria>

<output>
Create `.planning/phases/68-structured-tool-results-and-release-proof-gate/68-02-SUMMARY.md` when done.
</output>
