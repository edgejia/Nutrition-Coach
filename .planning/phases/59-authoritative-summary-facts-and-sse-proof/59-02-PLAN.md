---
phase: 59-authoritative-summary-facts-and-sse-proof
plan: 02
type: execute
wave: 2
depends_on:
  - 59-01
files_modified:
  - server/orchestrator/index.ts
  - tests/unit/orchestrator.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - "Orchestrator plain summary/history replies use the backend composer instead of model-authored fact text."
    - "The existing no-mutation guard remains available only as defense-in-depth."
    - "The final reply source identifies renderer-owned summary/history output."
  artifacts:
    - path: "server/orchestrator/index.ts"
      provides: "Orchestrator summary/history composition before returning plain replies"
      contains: "composeSummaryHistoryReply"
    - path: "tests/unit/orchestrator.test.ts"
      provides: "Regression coverage for orchestrator summary/history renderer use"
  key_links:
    - from: "server/orchestrator/tools.ts"
      to: "server/orchestrator/index.ts"
      via: "ToolExecutionResult.summaryHistoryFacts flows into composer"
      pattern: "summaryHistoryFacts"
    - from: "server/orchestrator/index.ts"
      to: "server/orchestrator/summary-history-renderer.ts"
      via: "composer import and final reply replacement"
      pattern: "composeSummaryHistoryReply"
---

<objective>
Wire the shared summary/history composer into the orchestrator plain-response path.

Purpose: The orchestrator must stop treating model-authored summary/history reply text as the primary source of facts after `get_daily_summary`; persisted facts own the final reply segment.

Output: Orchestrator code and unit regressions proving renderer-owned replies for summary/history tool turns.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59-authoritative-summary-facts-and-sse-proof/59-CONTEXT.md
@.planning/phases/59-authoritative-summary-facts-and-sse-proof/59-RESEARCH.md
@.planning/phases/59-authoritative-summary-facts-and-sse-proof/59-PATTERNS.md
@.planning/quick/260516-ppf-fix-v2-2-summary-history-fact-grounding-/260516-ppf-SUMMARY.md
@server/orchestrator/index.ts
@server/orchestrator/tools.ts
@server/orchestrator/summary-history-renderer.ts
@tests/unit/orchestrator.test.ts

<interfaces>
From Plan 59-01:
- `composeSummaryHistoryReply(facts, advice, options?)` returns backend deterministic facts plus accepted advice.
- `SummaryHistoryFacts` is exported from `server/orchestrator/summary-history-renderer.ts`.

Existing orchestrator contracts:
- `ToolExecutionResult.summaryHistoryFacts` is populated by `get_daily_summary`.
- `OrchestratorResult` can carry `reply`, `summaryHistoryFacts`, `dailySummary`, `affectedDate`, `finalReplySource`, and `finalReplyShape`.
- `guardNoMutationLoggingClaim(reply, didLogMeal, didMutateMeal, { summaryHistoryFacts })` remains exported for route defense-in-depth.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add orchestrator unit coverage for renderer-owned summary replies</name>
  <files>tests/unit/orchestrator.test.ts</files>
  <read_first>
    - tests/unit/orchestrator.test.ts
    - server/orchestrator/index.ts
    - server/orchestrator/tools.ts
    - server/orchestrator/summary-history-renderer.ts
    - .planning/quick/260516-ppf-fix-v2-2-summary-history-fact-grounding-/260516-ppf-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: after `get_daily_summary`, model content `今天已記錄牛肉飯，900 kcal。` returns deterministic `豆腐飯 520 kcal、鮭魚飯 380 kcal` output and does not contain `牛肉飯`.
    - Test 2: after `get_daily_summary`, generic safe advice is appended after the deterministic fact segment.
    - Test 3: after `get_daily_summary` for an empty day, output contains `0 餐` and `0 kcal` and not the no-mutation fallback copy.
    - Test 4: summary/history renderer-owned replies set `finalReplySource` to `renderer` and `finalReplyShape` to `plain_text`.
  </behavior>
  <action>Add failing tests in `tests/unit/orchestrator.test.ts` using existing `MockLLMProvider` or local streaming provider patterns. Seed persisted meals through existing service factories where the file already does so, queue a `get_daily_summary` tool call, then queue unsafe or safe model content. Per D-01 through D-08, assert the final `result.reply` is composed from persisted facts, unsafe model facts are absent, safe advice can append, and empty-day semantics do not use mutation-failure copy.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: new tests mention `get_daily_summary` and the exact persisted facts `豆腐飯`, `520`, `鮭魚飯`, and `380`.
    - Behavior assertion: at least one new assertion checks `result.finalReplySource === "renderer"`.
    - Behavior assertion: at least one new assertion rejects `牛肉飯` from `result.reply`.
    - Test-command assertion: the targeted command is RED before Task 2 implementation and GREEN after Task 2.
  </acceptance_criteria>
  <done>Orchestrator expected behavior is locked by failing summary/history renderer tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose orchestrator summary/history plain replies from persisted facts</name>
  <files>server/orchestrator/index.ts, tests/unit/orchestrator.test.ts</files>
  <read_first>
    - server/orchestrator/index.ts
    - server/orchestrator/summary-history-renderer.ts
    - server/orchestrator/tools.ts
    - tests/unit/orchestrator.test.ts
    - .planning/phases/59-authoritative-summary-facts-and-sse-proof/59-PATTERNS.md
  </read_first>
  <action>Update `server/orchestrator/index.ts` to import `SummaryHistoryFacts` and `composeSummaryHistoryReply` from `./summary-history-renderer.js`. Keep `guardNoMutationLoggingClaim` exported as defense-in-depth, but for plain `response.content` after a `get_daily_summary` turn, produce `finalReply = composeSummaryHistoryReply(summaryHistoryFacts, response.content, { affectedDate: resolvedAffectedDate })` before returning the `OrchestratorResult`. Set `finalReplySource` to `renderer` and `finalReplyShape` to `plain_text` when the composer is used. Preserve existing mutation receipt behavior and fallback behavior for log/update/delete/goals flows. Per D-15, do not expand natural-language parsing in `guardNoMutationLoggingClaim` as the primary fix.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/orchestrator.test.ts tests/unit/summary-history-renderer.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `server/orchestrator/index.ts` imports `composeSummaryHistoryReply` from `./summary-history-renderer.js`.
    - Source assertion: `server/orchestrator/index.ts` still exports `guardNoMutationLoggingClaim`.
    - Behavior assertion: the targeted orchestrator tests pass and include renderer-owned final reply source assertions.
    - CLI assertion: `yarn tsc --noEmit` exits 0.
    - Scope assertion: no route, service, database schema, migration, staging, or main-branch promotion files are modified by this plan.
  </acceptance_criteria>
  <done>Plain orchestrator summary/history replies are renderer-owned while legacy guard behavior remains available as defense-in-depth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool results -> orchestrator final reply | Persisted facts cross from service-backed tool execution into final reply composition. |
| LLM content -> orchestrator result | Model-authored text may contain invented or tampered meal facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-05 | Tampering | `server/orchestrator/index.ts` plain response handling | mitigate | Replace model-authored summary/history facts with `composeSummaryHistoryReply(summaryHistoryFacts, response.content, ...)`. |
| T-59-06 | Information Disclosure | `buildSummaryHistoryFacts` and `summaryHistoryFacts` | mitigate | Preserve same-device `deviceId` service calls already used by `foodLoggingService.getMealsByDate`; do not add raw history or cross-device lookup paths. |
| T-59-07 | Repudiation | Final reply trace metadata | mitigate | Mark renderer-owned summary/history final replies with `finalReplySource: "renderer"` and `finalReplyShape: "plain_text"`. |
</threat_model>

<verification>
<automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/orchestrator.test.ts tests/unit/summary-history-renderer.test.ts</automated>
<automated>yarn tsc --noEmit</automated>
</verification>

<success_criteria>
- Direct orchestrator summary/history replies are composed from persisted facts.
- Unsafe model-authored meal facts are absent from orchestrator replies.
- Empty-day summary/history replies do not regress to mutation-failure copy.
- Existing no-mutation guard remains exported and covered as fallback defense.
</success_criteria>

<output>
After completion, create `.planning/phases/59-authoritative-summary-facts-and-sse-proof/59-02-SUMMARY.md`.
</output>
