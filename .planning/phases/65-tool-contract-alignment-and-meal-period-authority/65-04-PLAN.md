---
phase: 65-tool-contract-alignment-and-meal-period-authority
plan: 04
type: execute
wave: 3
depends_on: [65-02]
files_modified:
  - server/services/meal-history.ts
  - server/services/history-query.ts
  - server/routes/meals.ts
  - server/routes/day-snapshot.ts
  - tests/integration/meals-api.test.ts
  - tests/integration/day-snapshot-api.test.ts
  - tests/integration/history-api.test.ts
autonomous: true
requirements: [INTENT-02]
must_haves:
  truths:
    - "D-16/D-17/D-18: current-day, day snapshot, and historical meal row DTOs expose explicit mealPeriod only when persisted."
    - "D-20/D-21: direct PATCH responses preserve and project mealPeriod when ordinary edits omit period changes."
  artifacts:
    - path: "server/services/meal-history.ts"
      provides: "Current-day/day-snapshot meal row mealPeriod projection"
    - path: "server/services/history-query.ts"
      provides: "HistoryMealDto explicit mealPeriod projection"
    - path: "server/routes/meals.ts"
      provides: "GET/PATCH /api/meals mealPeriod response projection"
  key_links:
    - from: "server/services/meal-history.ts"
      to: "server/routes/meals.ts"
      via: "getMealsByDate route mapping"
      pattern: "mealPeriod"
    - from: "server/services/history-query.ts"
      to: "server/routes/history.ts"
      via: "service DTO returned by route"
      pattern: "mealPeriod"
---

<objective>
Project explicit mealPeriod through backend meal row APIs without synthesizing inferred public values.

Purpose: INTENT-02 requires current-day and historical meal rows to expose persisted structured authority while legacy/no-authority rows remain distinguishable.
Output: Backend service/route DTO propagation and integration tests for `/api/meals`, `/api/day-snapshot`, and history APIs.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-PATTERNS.md
@AGENTS.md
@.codex/skills/nutrition-gen-test/SKILL.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Project mealPeriod in current-day and day snapshot rows</name>
  <files>server/services/meal-history.ts, server/routes/meals.ts, server/routes/day-snapshot.ts, tests/integration/meals-api.test.ts, tests/integration/day-snapshot-api.test.ts</files>
  <read_first>
    - server/services/meal-history.ts
    - server/routes/meals.ts
    - server/routes/day-snapshot.ts
    - server/lib/guest-session-resolver.ts
    - tests/integration/meals-api.test.ts
    - tests/integration/day-snapshot-api.test.ts
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
  </read_first>
  <behavior>
    - Test first: `GET /api/meals` returns `mealPeriod: "lunch"` for a persisted explicit lunch meal logged at breakfast hour.
    - Test first: `GET /api/meals` omits or returns null/undefined mealPeriod for legacy rows with no explicit authority; it does not infer breakfast from loggedAt.
    - Test first: direct `PATCH /api/meals/:id` that changes food/macros with no period input preserves and returns existing explicit `mealPeriod`.
    - Test first: `/api/day-snapshot?date=...` projects explicit mealPeriod and omits inferred public values for legacy rows.
  </behavior>
  <action>Select `mealTransactions.mealPeriod` in `server/services/meal-history.ts` and include explicit-only `mealPeriod` in returned meal entries. Update `server/routes/meals.ts` GET and PATCH response mapping to include `mealPeriod` only from service data; preserve cookie-backed `resolveGuestSession` ownership and existing `MealRevisionPreconditionError` behavior. Update `server/routes/day-snapshot.ts` only if route-level mapping drops the field; otherwise let the service DTO flow through. Do not add `inferredMealPeriod`, do not infer from `loggedAt` at the API boundary, and do not alter `summaryOutcome` or daily summary publish behavior.</action>
  <acceptance_criteria>
    - Behavior assertion: current-day and day snapshot route tests distinguish explicit `"lunch"` from legacy absent/null mealPeriod.
    - Behavior assertion: ordinary direct PATCH preserves existing explicit mealPeriod and revision precondition behavior remains unchanged.
    - Source assertion: route code still derives ownership from guest-session cookies, not raw `deviceId` query/header input.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/meals-api.test.ts tests/integration/day-snapshot-api.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>Current-day and day-snapshot APIs expose persisted explicit mealPeriod and do not publish inferred values as authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project mealPeriod in historical meal DTOs</name>
  <files>server/services/history-query.ts, tests/integration/history-api.test.ts</files>
  <read_first>
    - server/services/history-query.ts
    - server/routes/history.ts
    - tests/integration/history-api.test.ts
    - server/lib/meal-period.ts
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
  </read_first>
  <behavior>
    - Test first: `/api/history/meals` returns explicit `mealPeriod` for persisted authority.
    - Test first: `/api/history/days/:date` day snapshot history rows return explicit `mealPeriod`.
    - Test first: historical legacy rows do not expose inferred `mealPeriod` from loggedAt.
  </behavior>
  <action>Add optional explicit `mealPeriod` to `HistoryMealDto` and internal history meal header projections in `server/services/history-query.ts`. Select `mealTransactions.mealPeriod` in `getMeals`, `searchMeals`, and `getDaySnapshot` paths that project meal rows. Use `normalizeMealPeriod` for defensive DB value normalization. Leave `server/routes/history.ts` behavior unchanged unless TypeScript requires DTO typing adjustments, because it already returns service DTOs. Do not change history trends, cursors, date placement, or Phase 67 ranking policy.</action>
  <acceptance_criteria>
    - Behavior assertion: integration tests prove history list and day detail rows include `"lunch"` only when persisted explicit authority exists.
    - Source assertion: `HistoryMealDto` has optional `mealPeriod` with the four-value public enum and no `inferredMealPeriod`.
    - Behavior assertion: cursor/search/history behavior remains otherwise unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/history-api.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>Historical backend DTOs project explicit mealPeriod without inferring public authority from loggedAt.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite -> protected meal APIs | Stored meal facts become current-day and historical DTOs. |
| browser route auth -> meal rows | Cross-device read/write scope must remain cookie-backed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-11 | Information Disclosure | `server/routes/meals.ts` | mitigate | Preserve `resolveGuestSession` and existing device ownership checks before projecting mealPeriod. |
| T-65-12 | Information Disclosure | `server/services/history-query.ts` | mitigate | Select/projection remains scoped by deviceId; only enum mealPeriod leaves storage. |
| T-65-13 | Tampering | API DTOs | mitigate | Do not infer or backfill public mealPeriod for legacy rows; only persisted normalized enum is projected per D-18. |
| T-65-SC | Tampering | package installs | accept | No package install task exists. |
</threat_model>

<verification>
Run targeted integration tests after each task, then `yarn tsc --noEmit` because server TypeScript files changed.
</verification>

<success_criteria>
INTENT-02 backend projection is complete when current-day, day snapshot, and history APIs expose explicit mealPeriod from storage and preserve null/no-authority rows without inferred public values.
</success_criteria>

<output>
Create `.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-04-SUMMARY.md` when done.
</output>
