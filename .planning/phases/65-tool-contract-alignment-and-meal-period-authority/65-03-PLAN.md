---
phase: 65-tool-contract-alignment-and-meal-period-authority
plan: 03
type: execute
wave: 3
depends_on: [65-02]
files_modified:
  - server/orchestrator/tools.ts
  - server/orchestrator/system-prompt.ts
  - tests/unit/tools.test.ts
  - tests/unit/tool-contract.test.ts
  - tests/unit/system-prompt.test.ts
  - tests/unit/protein-trust.test.ts
  - tests/integration/orchestrator.test.ts
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03, INTENT-01]
must_haves:
  truths:
    - "D-01/D-03: log_food JSON schema and prompt treat protein_sources as optional parse-time evidence."
    - "D-02/D-04/D-05/D-06/D-07: backend-normalized counted protein sources are the only trusted-protein authority for receipt copy."
    - "D-10/D-11/D-12/D-13/D-14/D-15: log_food persists source-text mealPeriod authority without trusting raw model meal_period over user text."
  artifacts:
    - path: "server/orchestrator/tools.ts"
      provides: "Aligned log_food JSON schema, mealPeriod extraction, and loggedMeal receipt projection"
      contains: ["protein_sources", "mealPeriod"]
    - path: "server/orchestrator/system-prompt.ts"
      provides: "Conditional protein_sources prompt contract"
    - path: "tests/unit/tools.test.ts"
      provides: "Tool schema, trusted protein, and mealPeriod regression proof"
  key_links:
    - from: "server/orchestrator/tools.ts"
      to: "server/services/food-logging.ts"
      via: "logGroupedMeal input"
      pattern: "mealPeriod"
    - from: "server/orchestrator/tools.ts"
      to: "server/lib/meal-period.ts"
      via: "source-text explicit authority extraction"
      pattern: "extractExplicitMealPeriodFromSourceText"
---

<objective>
Align the LLM-facing `log_food` contract with runtime validation and persist explicit meal-period intent from source text.

Purpose: TOOL-01/TOOL-02/TOOL-03 and INTENT-01 require the model contract to stop overstating raw arguments as authority while preserving committed meal receipts and summaryOutcome behavior.
Output: Updated tool schema/prompt, source-text mealPeriod propagation, loggedMeal receipt field, and targeted unit/integration proof.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-RESEARCH.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-PATTERNS.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-VALIDATION.md
@AGENTS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Align protein_sources tool schema and prompt</name>
  <files>server/orchestrator/tools.ts, server/orchestrator/system-prompt.ts, tests/unit/tool-contract.test.ts, tests/unit/system-prompt.test.ts, tests/unit/protein-trust.test.ts, tests/unit/tools.test.ts</files>
  <read_first>
    - server/orchestrator/tools.ts
    - server/orchestrator/system-prompt.ts
    - server/orchestrator/protein-trust.ts
    - tests/unit/tool-contract.test.ts
    - tests/unit/system-prompt.test.ts
    - tests/unit/protein-trust.test.ts
    - tests/unit/tools.test.ts
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
  </read_first>
  <behavior>
    - Test first: `getToolDefinitions().log_food.parameters.required` does not require `protein_sources`.
    - Test first: Zod execution accepts omitted `protein_sources` for a valid meal.
    - Test first: prompt text no longer says `protein_sources` is always required and instead says to provide it only when credible anchors exist.
    - Test first: existing trusted-protein counted/excluded/weak-anchor behavior remains green.
  </behavior>
  <action>Update `logFoodContract.parameters` in `server/orchestrator/tools.ts` so top-level `protein_sources` remains documented but is not in `required`, matching optional Zod behavior per D-01/D-03. Change its description from required evidence to conditional credible-anchor evidence. Update `server/orchestrator/system-prompt.ts` protein-estimation/log-food receipt wording so the model may omit `protein_sources` when credible anchors are missing and successful reply copy mentions protein sources only when backend-counted sources exist per D-02/D-04/D-07. Preserve conservative commit-and-strip/normalize behavior for weak, vague, or unsupported positive trusted-protein evidence per D-05/D-06.</action>
  <acceptance_criteria>
    - Source assertion: `server/orchestrator/tools.ts` no longer has `required: ["protein_sources"]` for `log_food`.
    - Source assertion: `system-prompt.ts` does not include wording equivalent to `必須提供 protein_sources 陣列`.
    - Behavior assertion: unit tests prove omitted `protein_sources` succeeds and existing trusted-protein regression tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/tool-contract.test.ts tests/unit/system-prompt.test.ts tests/unit/protein-trust.test.ts tests/unit/tools.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>LLM JSON schema, prompt, and Zod runtime agree that protein_sources is optional parse-time evidence guarded by backend normalization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist source-text explicit mealPeriod from log_food</name>
  <files>server/orchestrator/tools.ts, tests/unit/tools.test.ts, tests/integration/orchestrator.test.ts</files>
  <read_first>
    - server/orchestrator/tools.ts
    - server/lib/meal-period.ts
    - server/services/food-logging.ts
    - tests/unit/tools.test.ts
    - tests/integration/orchestrator.test.ts
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
  </read_first>
  <behavior>
    - Test first: current user text `午餐我吃了雞腿便當` with a breakfast-hour loggedAt stores and returns `mealPeriod: "lunch"`.
    - Test first: conflicting raw `args.meal_period: "breakfast"` does not override source text `午餐`.
    - Test first: source text containing only `中午` produces no persisted `mealPeriod`.
    - Test first: historical midpoint behavior for `loggedAt` remains intact.
  </behavior>
  <action>Import `extractExplicitMealPeriodFromSourceText` from `../lib/meal-period.js` into `server/orchestrator/tools.ts`. During `log_food` execution, compute `mealPeriod` from `context.currentUserMessage` and pass it to `deps.foodLoggingService.logGroupedMeal` beside `loggedAt`. Keep raw `args.meal_period` only for existing historical `buildHistoricalLoggedAt` date/time placement behavior per D-10/D-13. Extend `ToolExecutionResult["loggedMeal"]` and `projectMealIdentityFields` to include optional `mealPeriod` from backend persisted/service result, preserving `summaryOutcome`, `dailySummary`, `dateKey`, `mealRevisionId`, image fields, and committed receipt semantics per TOOL-03.</action>
  <acceptance_criteria>
    - Behavior assertion: unit/integration tests show explicit `午餐` persists/projects `"lunch"` even if clock hour or raw `meal_period` says breakfast.
    - Behavior assertion: `早上`/`中午`/`晚上` source text alone does not populate `mealPeriod`.
    - Source assertion: raw `args.meal_period` is not assigned directly to persisted `mealPeriod`.
    - Behavior assertion: successful log_food still returns committed `loggedMeal` and `summaryOutcome`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/tools.test.ts tests/integration/orchestrator.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>log_food persists source-text explicit mealPeriod authority and keeps raw model meal_period/protein_sources as parse-time evidence only.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model tool args -> backend tool executor | Raw model `protein_sources` and `meal_period` cross from untrusted model output into validated backend code. |
| backend receipt -> client/chat | Logged meal facts become user-visible and editable receipt state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-07 | Tampering | `log_food` raw `protein_sources` | mitigate | Treat as optional evidence only; backend `normalizeTrustedProteinEstimate` counted sources drive trusted copy per D-01/D-04/D-07. |
| T-65-08 | Tampering | `log_food` raw `meal_period` | mitigate | Persist mealPeriod only from backend source-text extraction; raw args remain midpoint/date evidence per D-13. |
| T-65-09 | Repudiation | receipt projection | mitigate | Return committed meal id/revision/date/summaryOutcome facts, not LLM-authored mutation facts, per TOOL-03. |
| T-65-10 | Information Disclosure | tests/tool summaries | mitigate | Tests assert structured facts only; no raw prompts, user text transcripts, image data, provider body, or raw tool payloads per D-27. |
| T-65-SC | Tampering | package installs | accept | No package install task exists. |
</threat_model>

<verification>
Run the targeted Node tests listed in each task and `yarn tsc --noEmit` after TypeScript edits.
</verification>

<success_criteria>
TOOL-01 through TOOL-03 are satisfied for orchestrator contracts when optional protein_sources schema/prompt alignment is proven and successful log_food still returns backend-committed receipt facts plus summaryOutcome. INTENT-01 is satisfied at the tool boundary when explicit source text persists mealPeriod authority without trusting raw model `meal_period`.
</success_criteria>

<output>
Create `.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-03-SUMMARY.md` when done.
</output>
