---
phase: 65-tool-contract-alignment-and-meal-period-authority
plan: 07
type: execute
wave: 6
depends_on: [65-06]
files_modified:
  - client/src/components/HomeScreen.tsx
  - client/src/components/HistoryScreen.tsx
  - client/src/components/HistoryDayDetailScreen.tsx
  - client/src/components/SummaryDetailScreen.tsx
  - tests/unit/home-dashboard-contract.test.ts
  - tests/unit/history-screen-contract.test.ts
  - tests/unit/history-day-detail-screen.test.ts
  - tests/unit/summary-detail-screen.test.ts
autonomous: true
requirements: [INTENT-02]
must_haves:
  truths:
    - "D-17/D-19: visible meal labels prefer explicit mealPeriod and fall back to loggedAt only when missing."
    - "D-21: UI helper tests prove explicit preference, legacy fallback, and no new public inferred field."
  artifacts:
    - path: "client/src/components/HomeScreen.tsx"
      provides: "Shared display label and badge helper accepting mealPeriod first"
      exports: ["getDisplayMealLabel", "getMealBadge", "formatMealRowTime"]
    - path: "client/src/components/HistoryScreen.tsx"
      provides: "History row metadata and aria label using resolved meal period"
    - path: "client/src/components/HistoryDayDetailScreen.tsx"
      provides: "Day detail metadata using resolved meal period"
    - path: "client/src/components/SummaryDetailScreen.tsx"
      provides: "Summary detail metadata using resolved meal period"
  key_links:
    - from: "client/src/components/HomeScreen.tsx"
      to: "client/src/types.ts"
      via: "MealPeriod helper parameter"
      pattern: "MealPeriod"
    - from: "client/src/components/HistoryScreen.tsx"
      to: "HomeScreen helpers"
      via: "resolved label reuse"
      pattern: "getDisplayMealLabel"
---

<objective>
Update touched UI surfaces to display explicit mealPeriod before loggedAt fallback.

Purpose: INTENT-02 includes user-visible projection: a breakfast-hour meal with explicit lunch authority must read as lunch on affected meal-row surfaces.
Output: Shared label/badge helper update, history/detail metadata projection, accessibility label alignment, and source-contract unit proof.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-PATTERNS.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-UI-SPEC.md
@AGENTS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Update shared meal label and badge helper</name>
  <files>client/src/components/HomeScreen.tsx, tests/unit/home-dashboard-contract.test.ts</files>
  <read_first>
    - client/src/components/HomeScreen.tsx
    - client/src/types.ts
    - tests/unit/home-dashboard-contract.test.ts
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-UI-SPEC.md
  </read_first>
  <behavior>
    - Test first: `getDisplayMealLabel("lunch", "2026-04-29T07:30:00+08:00")` returns `午餐`.
    - Test first: `getMealBadge("late_night", "...")` returns `N`.
    - Test first: missing/invalid mealPeriod preserves existing loggedAt fallback labels, including `點心` and badge `S`.
  </behavior>
  <action>Change `getDisplayMealLabel` and `getMealBadge` in `client/src/components/HomeScreen.tsx` to accept `mealPeriod?: MealPeriod | null` as the first argument and `loggedAt?: string | null` as the second argument. Map explicit `breakfast/lunch/dinner/late_night` to `早餐/午餐/晚餐/宵夜` and `B/L/D/N`. If mealPeriod is missing or invalid, preserve existing loggedAt fallback windows: 05:00-10:59 `早餐`, 11:00-13:59 `午餐`, 14:00-16:59 `點心`, 17:00-22:59 `晚餐`, otherwise `餐點`. Update HomeScreen row calls to pass `meal.mealPeriod` first. Do not change layout classes, spacing, colors, cards, CTA copy, or add new controls.</action>
  <acceptance_criteria>
    - Behavior assertion: helper unit tests prove explicit mealPeriod wins over conflicting loggedAt hour.
    - Behavior assertion: legacy loggedAt fallback tests remain green.
    - Source assertion: HomeScreen row still uses existing metadata layout and classes.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/home-dashboard-contract.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>Shared HomeScreen helper and home rows prefer explicit mealPeriod while preserving legacy fallback behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply resolved labels to history and detail meal rows</name>
  <files>client/src/components/HistoryScreen.tsx, client/src/components/HistoryDayDetailScreen.tsx, client/src/components/SummaryDetailScreen.tsx, tests/unit/history-screen-contract.test.ts, tests/unit/history-day-detail-screen.test.ts, tests/unit/summary-detail-screen.test.ts</files>
  <read_first>
    - client/src/components/HistoryScreen.tsx
    - client/src/components/HistoryDayDetailScreen.tsx
    - client/src/components/SummaryDetailScreen.tsx
    - client/src/components/HomeScreen.tsx
    - tests/unit/history-screen-contract.test.ts
    - tests/unit/history-day-detail-screen.test.ts
    - tests/unit/summary-detail-screen.test.ts
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-UI-SPEC.md
  </read_first>
  <behavior>
    - Test first: History row metadata renders `HH:mm · 午餐` for explicit lunch at breakfast hour.
    - Test first: History, Day Detail, and Summary Detail accessible edit labels include the resolved meal label before food name when an edit button exists.
    - Test first: missing mealPeriod surfaces continue to use loggedAt fallback labels.
  </behavior>
  <action>Reuse `formatMealRowTime` and `getDisplayMealLabel` from `HomeScreen.tsx` in `HistoryScreen.tsx`, `HistoryDayDetailScreen.tsx`, and `SummaryDetailScreen.tsx`. Render metadata as `HH:mm · {resolvedLabel}` where the UI spec requires it. Update edit button `aria-label` strings to include the resolved label before the food name, for example `編輯 午餐 雞腿便當`. Keep existing visual hierarchy, typography, CSS classes, empty/error copy, and action controls unchanged.</action>
  <acceptance_criteria>
    - Behavior assertion: source-contract tests prove visible metadata uses explicit mealPeriod first and fallback second on all touched surfaces.
    - Accessibility assertion: edit aria labels include resolved meal label and food name.
    - Source assertion: no new meal-period picker, correction modal, toast, or redesign is introduced.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/history-screen-contract.test.ts tests/unit/history-day-detail-screen.test.ts tests/unit/summary-detail-screen.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>History, Day Detail, and Summary Detail rows present explicit mealPeriod labels consistently with legacy fallback.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client DTO -> rendered UI | Structured mealPeriod becomes visible copy and accessibility text. |
| fallback display helper -> user interpretation | Legacy loggedAt inference remains display-only when authority is absent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-20 | Tampering | UI helpers | mitigate | Prefer explicit backend enum over loggedAt fallback; fallback is display-only and not serialized as authority. |
| T-65-21 | Information Disclosure | UI tests | mitigate | Source-contract tests assert rendered labels only; no raw transcripts, tool args, image data, or session material. |
| T-65-22 | Repudiation | accessibility labels | mitigate | Visible labels and edit aria-labels use the same resolver so user-facing intent is consistent. |
| T-65-SC | Tampering | package installs | accept | No package install task exists. |
</threat_model>

<verification>
Run targeted UI contract unit tests and `yarn tsc --noEmit` after TypeScript/TSX edits.
</verification>

<success_criteria>
UI projection is complete when every touched meal-row surface prefers explicit mealPeriod, preserves existing loggedAt fallback behavior, and keeps visible and accessible labels aligned.
</success_criteria>

<output>
Create `.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-07-SUMMARY.md` when done.
</output>
