---
phase: 65-tool-contract-alignment-and-meal-period-authority
plan: 08
type: execute
wave: 3
depends_on: [65-02]
files_modified:
  - server/services/meal-correction.ts
  - tests/unit/meal-correction.test.ts
autonomous: true
requirements: [INTENT-03]
must_haves:
  truths:
    - "D-22/D-23/D-24/D-25/D-26: correction candidates expose effective mealPeriod plus source explicit/inferred without Phase 67 ranking changes."
    - "Explicit persisted mealPeriod outranks loggedAt hour for candidate facts, while legacy/no-authority rows retain loggedAt fallback."
  artifacts:
    - path: "server/services/meal-correction.ts"
      provides: "MealCorrectionCandidate.mealPeriodSource and explicit-first effective mealPeriod"
      contains: "mealPeriodSource"
    - path: "tests/unit/meal-correction.test.ts"
      provides: "Candidate authority handoff regression proof"
  key_links:
    - from: "server/services/meal-correction.ts"
      to: "server/db/schema.ts"
      via: "mealTransactions.mealPeriod select"
      pattern: "mealPeriodSource"
---

<objective>
Expose persisted meal-period authority to correction candidates without changing Phase 67 ranking policy.

Purpose: INTENT-03 is a fact-authority handoff: candidate scoring must be able to see explicit period authority separately from loggedAt fallback.
Output: `MealCorrectionCandidate.mealPeriodSource`, explicit-first candidate projection, and focused unit tests.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-PATTERNS.md
@AGENTS.md
@.codex/skills/nutrition-gen-test/SKILL.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add mealPeriodSource to correction candidates</name>
  <files>server/services/meal-correction.ts, tests/unit/meal-correction.test.ts</files>
  <read_first>
    - server/services/meal-correction.ts
    - server/lib/meal-period.ts
    - server/db/schema.ts
    - tests/unit/meal-correction.test.ts
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
  </read_first>
  <behavior>
    - Test first: explicit persisted lunch with breakfast-hour loggedAt projects candidate `mealPeriod: "lunch"` and `mealPeriodSource: "explicit"`.
    - Test first: legacy/no-authority row at breakfast hour projects candidate `mealPeriod: "breakfast"` and `mealPeriodSource: "inferred"`.
    - Test first: existing findMeals resolution/ranking tests remain semantically unchanged except for the added source field.
  </behavior>
  <action>Update `MealCorrectionCandidate` in `server/services/meal-correction.ts` with `mealPeriodSource: "explicit" | "inferred"`. Select `mealTransactions.mealPeriod` when loading active candidates. Use `normalizeMealPeriod(header.mealPeriod)` to compute `explicitMealPeriod`; set `mealPeriod` to explicit value when present, otherwise existing `inferMealPeriod(header.loggedAt)`, and set source to `explicit` or `inferred` accordingly. Do not alter ranking weights, tie-breaking, hard/soft matching, food-label precedence, pending selection state, clarification copy, or resolved target behavior; those remain Phase 67 per D-22/D-26.</action>
  <acceptance_criteria>
    - Source assertion: `MealCorrectionCandidate` includes `mealPeriodSource` and only domain values `explicit` / `inferred`.
    - Behavior assertion: unit tests prove explicit lunch beats breakfast-hour loggedAt in candidate facts.
    - Behavior assertion: legacy/no-authority candidates still infer from loggedAt and existing resolution tests remain green.
    - Scope assertion: no ranking weight, tie-breaker, clarification rendering, or food-label precedence logic changes are introduced.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/meal-correction.test.ts</automated>
  </verify>
  <done>Correction candidates expose effective mealPeriod with explicit/inferred provenance while leaving Phase 67 targeting policy untouched.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored meal facts -> correction candidate facts | Structured mealPeriod becomes matching evidence for future correction targeting. |
| candidate facts -> later correction scorer | Phase 67 will consume these fields, so provenance must be clear. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-23 | Tampering | `server/services/meal-correction.ts` | mitigate | Persisted explicit mealPeriod wins; loggedAt fallback is tagged `inferred` and cannot masquerade as explicit authority. |
| T-65-24 | Tampering | correction ranking scope | mitigate | Do not change ranking weights, tie-breaking, hard/soft matching, food-label precedence, or clarification behavior in Phase 65. |
| T-65-25 | Information Disclosure | candidate tests | mitigate | Tests assert enum/source metadata only and avoid raw user text/proof payload persistence per D-27. |
| T-65-SC | Tampering | package installs | accept | No package install task exists. |
</threat_model>

<verification>
Run targeted meal-correction unit tests and `yarn tsc --noEmit` after TypeScript edits. This plan can execute after the migration gate and does not depend on client/UI plans.
</verification>

<success_criteria>
INTENT-03 is complete when correction candidates expose explicit persisted period facts with `mealPeriodSource: "explicit"` and legacy fallback facts with `mealPeriodSource: "inferred"` without altering target ranking policy.
</success_criteria>

<output>
Create `.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-08-SUMMARY.md` when done.
</output>
