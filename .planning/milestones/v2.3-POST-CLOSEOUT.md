# v2.3 Post-Closeout Notes

**Date:** 2026-05-20
**Milestone:** v2.3 Authoritative Mutation Outcomes and Fresh Meal State

## Dependency Review

Commands run:

- `yarn outdated --json`
- `yarn audit --json`

### Outdated Packages

Patch/minor updates available:

- `@fastify/static` 9.1.1 -> 9.1.3
- `@tailwindcss/vite` 4.2.2 -> 4.3.0
- `@types/react` 19.2.14 -> 19.2.15
- `fastify` 5.8.4 -> 5.8.5
- `react` / `react-dom` 19.2.4 -> 19.2.6
- `tailwindcss` 4.2.2 -> 4.3.0
- `tsx` 4.21.0 -> 4.22.3
- `vite` 6.4.1 -> 6.4.2
- `zod` 4.3.6 -> 4.4.3
- `zustand` 5.0.12 -> 5.0.13

Major updates available:

- `@fastify/multipart` 9.4.0 -> 10.0.0
- `@types/node` 22.19.15 -> 25.9.1
- `@vitejs/plugin-react` 4.7.0 -> 6.0.2
- `better-sqlite3` 11.10.0 -> 12.10.0
- `drizzle-orm` 0.39.3 -> 0.45.2
- `openai` 4.104.0 -> 6.38.0
- `typescript` 5.9.3 -> 6.0.3

### Audit Findings

`yarn audit --json` exits non-zero with 11 high and 4 moderate advisories.

Tracked upgrade targets:

- `drizzle-orm` >= 0.45.2 for SQL identifier escaping advisory.
- `fastify` >= 5.8.5 for body schema validation bypass.
- Transitive `fast-uri` >= 3.1.2 through Fastify/AJV dependency tree.
- `vite` >= 6.4.2 for dev-server file-read/path traversal advisories.
- `postcss` >= 8.5.10 through Vite.
- `@fastify/static` dependency tree for transitive `brace-expansion` >= 5.0.6.
- Drizzle tooling path for transitive `esbuild` >= 0.25.0.

### Disposition

Deferred to the next milestone backlog. These advisories do not retroactively block the v2.3 archive because v2.3 did not change dependency versions, the local release gate passed, and deployment promotion remains a separate explicitly approved ship workflow.

## Dead-Code Review

No dedicated dead-code tool is configured in this repo. The next milestone should choose a focused cleanup target instead of broad speculative deletion.

## Coverage And Bundle Trends

Captured during `yarn release:check`:

- Node test suite: 1117 passing, 0 failing.
- Frontend production build output:
  - CSS: `76.82 kB` raw, `13.35 kB` gzip.
  - JS: `336.39 kB` raw, `100.75 kB` gzip.

These are useful as rough trend baselines only; no formal coverage threshold exists.

## Harness Health

No harness scenario was rerun after archive. Phase 64 already recorded metadata-only artifact privacy proof and final local release gates. Obsolete or flaky scenario cleanup should be planned explicitly if it becomes part of v2.4.
