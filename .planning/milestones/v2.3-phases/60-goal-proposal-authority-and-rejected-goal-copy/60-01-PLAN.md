---
phase: 60-goal-proposal-authority-and-rejected-goal-copy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/goal-proposals.ts
  - tests/unit/goal-proposals.test.ts
  - server/orchestrator/mutation-receipts.ts
  - tests/unit/mutation-receipts.test.ts
autonomous: true
requirements:
  - GOAL-01
  - GOAL-03
  - GOAL-04
must_haves:
  truths:
    - "Backend can persist exactly one active pending goal proposal per device without changing daily targets."
    - "Backend can render deterministic Traditional Chinese proposal, generic fail-closed, validation, and cancel copy without LLM authorship."
    - "Expired or cleared pending proposal state is unavailable to later confirmation."
  artifacts:
    - path: "server/services/goal-proposals.ts"
      provides: "Thin pending goal proposal wrapper over existing turn_states"
      exports: ["GOAL_PROPOSAL_KIND", "GOAL_PROPOSAL_TTL_MS", "createGoalProposalService"]
    - path: "server/orchestrator/mutation-receipts.ts"
      provides: "Renderer-owned goal proposal, fail-closed, validation, and cancel copy"
      exports: ["renderGoalProposalCopy", "renderGoalAuthorityFailureCopy", "renderGoalValidationFailureCopy", "renderGoalCancelCopy"]
    - path: "tests/unit/goal-proposals.test.ts"
      provides: "Real SQLite proposal lifecycle proof"
    - path: "tests/unit/mutation-receipts.test.ts"
      provides: "Exact-copy and forbidden-term proof for goal renderers"
  key_links:
    - from: "server/services/goal-proposals.ts"
      to: "server/services/turn-state.ts"
      via: "createTurnStateService(db)"
      pattern: "putState|getState|clearState"
    - from: "server/orchestrator/mutation-receipts.ts"
      to: "tests/unit/mutation-receipts.test.ts"
      via: "exact exported copy constants/renderers"
      pattern: "renderGoal(Proposal|Authority|Validation|Cancel)"
---

<objective>
Create the backend-only foundation for Phase 60: a pending goal proposal service over the existing `turn_states` table and deterministic Traditional Chinese renderer copy for proposal, authority failure, validation failure, and cancel outcomes.

Purpose: This gives later plans a concrete persistence and copy contract before tool execution and orchestration are changed.
Output: `createGoalProposalService`, exact copy renderers, and unit tests proving lifecycle and copy invariants.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-CONTEXT.md
@.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-RESEARCH.md
@.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-PATTERNS.md
@.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-AI-SPEC.md
@.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-VALIDATION.md
@AGENTS.md

<interfaces>
From `server/services/turn-state.ts`:
- `createTurnStateService(db)` returns `putState<T>(deviceId, kind, payload, ttlMs)`, `getState<T>(deviceId, kind)`, and `clearState(deviceId, kind)`.
- `getState()` clears expired rows before returning `undefined`.
- The existing `turn_states` schema already enforces one row per `(device_id, kind)`.

From `server/services/device.ts`:
- `DailyTargets` is `{ calories: number; protein: number; carbs: number; fat: number }`.

Planning choice:
- Use explicit single-active latest-proposal mode per D-17. Hidden `proposal_id` handoff is not proven reliable by current prompt/tool plumbing, and D-05 already locks one active proposal per device.
- Do not modify `server/db/schema.ts` or add migrations. The locked `turn_states` decision is sufficient for D-05 through D-10.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pending goal proposal service over turn_states</name>
  <files>server/services/goal-proposals.ts, tests/unit/goal-proposals.test.ts</files>
  <read_first>
    server/services/turn-state.ts
    server/services/device.ts
    tests/unit/meal-correction.test.ts
    .planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-CONTEXT.md
    .planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-PATTERNS.md
    server/services/goal-proposals.ts
    tests/unit/goal-proposals.test.ts
  </read_first>
  <behavior>
    - Test 1: `putLatest(deviceId, targets)` creates a pending proposal with a generated `proposalId`, exact `targets`, and `createdAt`.
    - Test 2: a second `putLatest` for the same device overwrites the earlier proposal because the underlying kind is one-active-per-device.
    - Test 3: `getLatest(deviceId)` returns `undefined` after the row is expired by setting `turn_states.expires_at` to the past.
    - Test 4: `clear(deviceId)` removes the active proposal and `getLatest(deviceId)` returns `undefined`.
  </behavior>
  <action>
    Create `server/services/goal-proposals.ts` with `GOAL_PROPOSAL_KIND = "goal_proposal"`, `GOAL_PROPOSAL_TTL_MS = 30 * 60 * 1000`, `GoalProposalPayload`, and `createGoalProposalService(db)`. The service must use `createTurnStateService(db)` internally and expose `putLatest(deviceId, targets)`, `getLatest(deviceId)`, and `clear(deviceId)`. `putLatest` must generate `proposalId` with `crypto.randomUUID()`, preserve all four `DailyTargets`, and persist through `turn_states` only per D-01, D-05, D-06, D-07, and the schema-push requirement. Do not import or instantiate LLM clients. Do not modify `server/db/schema.ts`, `drizzle/**`, or migration metadata.
  </action>
  <acceptance_criteria>
    - Source assertion: `server/services/goal-proposals.ts` exports `GOAL_PROPOSAL_KIND`, `GOAL_PROPOSAL_TTL_MS`, `GoalProposalPayload`, and `createGoalProposalService`.
    - Source assertion: `server/services/goal-proposals.ts` imports `createTurnStateService` and contains no SQL strings, no Drizzle table imports, and no schema/migration changes.
    - Behavior assertion: `tests/unit/goal-proposals.test.ts` proves create, overwrite, expire/unavailable, and clear behavior with real SQLite `:memory:`.
    - CLI assertion: `git diff --name-only -- server/db/schema.ts drizzle | wc -l` returns `0`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/goal-proposals.test.ts</automated>
  </verify>
  <done>Pending proposals are persisted in `turn_states`, one active proposal per device is enforced by overwrite, 30-minute TTL is encoded, and expired/cleared proposals are unavailable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic goal proposal and rejection renderers</name>
  <files>server/orchestrator/mutation-receipts.ts, tests/unit/mutation-receipts.test.ts</files>
  <read_first>
    server/orchestrator/mutation-receipts.ts
    tests/unit/mutation-receipts.test.ts
    .planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-CONTEXT.md
    .planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-AI-SPEC.md
    server/orchestrator/mutation-receipts.ts
    tests/unit/mutation-receipts.test.ts
  </read_first>
  <behavior>
    - Test 1: `renderGoalProposalCopy({ calories: 1400, protein: 120, carbs: 130, fat: 45 })` returns exact Traditional Chinese proposal copy listing all four targets.
    - Test 2: `renderGoalAuthorityFailureCopy()` returns one exact generic fail-closed copy for missing, expired, consumed, replaced, mismatch, and unauthorized proposal states.
    - Test 3: `renderGoalValidationFailureCopy(["calories"])` returns exact field-specific range copy for calories; analogous protein/carbs/fat copies mention their configured ranges.
    - Test 4: `renderGoalCancelCopy()` returns exact neutral cancel copy and does not imply success.
    - Test 5: every new renderer output passes `assertNoForbiddenReceiptTerms()` and does not contain internal tool names, reason-code names, `proposalId`, `turn_states`, or API terms.
  </behavior>
  <action>
    Extend `server/orchestrator/mutation-receipts.ts` with goal-specific renderers next to the existing backend-owned receipt renderer. Use these exact copy contracts unless the tests define equivalent constants in the same file: proposal copy starts `我可以先幫你改成這組每日目標：` and lists `• 卡路里 {n} kcal`, `• 蛋白質 {n} g`, `• 碳水 {n} g`, `• 脂肪 {n} g`, then `如果要套用，請回覆「好」；如果要調整，請直接給新的數字。`; authority failure copy is `這次沒有套用目標更新。請直接提供新的每日目標數字，或再請我產生一組建議。`; cancel copy is `已取消這組目標提案，沒有套用任何更新。之後可以直接提供新的目標數字，或再請我產生一組建議。`; validation copies must be field-specific for calories 500-8000 kcal, protein 0-400 g, carbs 0-1000 g, and fat 0-300 g. This implements D-03, D-20 through D-24, D-27, D-28, and D-29.
  </action>
  <acceptance_criteria>
    - Source assertion: renderer functions are exported from `server/orchestrator/mutation-receipts.ts` and reuse the existing local number formatter for integer/decimal formatting.
    - Behavior assertion: `tests/unit/mutation-receipts.test.ts` contains exact-copy assertions for proposal, authority failure, calories validation failure, and cancel copy.
    - Behavior assertion: tests assert all new copy has zero forbidden receipt terms and no `proposalId`, `turn_states`, `update_goals`, `propose_goals`, `schema_validation`, `source_text_guard`, `API`, or `/api`.
    - Behavior assertion: proposal copy lists exact proposed calories, protein, carbs, and fat values and contains no success wording such as `已更新每日目標`.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/mutation-receipts.test.ts tests/unit/goal-proposals.test.ts</automated>
  </verify>
  <done>All Phase 60 renderer-owned copy is deterministic, Traditional Chinese, exact-test-covered, and free of internal terms or LLM-authored rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite turn state to backend service | Persisted proposal payloads are read later to authorize mutation. |
| Backend renderer to user-visible reply | Renderer text becomes final user copy and must not expose internals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Tampering | `server/services/goal-proposals.ts` | mitigate | Store pending proposal state only through `turn_states` one-row-per-device-kind overwrite and test overwrite/expiry/clear behavior with real SQLite. |
| T-60-02 | Information Disclosure | `server/orchestrator/mutation-receipts.ts` | mitigate | Exact-copy tests assert no internal tool names, proposal ids, schema/reason codes, route/API terms, raw user text, or provider/session material. |
| T-60-03 | Repudiation | `tests/unit/goal-proposals.test.ts` | mitigate | Unit tests prove proposal lifecycle transitions with source assertions and stable method names so later plans can attribute mutation authority to backend state. |
</threat_model>

<verification>
Run the task commands, then run `yarn tsc --noEmit` because this plan adds TypeScript files and exports.
</verification>

<success_criteria>
- `server/services/goal-proposals.ts` exists and uses existing `turn_states` with no schema or migration changes.
- Goal proposal, authority failure, validation failure, and cancel copy render deterministically from backend code.
- Unit tests prove lifecycle and exact-copy behavior using Node `node:test` and real SQLite.
</success_criteria>

<output>
After completion, create `.planning/phases/60-goal-proposal-authority-and-rejected-goal-copy/60-01-SUMMARY.md`
</output>
