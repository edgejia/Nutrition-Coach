---
phase: 61-committed-mutation-outcome-and-summary-contract
plan: 05
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - server/routes/meals.ts
  - tests/integration/meals-api.test.ts
autonomous: true
requirements: [MUT-04]
user_setup: []
must_haves:
  truths:
    - "Direct PATCH and DELETE return HTTP 200 with committed facts after SQLite commit even when summary refresh degrades."
    - "Direct route response bodies distinguish committed mutation facts from summaryOutcome freshness status."
    - "D-05: Apply the same public summaryOutcome contract to direct PATCH and DELETE meal route responses."
    - "D-07: Derive temporary top-level dailySummary only from fresh or recovered summaryOutcome."
    - "D-19: Keep committed direct PATCH and DELETE responses HTTP 200 regardless of summary outcome status."
    - "D-21: Preserve publish failure as metadata-only observability outside the response body."
  artifacts:
    - path: "server/routes/meals.ts"
      provides: "Direct PATCH/DELETE response projection with summaryOutcome and non-fatal publish"
      contains: "summaryOutcome"
    - path: "tests/integration/meals-api.test.ts"
      provides: "Fastify app.inject proof for direct committed degraded outcomes, auth, and cross-device guards"
  key_links:
    - from: "server/routes/meals.ts"
      to: "server/services/summary-outcome.ts"
      via: "buildSummaryOutcomeAfterMealCommit after direct update/delete commit"
      pattern: "buildSummaryOutcomeAfterMealCommit"
    - from: "server/routes/meals.ts"
      to: "resolveGuestSession"
      via: "signed cookie route authorization"
      pattern: "resolveGuestSession"
---

<objective>
Apply the shared summary outcome contract to direct meal `PATCH` and `DELETE` routes.

Purpose: MUT-04 requires direct browser mutation responses to distinguish committed mutation facts from summary refresh status without turning post-commit degradation into route failure.
Output: `server/routes/meals.ts` returns HTTP 200 committed facts with `summaryOutcome`; integration tests prove auth, cross-device guards, and degraded-summary behavior.
</objective>

<decision_coverage>
- D-05: Apply the same public `summaryOutcome` contract to direct `PATCH` and `DELETE` meal route responses.
- D-07: Derive temporary top-level `dailySummary` only from fresh or recovered `summaryOutcome`.
- D-19: Keep committed direct `PATCH` and `DELETE` responses HTTP `200` regardless of summary outcome status.
- D-21: Preserve publish failure as metadata-only observability outside the response body.
</decision_coverage>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-RESEARCH.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-VALIDATION.md
@.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-PATTERNS.md
@server/services/summary-outcome.ts
@server/routes/meals.ts
@tests/integration/meals-api.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add direct PATCH/DELETE degraded-summary integration tests</name>
  <files>tests/integration/meals-api.test.ts</files>
  <read_first>
    AGENTS.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-VALIDATION.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-PATTERNS.md
    server/routes/meals.ts
    tests/integration/meals-api.test.ts
  </read_first>
  <behavior>
    - `PATCH /api/meals/:id` returns HTTP 200 after committed update with `summaryOutcome.status === "recovered"` when recompute fails and recovery succeeds.
    - `PATCH /api/meals/:id` returns HTTP 200 after committed update with `summaryOutcome.status === "unavailable"` when recompute and recovery fail, and omits top-level `dailySummary`.
    - `DELETE /api/meals/:id` returns HTTP 200 after committed delete with `summaryOutcome` and explicit committed facts including at least `affectedDate` and `deletedMealId`.
    - Publisher throws do not change HTTP status and do not appear as `summaryOutcome.status` or `summaryOutcome.reason`.
  </behavior>
  <action>
    Extend `tests/integration/meals-api.test.ts` using existing `buildApp()`, real SQLite, `app.inject()`, and cookie setup. Include assertions that signed guest-session authorization still gates routes, foreign-device meal mutation remains denied/not found as currently expected, request validation and stale/conflict behavior remain unchanged, committed degraded responses stay HTTP `200`, and top-level `dailySummary` exists only for fresh/recovered. Do not hand-edit harness artifacts.
  </action>
  <acceptance_criteria>
    Integration tests fail before route implementation; tests cover both direct `PATCH` and direct `DELETE`; tests assert no `publish_failed` in response bodies; tests preserve current 401/400/404/409 expectations for invalid, unauthorized, foreign, stale, or conflict paths.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/meals-api.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Direct meal route tests prove committed mutation facts and summaryOutcome are separate for degraded summaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement direct route summaryOutcome projection</name>
  <files>server/routes/meals.ts, tests/integration/meals-api.test.ts</files>
  <read_first>
    AGENTS.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-CONTEXT.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-RESEARCH.md
    .planning/phases/61-committed-mutation-outcome-and-summary-contract/61-PATTERNS.md
    server/services/summary-outcome.ts
    server/routes/meals.ts
    tests/integration/meals-api.test.ts
  </read_first>
  <action>
    Update `server/routes/meals.ts` so direct `PATCH /api/meals/:id` and `DELETE /api/meals/:id` commit through existing services first, then call the shared summary outcome helper for the affected date. Return HTTP `200` for committed mutations regardless of `summaryOutcome.status` per D-19. Include `summaryOutcome` in the response body; include top-level `dailySummary` only via `dailySummaryFromOutcome(summaryOutcome)` for fresh/recovered; for delete include explicit committed facts `affectedDate` and `deletedMealId` without disrupting existing clients. Wrap `publisher.publishDailySummary()` with non-fatal metadata-only logging like `publishSummarySafe`; do not include publish status in the body. Preserve signed guest-session resolution, cookie clearing, validation, cross-device prevention, and current stale/conflict behavior.
  </action>
  <acceptance_criteria>
    Direct PATCH/DELETE degraded committed responses are HTTP 200; response body includes `summaryOutcome`; unavailable responses omit `dailySummary`; publisher exceptions are warn-logged only with metadata and do not alter the response; existing authorization and cross-device tests still pass; no raw user text, tool payloads, provider bodies, image data, sessions, or DB snapshots are logged.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/meals-api.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Direct meal routes expose committed mutation facts and summaryOutcome without coupling response success to summary recompute or publish.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to direct meal routes | Protected mutation requests rely on signed guest-session cookies. |
| route to meal services | Route passes resolved `deviceId` and validated body to service/transaction layer. |
| route to response/logs | Committed facts and degraded summary status become public JSON and metadata-only logs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-17 | Spoofing | `PATCH/DELETE /api/meals/:id` | mitigate | Preserve `resolveGuestSession()` and cookie clearing; do not authorize from raw `deviceId` query params or `x-device-id` headers. |
| T-61-18 | Elevation of Privilege | cross-device meal mutation | mitigate | Keep service calls scoped by resolved `deviceId`; integration tests preserve foreign meal denial/not-found behavior. |
| T-61-19 | Information Disclosure | degraded summary and publish logs | mitigate | Warn logs contain event name, status/reason, affected date/id metadata only; no raw user text, tool payloads, provider bodies, image data, sessions, or DB snapshots. |
| T-61-20 | Integrity | response projection | mitigate | Preserve request validation and stale/conflict behavior; only committed mutations receive HTTP 200 with `summaryOutcome`. |
</threat_model>

<verification>
Targeted: `node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/meals-api.test.ts`
Static: `yarn tsc --noEmit`
Wave gate after this plan if executed with Plan 01: `yarn test:integration`
</verification>

<source_audit>
GOAL: Covered by direct route committed-outcome behavior.
REQ: MUT-04 direct PATCH/DELETE.
RESEARCH: Implements direct route response projection and non-fatal publish failure isolation.
CONTEXT: Implements D-01 through D-08, D-13 through D-19, and D-21; excludes visible UI indicators and `/api/sse daily_summary` freshness.
</source_audit>

<success_criteria>
Direct meal update/delete responses distinguish committed mutation facts from summary freshness, keep HTTP 200 after commit, and preserve current auth/validation/conflict behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/61-committed-mutation-outcome-and-summary-contract/61-05-SUMMARY.md`.
</output>
