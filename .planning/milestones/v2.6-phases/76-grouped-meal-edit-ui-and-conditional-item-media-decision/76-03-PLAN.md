---
phase: 76-grouped-meal-edit-ui-and-conditional-item-media-decision
plan: 03
type: execute
wave: 2
depends_on:
  - 76-01
files_modified:
  - tests/integration/meals-api.test.ts
  - server/services/meal-history.ts
  - server/routes/meals.ts
autonomous: true
requirements:
  - GROUP-UI-01
  - GROUP-UI-03
  - MEDIA-DECISION-01
must_haves:
  truths:
    - "Home-origin grouped meals can open Meal Edit with authoritative flat item details from the existing /api/meals DTO path."
    - "Grouped read DTOs expose only item text/nutrition facts and do not add item media evidence."
    - "Post-commit grouped refresh can rely on getMeals and setMeals through refreshAfterMealMutation."
    - "Unauthorized meal reads remain protected by the existing signed guest-session route boundary."
  artifacts:
    - path: "tests/integration/meals-api.test.ts"
      provides: "GET /api/meals grouped items projection proof"
      contains: "items"
    - path: "server/services/meal-history.ts"
      provides: "MealHistoryEntry optional flat grouped items[] projection"
      contains: "items?:"
    - path: "server/routes/meals.ts"
      provides: "Authorized GET /api/meals DTO includes grouped items when available"
      contains: "...(meal.items"
  key_links:
    - from: "server/services/meal-history.ts"
      to: "meal_revision_items.position"
      via: "itemsByRevisionId ordered projection"
      pattern: "position"
    - from: "server/routes/meals.ts"
      to: "client/src/api.ts normalizeMealEntry"
      via: "GET /api/meals row items[]"
      pattern: "items"
    - from: "client/src/meal-edit-refresh.ts"
      to: "GET /api/meals"
      via: "getMeals({ refreshReason: \"meal_mutation\" })"
      pattern: "refreshReason: \"meal_mutation\""
---

<objective>
Expose grouped item details through the existing authorized meal read path used by Home and post-commit refresh.

Purpose: Close the GROUP-UI-03 DTO gap identified in research without adding a separate edit-detail endpoint, and keep MEDIA-DECISION-01 tied to whole-meal media rather than per-item evidence.
Output: Integration proof plus a small `/api/meals` DTO projection update for flat media-free grouped `items[]`.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/76-grouped-meal-edit-ui-and-conditional-item-media-decision/76-CONTEXT.md
@.planning/phases/76-grouped-meal-edit-ui-and-conditional-item-media-decision/76-RESEARCH.md
@.planning/phases/76-grouped-meal-edit-ui-and-conditional-item-media-decision/76-VALIDATION.md
@.planning/phases/76-grouped-meal-edit-ui-and-conditional-item-media-decision/76-PATTERNS.md
@.planning/phases/76-grouped-meal-edit-ui-and-conditional-item-media-decision/76-01-SUMMARY.md
@.planning/phases/75-grouped-meal-direct-crud-contract/75-CONTEXT.md
@.planning/phases/75-grouped-meal-direct-crud-contract/75-02-SUMMARY.md
@.planning/phases/75-grouped-meal-direct-crud-contract/75-03-SUMMARY.md
@AGENTS.md
@server/services/meal-history.ts
@server/routes/meals.ts
@client/src/api.ts
@client/src/meal-edit-refresh.ts
@client/src/meal-edit-payload.ts
@tests/integration/meals-api.test.ts
</context>

## Artifacts this phase produces

| Artifact | Owning Plan | Contract |
|---|---:|---|
| `tests/unit/meal-edit-screen.test.ts` grouped editor contracts | 01 | Source assertions for grouped rows, edit/add/delete controls, invalid save copy, stale-block reuse, dirty discard confirmation, unsupported grouped payload copy, and no per-item media implication. |
| `tests/unit/api-client.test.ts` grouped update contracts | 01 | `updateMeal()` accepts grouped `{ expectedMealRevisionId, items }`, sends that body exactly, and parses grouped stale conflicts through `MealRevisionConflictError`. |
| `tests/unit/meal-edit-payload.test.ts` media and item authority contracts | 01 | Grouped item details remain sorted and image identity remains whole-meal; source assertions reject item media fields. |
| `client/src/meal-edit-grouped-draft.ts` | 02 | Exports `GroupedMealDraftRow`, `GroupedMealDraftField`, `GroupedMealDraftFieldErrors`, `GroupedMealDraftValidation`, `createGroupedMealDraftRows()`, `computeGroupedMealDraftTotals()`, `validateGroupedMealDraftRows()`, `buildGroupedMealUpdateItems()`, and `isGroupedMealDraftDirty()`. |
| `tests/unit/meal-edit-grouped-draft.test.ts` | 02 | Pure helper tests for totals, blank/non-numeric/negative validation, first invalid row, append/delete draft behavior, and zero-based position rebuild. |
| `client/src/types.ts` | 02 | Adds `ScalarUpdateMealInput`, `GroupedUpdateMealInput`, and `UpdateMealInput = ScalarUpdateMealInput | GroupedUpdateMealInput`; keeps `MealItemDetail` media-free with a source note tied to D-01/D-03. |
| `client/src/components/MealEditScreen.tsx` | 02 | Replaces grouped lock with grouped editor state, `GroupedMealEditor`, `GroupedMealRow`, `formatGroupedItemSummary()`, grouped save branch, unsupported state, row errors, final-row delete block, dirty discard, stale recovery, and `refreshAfterMealMutation` success close. |
| `client/src/app.css` grouped editor classes | 02 | Adds `sp-meal-edit-grouped-card`, `sp-meal-edit-grouped-header`, `sp-meal-edit-grouped-totals`, `sp-meal-edit-grouped-row`, `sp-meal-edit-grouped-row-expanded`, `sp-meal-edit-grouped-summary`, `sp-meal-edit-grouped-row-actions`, `sp-meal-edit-grouped-add`, `sp-meal-edit-field-error`, `sp-meal-edit-grouped-final-delete-error`, and `sp-meal-edit-grouped-empty`. |
| `server/services/meal-history.ts` DTO projection | 03 | Extends `MealHistoryEntry` rows with optional flat `items[]` details containing `name`, `position`, calories, protein, carbs, and fat. |
| `server/routes/meals.ts` GET `/api/meals` row DTO | 03 | Adds `items` to grouped authorized meal rows when available and keeps item rows media-free. |
| `tests/integration/meals-api.test.ts` grouped read-path proof | 03 | Fastify/SQLite proof that `GET /api/meals` exposes grouped flat `items[]`, no item media fields, and authorized rows only. |

## Multi-Source Coverage Audit

| Source | ID | Item | Covered By | Status |
|---|---|---|---|---|
| GOAL | Phase 76 goal | Grouped Meal Edit uses authoritative item rows and refreshes through existing DTO/store paths. | Plans 02, 03 | COVERED |
| REQ | GROUP-UI-01 | Grouped rows are editable from Home-origin payloads. | Plan 03 DTO path plus Plan 02 UI | COVERED |
| REQ | GROUP-UI-02 | Unsupported item-missing states are visible and recoverable. | Plan 02 | COVERED |
| REQ | GROUP-UI-03 | Successful grouped edits refresh affected meal, summary, and history state through existing authoritative DTO/store paths. | Tasks 1, 2 | COVERED |
| REQ | MEDIA-DECISION-01 | Item-level photo mapping is explicitly deferred with source assertions. | Tasks 1, 2 plus Plan 02 source note | COVERED |
| RESEARCH | R-DTO | `/api/meals` currently preserves `itemCount` but not item details; add flat `items[]` to existing path. | Tasks 1, 2 | COVERED |
| RESEARCH | R-AUTH | Route must keep signed guest-session ownership for meal reads. | Task 2 | COVERED |
| RESEARCH | R-MEDIA | Read DTO item rows must remain media-free. | Tasks 1, 2 | COVERED |
| CONTEXT | D-01 D-02 D-03 | Whole-meal media identity only and item-level mapping deferred. | Tasks 1, 2 | COVERED |
| CONTEXT | D-04 D-05 D-06 D-07 D-08 | UI can derive rows and draft totals only if authoritative items are present. | Plan 02, Task 2 | COVERED |
| CONTEXT | D-22 | Successful grouped save reuses `refreshAfterMealMutation`; this plan keeps that helper's `getMeals` path authoritative. | Tasks 1, 2 | COVERED |
| CONTEXT | D-24 | Unauthorized recovery follows existing pattern; this plan does not widen read ownership. | Task 2 | COVERED |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add grouped /api/meals projection proof</name>
  <files>tests/integration/meals-api.test.ts</files>
  <read_first>
    @tests/integration/meals-api.test.ts
    @server/routes/meals.ts
    @server/services/meal-history.ts
    @client/src/api.ts
    @.planning/phases/76-grouped-meal-edit-ui-and-conditional-item-media-decision/76-RESEARCH.md
    @.planning/phases/76-grouped-meal-edit-ui-and-conditional-item-media-decision/76-PATTERNS.md
    @.planning/phases/75-grouped-meal-direct-crud-contract/75-03-SUMMARY.md
  </read_first>
  <behavior>
    - `GET /api/meals` returns grouped rows with `items[]` ordered by zero-based position for meals owned by the signed guest session.
    - Each item row has exactly `name`, `position`, `calories`, `protein`, `carbs`, and `fat`.
    - No item row contains media fields such as `image`, `imageAssetId`, `asset`, `crop`, `thumbnail`, or `evidence`.
    - Aggregate fields and existing `imageAssetId`/`imageUrl` remain meal-level fields.
  </behavior>
  <action>Update the existing `GET /api/meals preserves grouped itemCount from meal history service rows` integration coverage or add a nearby test. Seed a grouped meal through the existing service fixture, call `app.inject()` with the signed session cookie, and assert the returned meal row includes `items` in submitted/persisted order as flat public item details: `name`, `position`, `calories`, `protein`, `carbs`, and `fat`. Assert the row still includes aggregate meal fields, meal-level `imageAssetId`/`imageUrl`, and `itemCount`. Assert item objects do not contain `image`, `imageAssetId`, `asset`, `crop`, `thumbnail`, or `evidence` per D-01 through D-03. Keep the existing unauthorized GET route coverage intact; do not add new auth selectors or a separate detail endpoint.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/meals-api.test.ts (expected nonzero until Task 2; failures must be limited to grouped /api/meals items projection assertions)</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Test uses real Fastify `app.inject()` and real SQLite fixture patterns from `meals-api.test.ts`.
    - Red failure proves current `/api/meals` does not yet expose grouped item details.
    - Test does not require a new endpoint, media persistence, or item-level photo mapping.
  </acceptance_criteria>
  <done>Read-path projection behavior is pinned before server DTO changes.</done>
</task>

<task type="auto">
  <name>Task 2: Expose media-free grouped item details on the existing meal read path</name>
  <files>server/services/meal-history.ts, server/routes/meals.ts</files>
  <read_first>
    @server/services/meal-history.ts
    @server/routes/meals.ts
    @tests/integration/meals-api.test.ts
    @client/src/api.ts
    @client/src/meal-edit-refresh.ts
    @.planning/phases/76-grouped-meal-edit-ui-and-conditional-item-media-decision/76-CONTEXT.md
    @.planning/phases/76-grouped-meal-edit-ui-and-conditional-item-media-decision/76-PATTERNS.md
    @AGENTS.md
  </read_first>
  <action>Extend `MealHistoryEntry` in `server/services/meal-history.ts` with optional `items?: Array<{ name: string; position: number; calories: number; protein: number; carbs: number; fat: number }>` or an equivalent local exported type. When building `itemsByRevisionId`, include each `meal_revision_items.position`, map persistence `foodName` to public `name`, keep order by `position`, and attach `items` only when a row has at least one revision item. Do not include per-item media fields. In `server/routes/meals.ts`, extend the existing authorized `GET /api/meals` row projection to spread `items: meal.items` when present, while preserving existing `mealRevisionId`, aggregate nutrition, `itemCount`, `imageAssetId`, `imageUrl`, `loggedAt`, and `mealPeriod` behavior. Keep the existing `resolveGuestSession()` route boundary and do not accept `deviceId` query params or headers. Do not change the Phase 75 `PATCH /api/meals/:id` grouped parser or response shape except if TypeScript requires shared local type names; grouped save refresh should continue to work because `client/src/api.ts normalizeMealEntry()` already normalizes `items[]` on GET rows.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/meals-api.test.ts</automated>
    <automated>yarn test:integration</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/meals` grouped row integration test passes with ordered flat `items[]`.
    - Unauthorized meal read behavior remains unchanged and cookie-backed.
    - No item-level media, crop, evidence, thumbnail, or asset fields appear on item rows.
    - Existing grouped PATCH success, invalid-body, missing/stale revision, summaryOutcome, and publish tests remain green.
  </acceptance_criteria>
  <done>Home and post-commit refresh can obtain grouped item details through the existing authoritative meal read path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Signed guest session -> `GET /api/meals` | Read ownership must remain derived from cookies through `resolveGuestSession()`. |
| SQLite revision items -> public DTO | Internal item rows become client-visible facts and must be shaped narrowly. |
| Public DTO -> Meal Edit payload builder | `items[]` must be authoritative enough for editing without adding media claims. |
| Grouped save refresh -> store state | Refresh uses existing `getMeals`/`setMeals` DTO path rather than local draft patching. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-76-13 | Elevation of Privilege | `GET /api/meals` | mitigate | Preserve `resolveGuestSession()` and device-scoped service call; integration test continues using signed cookie fixtures. |
| T-76-14 | Information Disclosure | grouped read DTO | mitigate | Expose only authorized rows already returned by `/api/meals`; do not add raw revision ids beyond existing public `mealRevisionId`, DB snapshots, or internal asset paths. |
| T-76-15 | Information Integrity | item media evidence | mitigate | Integration test asserts item rows contain no media/evidence fields and media stays meal-level only per D-01/D-03. |
| T-76-16 | Tampering | stale revision replay after refresh | mitigate | This plan does not alter PATCH conflict handling; existing Phase 75 grouped stale tests and Plan 02 UI stale-block contracts remain in force. |
| T-76-17 | Tampering | grouped over-posting | mitigate | This plan does not widen PATCH parser; existing strict grouped write tests must remain green after DTO projection changes. |
| T-76-18 | Information Disclosure | item-name rendering / XSS | transfer | Server returns strings as JSON; Plan 02 client renders names via JSX/input values and asserts no `dangerouslySetInnerHTML`. |
| T-76-SC | Tampering | package-manager installs | mitigate | No package installs are planned. If execution attempts npm/pip/cargo install, stop for package legitimacy audit first. |
</threat_model>

<verification>
Automated checks for this plan: `node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/meals-api.test.ts`, `yarn test:integration`, and `yarn tsc --noEmit`.
</verification>

<success_criteria>
- Grouped today rows returned by `/api/meals` include ordered flat item details for existing Meal Edit entry and refresh paths.
- DTO projection keeps item rows media-free while preserving whole-meal image identity.
- Existing route auth, grouped PATCH validation, revision conflict, summaryOutcome, and realtime publish tests remain green.
- No separate edit-detail endpoint, schema migration, package, item media persistence, or chat receipt persistence is introduced.
</success_criteria>

<output>
Create `.planning/phases/76-grouped-meal-edit-ui-and-conditional-item-media-decision/76-03-SUMMARY.md` when done.
</output>
