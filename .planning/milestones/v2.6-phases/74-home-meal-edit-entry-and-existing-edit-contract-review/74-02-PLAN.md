---
phase: 74-home-meal-edit-entry-and-existing-edit-contract-review
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/MealEditScreen.tsx
  - tests/unit/meal-edit-screen.test.ts
  - tests/integration/meals-api.test.ts
autonomous: true
requirements:
  - EDIT-BASE-01
must_haves:
  truths:
    - "D-05: Home-origin Meal Edit exposes an explicit `返回首頁` back label."
    - "D-10: Single-item save/delete still send `expectedMealRevisionId` and refresh through `refreshAfterMealMutation`."
    - "D-04/D-10: Grouped payloads remain locked from direct single-form editing before Phase 75 expands the contract."
  artifacts:
    - path: "client/src/components/MealEditScreen.tsx"
      provides: "Home-origin back label while preserving save/delete/revision/grouped-lock behavior."
      contains: "返回首頁"
    - path: "tests/unit/meal-edit-screen.test.ts"
      provides: "Source contract for Home back label, expected revision writes, refresh helper, stale blocking, and grouped lock."
    - path: "tests/integration/meals-api.test.ts"
      provides: "Fastify/SQLite revalidation for missing/stale revisions and grouped direct PATCH rejection."
  key_links:
    - from: "client/src/components/MealEditScreen.tsx"
      to: "client/src/api.ts"
      via: "updateMeal/deleteMeal expectedMealRevisionId"
      pattern: "expectedMealRevisionId: payload\\.mealRevisionId"
    - from: "client/src/components/MealEditScreen.tsx"
      to: "client/src/meal-edit-refresh.ts"
      via: "refreshAfterMealMutation after committed save/delete"
      pattern: "refreshAfterMealMutation\\("
---

<objective>
Revalidate the existing Meal Edit single-item contract and add the Home-origin back label required by Phase 74.

Purpose: EDIT-BASE-01 must stay green before grouped direct editing expands in later phases. This plan preserves the existing server-side revision authority and grouped-lock behavior while making Home-origin navigation explicit per D-05.
Output: A Home-origin label branch, strengthened source contracts, and Fastify/SQLite integration proof for revision and grouped-lock protections.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-CONTEXT.md
@.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-RESEARCH.md
@.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-PATTERNS.md
@.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-UI-SPEC.md
@.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-VALIDATION.md
@.planning/milestones/v2.3/phases/62-meal-revision-tokens-and-stale-receipt-protection/62-04-SUMMARY.md
@.planning/milestones/v2.3/phases/62-meal-revision-tokens-and-stale-receipt-protection/62-05-SUMMARY.md
@client/src/components/MealEditScreen.tsx
@client/src/meal-edit-refresh.ts
@tests/unit/meal-edit-screen.test.ts
@tests/unit/meal-edit-refresh.test.ts
@tests/integration/meals-api.test.ts
</context>

## Artifacts this phase produces

- `MealEditScreen` Home-origin back label branch returning `返回首頁`.
- Source-contract assertions that `MealEditScreen` still sends `expectedMealRevisionId: payload.mealRevisionId` for save/delete and still calls `refreshAfterMealMutation`.
- Source-contract assertions that grouped payloads remain in the existing `payload.itemCount > 1` lock with `到對話修正` and without save/delete/input controls.
- Integration assertions in `tests/integration/meals-api.test.ts` for missing/stale PATCH, missing/stale DELETE, no summary/publish side effects on revision conflicts, stale-before-grouped precedence, and current grouped direct PATCH rejection.
- Phase-wide related artifacts produced by other plans: nullable Home payload helper, Home whole-row activation, capability matrix source/doc corrections.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Home-origin Meal Edit back label and source proof</name>
  <files>client/src/components/MealEditScreen.tsx, tests/unit/meal-edit-screen.test.ts</files>
  <read_first>
    @.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-CONTEXT.md
    @.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-UI-SPEC.md
    @.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-PATTERNS.md
    @client/src/components/MealEditScreen.tsx
    @tests/unit/meal-edit-screen.test.ts
    @.planning/milestones/v2.3/phases/62-meal-revision-tokens-and-stale-receipt-protection/62-04-SUMMARY.md
    @.planning/milestones/v2.3/phases/62-meal-revision-tokens-and-stale-receipt-protection/62-05-SUMMARY.md
  </read_first>
  <behavior>
    - Home-origin `secondaryScreen.origin === "home"` uses `aria-label="返回首頁"` for Meal Edit back controls.
    - Chat and History back labels remain `返回對話` and `返回歷史`.
    - Save and delete continue to send `expectedMealRevisionId: payload.mealRevisionId`.
    - Grouped payloads still render the lock branch and do not expose direct save/delete/input controls.
  </behavior>
  <action>
    Update `MealEditScreen.tsx` back-label logic to include an explicit Home branch: `origin === "home" ? "返回首頁"` per D-05. Do not change `openMealEdit`, `closeSecondaryScreen`, save/delete mutation calls, stale conflict handling, or grouped-lock branching.

    Update `tests/unit/meal-edit-screen.test.ts` to assert the Home back label branch while preserving existing assertions for chat/history labels if already present or adding them if missing. Keep and strengthen source assertions for D-04, D-10, and D-11: `expectedMealRevisionId: payload.mealRevisionId`, `refreshAfterMealMutation`, `redactChatReceiptIdentity`, `recordMealMutation`, `setMeals`, `MealRevisionConflictError`, `payload.itemCount > 1`, `sp-meal-edit-grouped-lock`, `payload.items.map`, and absence of `儲存`, `<input`, macro fields, and `刪除` inside the grouped branch.
  </action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/meal-edit-screen.test.ts tests/unit/meal-edit-refresh.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `MealEditScreen.tsx` exposes `返回首頁` only for Home-origin Meal Edit.
    - Unit/source tests cover Home label, expected revision writes, shared refresh helper, stale conflict blocking, and grouped lock.
    - No grouped direct save/edit/delete UI is introduced.
    - The automated commands in `<verify>` pass.
  </acceptance_criteria>
  <done>Home-origin Meal Edit navigation is explicit and the existing single-item/grouped-lock source contract remains intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Revalidate server revision and grouped direct-edit protections</name>
  <files>tests/integration/meals-api.test.ts</files>
  <read_first>
    @.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-CONTEXT.md
    @.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-RESEARCH.md
    @.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-PATTERNS.md
    @tests/integration/meals-api.test.ts
    @server/routes/meals.ts
    @server/services/meal-transactions.ts
    @server/services/food-logging.ts
    @.planning/milestones/v2.3/phases/62-meal-revision-tokens-and-stale-receipt-protection/62-05-SUMMARY.md
  </read_first>
  <behavior>
    - PATCH without `expectedMealRevisionId` returns `409 MEAL_REVISION_REQUIRED`.
    - PATCH/DELETE with stale `expectedMealRevisionId` returns `409 MEAL_REVISION_STALE`.
    - Revision conflicts return `mealId`, `affectedDate`, and `currentMealRevisionId` only, with no summary fields and no publish side effects.
    - Stale single-item editor revisions are rejected before grouped-shape validation when the current meal has become grouped.
    - Current grouped direct PATCH remains rejected by the existing grouped-update lock when the expected revision is fresh.
  </behavior>
  <action>
    Extend the existing `tests/integration/meals-api.test.ts` coverage for EDIT-BASE-01 rather than editing server production files. Preserve real SQLite/Fastify `app.inject()` fixtures. Ensure the existing missing/stale PATCH and DELETE assertions also prove no `dailySummary`, `summaryOutcome`, or publish side effects on revision conflicts. Ensure the existing stale single-to-current-grouped PATCH test still proves stale revision wins before grouped-shape rejection. Add or tighten a fresh-current grouped direct PATCH assertion for `MEAL_REQUIRES_GROUPED_UPDATE` only if the current file does not already prove it with a matching expected revision.

    Do not modify `server/routes/meals.ts`, `server/services/meal-transactions.ts`, or `server/services/food-logging.ts` unless an integration failure proves the existing server contract regressed. If a server regression appears, stop and record the exact failing assertion before making the smallest required fix; server-side revision checks remain authoritative per D-10 and the security requirement.
  </action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/meals-api.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/meals-api.test.ts` proves missing/stale revision PATCH and DELETE fail closed with stable 409 bodies and no summary/publish side effects.
    - Stale-before-grouped precedence remains covered.
    - Fresh grouped direct PATCH remains rejected by the existing grouped update lock until Phase 75.
    - Server route/service code remains unchanged unless a regression was proven and fixed with targeted integration evidence recorded in the summary.
    - The automated commands in `<verify>` pass.
  </acceptance_criteria>
  <done>Existing single-item edit/delete stale protection and grouped direct-edit lock are revalidated before grouped CRUD planning continues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meal Edit UI -> `/api/meals/:id` | Client write requests carry expected revision identity into server mutation routes. |
| Guest session -> device-owned meal rows | Fastify routes must derive ownership from cookie-backed guest sessions and device-scoped services. |
| Grouped payload -> single-form editor | Existing grouped-lock branch must prevent direct grouped mutation until Phase 75 creates a validated contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Tampering | PATCH/DELETE stale meal overwrite | mitigate | Unit/source and integration proof must keep `expectedMealRevisionId` on save/delete and 409 missing/stale responses before mutation side effects. |
| T-74-02 | Elevation of Privilege | Cross-device meal mutation | mitigate | Do not weaken route/service ownership; run real Fastify/SQLite `tests/integration/meals-api.test.ts` through the guest-session fixture. |
| T-74-03 | Tampering | Fabricated edit identity | mitigate | Plan 01 owns nullable payload eligibility; this plan verifies Meal Edit writes consume only `payload.mealRevisionId`, not derived display text or Home-only state. |
| T-74-04 | Repudiation / Integrity | Capability matrix over-claiming | mitigate | Plan 03 corrects generated capability claims after this plan proves the real edit contract remains active. |
| T-74-SC | Tampering | npm/pip/cargo installs | accept | No dependency install is planned or needed. |
</threat_model>

<verification>
Plan-local gates:
- `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/meal-edit-screen.test.ts tests/unit/meal-edit-refresh.test.ts`
- `node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/meals-api.test.ts`
- `yarn tsc --noEmit`
</verification>

<success_criteria>
- EDIT-BASE-01 is covered by focused unit/source and real Fastify/SQLite integration proof.
- D-04, D-05, D-10, and D-11 are implemented or revalidated without adding Phase 75 grouped CRUD behavior.
- Server-side revision checks remain authoritative; Home navigation remains UX support only.
</success_criteria>

<output>
Create `.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-02-SUMMARY.md` when done.
</output>
