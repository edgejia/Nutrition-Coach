---
phase: 67-correction-targeting-and-backend-clarification-rendering
plan: 05
type: execute
wave: 4
depends_on:
  - 67-04
files_modified:
  - server/services/meal-correction.ts
  - server/orchestrator/tools.ts
  - tests/unit/meal-correction.test.ts
  - tests/unit/tools.test.ts
  - tests/integration/chat-meal-correction.integration.test.ts
autonomous: true
requirements:
  - TARGET-01
  - TARGET-02
user_setup: []
must_haves:
  truths:
    - "Delayed follow-up selections are revalidated before mutation."
    - "Stale or deleted selected options fail closed with no mutation, no summaryOutcome, and no daily_summary publish."
    - "The backend never auto-retargets by same label after stale/deleted selection failure."
    - "Mixed option-plus-numeric follow-up separates target resolution from numeric mutation authority."
  artifacts:
    - path: "server/services/meal-correction.ts"
      provides: "Pending option revalidation and stale/deleted recovery support"
    - path: "server/orchestrator/tools.ts"
      provides: "Fail-closed stale selected-target handling without auto-retargeting"
    - path: "tests/integration/chat-meal-correction.integration.test.ts"
      provides: "Fastify proof for stable copy, no mutation/publish, and mixed follow-up behavior"
  key_links:
    - from: "server/orchestrator/tools.ts"
      to: "server/services/meal-correction.ts"
      via: "selected target mealRevisionId and recovery helper"
      pattern: "MealRevisionPreconditionError"
    - from: "tests/integration/chat-meal-correction.integration.test.ts"
      to: "server/routes/chat.ts"
      via: "app.inject and publishDailySummary spy"
      pattern: "publishDailySummary"
---

<objective>
Finish follow-up selection, stale recovery, and route-level TARGET-01/TARGET-02 proof.

Purpose: keep helpful delayed option replies while preserving Phase 62/66 authority boundaries: revalidate before mutation, fail closed when stale, and never auto-retarget by label.
Output: green unit and integration tests for stale/deleted selected options, mixed follow-ups, no-publish behavior, and stable backend copy.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-CONTEXT.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-RESEARCH.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-VALIDATION.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-PATTERNS.md
@.planning/phases/66-numeric-correction-provenance-guard/66-VERIFICATION.md
@.codex/skills/nutrition-gen-test/SKILL.md
@.codex/skills/nutrition-verify-change/SKILL.md
@.codex/skills/nutrition-security-review/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Revalidate delayed selections and fail closed on stale/deleted selected targets</name>
  <files>server/services/meal-correction.ts, server/orchestrator/tools.ts, tests/unit/meal-correction.test.ts, tests/unit/tools.test.ts</files>
  <read_first>
    @server/services/meal-correction.ts
    @server/orchestrator/tools.ts
    @tests/unit/meal-correction.test.ts
    @tests/unit/tools.test.ts
    @server/services/meal-transactions.ts
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-CONTEXT.md
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-PATTERNS.md
    @.planning/phases/66-numeric-correction-provenance-guard/66-VERIFICATION.md
  </read_first>
  <action>Add service/tool support for delayed selection revalidation before mutation. A selected pending option may resolve only if the selected meal still exists, action still matches, and the original resolver-owned `mealRevisionId` remains the write precondition. If `update_meal` or `delete_meal` receives `MealRevisionPreconditionError` for a selected pending target, return renderer-owned no-mutation copy or current scoped options if recoverable, but do not preselect or auto-retarget a same-label replacement. Preserve existing Phase 62 `MEAL_REVISION_STALE` metadata and Phase 66 numeric authority checks. Implement D-39, D-40, D-41, D-44, D-45, D-46, and D-46a.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/meal-correction.test.ts tests/unit/tools.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: delayed valid number resolves only the originally rendered option and still passes the selected option's revision id to update/delete.
    - Behavior assertion: stale selected option produces no mutation result, no `mealMutationKind`, no `summaryOutcome`, and a renderer-owned no-update/clarification reply.
    - Behavior assertion: same-label replacement meals are shown only as fresh choices when safe; they are never auto-selected as the stale target replacement.
    - Security assertion: cross-device selected options cannot be revalidated because candidate loading and mutation services remain scoped to the original `deviceId`.
  </acceptance_criteria>
  <done>Delayed selections are useful but still revision-scoped; stale/deleted selected targets fail closed without retargeting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove route-level clarification, no-publish, and mixed follow-up behavior</name>
  <files>tests/integration/chat-meal-correction.integration.test.ts</files>
  <read_first>
    @tests/integration/chat-meal-correction.integration.test.ts
    @server/routes/chat.ts
    @server/app.ts
    @server/orchestrator/index.ts
    @server/orchestrator/tools.ts
    @server/services/meal-correction.ts
    @server/orchestrator/meal-numeric-authority.ts
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-CONTEXT.md
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-VALIDATION.md
  </read_first>
  <action>Make the Phase 67 Fastify tests green. Use `buildApp({ dbPath: ":memory:", llmProvider: mockLLM, onServicesReady })`, queue `find_meals` tool calls through `MockLLMProvider`, and spy on `services.publisher.publishDailySummary`. Required route behaviors: multi-candidate clarification reply contains stable numbers and `請直接回覆編號`; body has `didLogMeal: false`, `didMutateMeal: false`, no `summaryOutcome`, and no raw target echo; unresolved, invalid-number, stale/deleted, and vague mixed-follow-up paths call no `publishDailySummary`; `2，蛋白質改 28g` can commit only when `update_meal` also has current-turn numeric authority; `2，蛋白質改合理一點` resolves target but enters no-mutation clarification/proposal behavior. Implement D-42 and D-43 while preserving all prior D-01 through D-46a behavior.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/chat-meal-correction.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: Fastify JSON path returns backend-rendered copy as final `reply` without requiring a second LLM prose response.
    - Behavior assertion: unresolved and stale/deleted paths leave meal row/revision counts unchanged.
    - Behavior assertion: route body omits `summaryOutcome` and publisher spy receives zero calls for every no-mutation path.
    - Behavior assertion: mixed numeric follow-up respects Phase 66 current-turn numeric authority and does not let target selection bypass numeric guards.
  </acceptance_criteria>
  <done>Route-level proof for TARGET-01/TARGET-02 is green across stable clarification, no mutation, no publish, and mixed follow-up cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pending option -> mutator tool | A follow-up selection becomes a candidate write target only after revalidation. |
| mutator service -> summary publisher | Mutation success can publish summary; unresolved/stale paths must not. |
| user mixed follow-up -> numeric authority | Selected target and numeric mutation authority are separate gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Elevation of privilege | delayed selection revalidation | mitigate | Revalidate selected option against device-scoped service facts before mutation. |
| T-67-02 | Tampering | same-label retargeting | mitigate | Never auto-retarget stale/deleted selected options by label; only re-render safe choices. |
| T-67-03 | Tampering | stale revision overwrite | mitigate | Preserve resolver-owned expected revision id and existing `MealRevisionPreconditionError` fail-closed path. |
| T-67-04 | Information disclosure | route reply body | mitigate | Integration tests assert no raw correction echo, no internal identifiers, no summary internals on no-mutation paths. |
| T-67-05 | Tampering | SQL/query safety | mitigate | Reuse `mealCorrectionService` candidate loading and Drizzle builders; do not add raw SQL. |
| T-67-SC | Tampering | package installs | mitigate | No dependency additions. |
</threat_model>

<verification>
Run `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/meal-correction.test.ts tests/unit/tools.test.ts tests/integration/chat-meal-correction.integration.test.ts`, then `yarn tsc --noEmit`, `yarn test:unit`, and `yarn test:integration` because this plan touches service, orchestrator tool, unit tests, and integration tests.
</verification>

<success_criteria>
TARGET-01/TARGET-02 route behavior is green with real SQLite and `MockLLMProvider`: stable backend copy, no unresolved/stale mutation, no summaryOutcome, no daily_summary publish, no raw correction echo, and no same-label retargeting.
</success_criteria>

<output>
Create `.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-05-SUMMARY.md` when done.
</output>
