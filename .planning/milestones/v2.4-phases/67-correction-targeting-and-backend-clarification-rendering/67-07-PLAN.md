---
phase: 67-correction-targeting-and-backend-clarification-rendering
plan: 07
type: execute
wave: 6
depends_on:
  - 67-06
files_modified:
  - server/services/meal-correction.ts
  - tests/unit/meal-correction.test.ts
  - .planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-VALIDATION.md
autonomous: true
requirements:
  - TARGET-01
user_setup: []
must_haves:
  truths:
    - "Explicit date scope is applied before the newest-candidate cap, so an existing historical-date meal cannot be dropped by more than 20 newer meals."
    - "Residual Latin food labels such as `burger` count as explicit food evidence after date, period, verb, nutrient, unit, and numeric target text are stripped."
    - "When residual food evidence exists but no candidate label matches, resolver output does not fall back to weak period-only or recency-only target resolution."
    - "Phase 67 validation bookkeeping records the gap-closure proof after targeted service, typecheck, unit, and integration gates pass."
  artifacts:
    - path: "server/services/meal-correction.ts"
      provides: "Gap-closed candidate loading and food-evidence detection for TARGET-01"
      exports: ["createMealCorrectionService"]
    - path: "tests/unit/meal-correction.test.ts"
      provides: "Regression proof for explicit-date cap ordering and unmatched Latin food evidence"
    - path: ".planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-VALIDATION.md"
      provides: "Updated Phase 67 verification map for the gap-closure plan"
  key_links:
    - from: "tests/unit/meal-correction.test.ts"
      to: "server/services/meal-correction.ts"
      via: "createMealCorrectionService(db).findMeals(...)"
      pattern: "findMeals"
    - from: "server/services/meal-correction.ts"
      to: "server/db/schema.ts"
      via: "Drizzle candidate query scoped by deviceId before date-filtered candidate limiting"
      pattern: "eq\\(mealTransactions\\.deviceId"
---

<objective>
Close the Phase 67 TARGET-01 verification gaps found in `67-VERIFICATION.md`.

Purpose: preserve backend-owned correction target authority when explicit historical dates or Latin food-label evidence are present.
Output: service resolver fix, targeted regression tests, and updated Phase 67 validation bookkeeping.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-CONTEXT.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-RESEARCH.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-PATTERNS.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-VERIFICATION.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-VALIDATION.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-02-SUMMARY.md
@.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-06-SUMMARY.md
@.codex/skills/nutrition-gen-test/SKILL.md
@.codex/skills/nutrition-verify-change/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TARGET-01 gap regression tests</name>
  <files>tests/unit/meal-correction.test.ts</files>
  <read_first>
    @tests/unit/meal-correction.test.ts
    @server/services/meal-correction.ts
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-VERIFICATION.md
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-CONTEXT.md
  </read_first>
  <action>Add two red-first regression tests in `tests/unit/meal-correction.test.ts`. First, create one explicit historical-date meal on `2026-04-18`, then create more than 20 newer active meals for the same device, and assert `mealCorrectionService.findMeals(deviceId, "update", "把 4/18 的雞腿飯改成 500 卡")` resolves the historical meal instead of returning no target or a newer meal. Second, create a current-day lunch candidate whose label does not include `burger`, call `findMeals(deviceId, "update", "把今天午餐 burger 改成 500 卡")`, and assert the result is not `resolved`, has no `resolvedMealId`, and does not return the unrelated lunch as the chosen target. The tests must use real SQLite helpers already present in the file and must not add fixtures outside the test file.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test --test-name-pattern "Phase 67 gap" tests/unit/meal-correction.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/unit/meal-correction.test.ts` contains a `Phase 67 gap` test where at least 21 newer meals are inserted after the explicit historical-date meal.
    - Behavior assertion: the explicit historical-date test fails before the service fix because candidate limiting happens before date scoping.
    - Source assertion: `tests/unit/meal-correction.test.ts` contains a `Phase 67 gap` test for unmatched Latin food evidence using the token `burger`.
    - Behavior assertion: the Latin-label test fails before the service fix because weak period-only matching can resolve the unrelated lunch.
  </acceptance_criteria>
  <done>The two verification gaps are captured as focused failing service tests before production logic changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply date-scoped loading and residual Latin food evidence</name>
  <files>server/services/meal-correction.ts, tests/unit/meal-correction.test.ts</files>
  <read_first>
    @server/services/meal-correction.ts
    @tests/unit/meal-correction.test.ts
    @server/lib/historical-date.ts
    @server/lib/meal-period.ts
    @server/services/meal-display.ts
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-VERIFICATION.md
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-CONTEXT.md
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-PATTERNS.md
  </read_first>
  <action>In `server/services/meal-correction.ts`, resolve the date intent before loading candidates in `findMeals()`. Change `loadActiveCandidates` to accept an options object such as `{ limit?: number; targetDateKey?: string }`; when `targetDateKey` is present, filter selected headers by `formatLocalDate(new Date(header.loggedAt)) === targetDateKey` before applying the newest `limit` cap, then load revision items only for that filtered header set. Preserve the Drizzle `deviceId` and `deletedAt` query guard and keep the default newest-20 behavior for calls without a target date. Ensure explicit historical dates with no meals still return D-30 date-specific no-meals copy instead of the generic no-candidate prompt. Also replace `hasLikelyFoodReference()` with residue-based food evidence detection: after `extractTargetEvidenceText()` and existing date, period, recent-reference, action, nutrient, unit, and numeric stripping, treat remaining CJK food-like text or Latin alphabet tokens of length at least 2, such as `burger`, as explicit food evidence. Do not count stripped tokens such as `today`, `lunch`, `protein`, `kcal`, `cal`, `g`, `合理`, or numeric/unit text as food evidence. Keep D-18/D-19 behavior: label matches narrow first; unmatched likely food evidence returns clarification/not-found and cannot fall through to weak period-only or recent-only resolution.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test --test-name-pattern "Phase 67 gap|Phase 67 D-01|Phase 67 D-19" tests/unit/meal-correction.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `findMeals()` calls `resolveFindMealsTargetDateKey()` before `loadActiveCandidates(...)`.
    - Source assertion: `loadActiveCandidates` applies `targetDateKey` filtering before the newest `limit` cap.
    - Behavior assertion: an explicit historical-date meal older than more than 20 newer active meals still resolves when label evidence matches that date.
    - Behavior assertion: `把今天午餐 burger 改成 500 卡` does not resolve an unrelated lunch candidate when no candidate label includes `burger`.
    - Behavior assertion: existing Chinese unmatched-label D-19 behavior remains green.
    - Security assertion: candidate SQL remains built with Drizzle query builders and remains scoped by `eq(mealTransactions.deviceId, deviceId)`.
  </acceptance_criteria>
  <done>The resolver applies date scope before candidate limiting and treats residual Latin food labels as explicit unmatched food evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run Phase 67 gates and update validation bookkeeping</name>
  <files>.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-VALIDATION.md</files>
  <read_first>
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-VALIDATION.md
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-VERIFICATION.md
    @.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-06-SUMMARY.md
    @AGENTS.md
    @.codex/skills/nutrition-verify-change/SKILL.md
  </read_first>
  <action>Run the gap-closure verification chain after Task 2 is green: `node scripts/run-node-with-tz.mjs --import tsx --test --test-name-pattern "Phase 67 gap|Phase 67 D-01|Phase 67 D-19" tests/unit/meal-correction.test.ts`, `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/meal-correction.test.ts`, `yarn tsc --noEmit`, `yarn test:unit`, and `yarn test:integration`. If all pass, update `67-VALIDATION.md` by adding task row `67-07-01` or equivalent gap-closure row for TARGET-01 with status `green`, noting that explicit-date candidate loading and Latin food evidence regressions are covered. Keep Phase 67 scoped to TARGET-01/TARGET-02; do not run `yarn release:check`, Railway smoke, staging promotion, main promotion, or Phase 68 structured-tool-result work.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/meal-correction.test.ts && yarn tsc --noEmit && yarn test:unit && yarn test:integration</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: every command in the automated verification chain exits 0.
    - Source assertion: `67-VALIDATION.md` includes a green Phase 67 gap-closure verification row for TARGET-01.
    - Source assertion: the validation update references explicit-date candidate loading before cap and residual Latin food evidence.
    - Scope assertion: no staging/main promotion, Railway smoke, schema push, package install, harness artifact generation, `yarn release:check`, or Phase 68 structured tool-result work is performed.
  </acceptance_criteria>
  <done>Phase 67 gap-closure proof is green and validation bookkeeping reflects the additional TARGET-01 regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User/model query text -> food evidence detector | Untrusted target text controls whether resolver can consider weak fallback tiers. |
| Historical date intent -> candidate loading | Date text controls which persisted meal headers are eligible before limiting. |
| Candidate loading -> mutator tools | Resolved meal id and revision authorize later update/delete operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-07-01 | Tampering | `loadActiveCandidates` | mitigate | Apply explicit `targetDateKey` filtering before newest-limit truncation so older in-scope meals are not silently excluded. |
| T-67-07-02 | Tampering | `hasLikelyFoodReference` / evidence-tier resolver | mitigate | Treat residual Latin/CJK target terms as food evidence and block fallback to unrelated period/recency targets when labels do not match. |
| T-67-07-03 | Information disclosure | clarification/no-target copy | mitigate | Reuse existing renderer-owned generic clarification/no-meals copy; do not add raw payloads or sensitive artifacts. |
| T-67-07-SC | Supply chain | package management | mitigate | No dependency additions or package-manager changes. |
</threat_model>

<verification>
Run `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/meal-correction.test.ts`, `yarn tsc --noEmit`, `yarn test:unit`, and `yarn test:integration`.
</verification>

<success_criteria>
TARGET-01 verification gaps from `67-VERIFICATION.md` are closed by executable tests and service logic. Explicit historical-date scoping happens before candidate limiting, and unmatched Latin food-label evidence cannot resolve an unrelated meal through weaker fallback tiers.
</success_criteria>

<output>
Create `.planning/phases/67-correction-targeting-and-backend-clarification-rendering/67-07-SUMMARY.md` when done.
</output>
