---
phase: 65-tool-contract-alignment-and-meal-period-authority
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/db/schema.ts
  - server/lib/meal-period.ts
  - server/services/meal-transactions.ts
  - server/services/food-logging.ts
  - tests/unit/meal-transactions.test.ts
  - tests/unit/food-logging.test.ts
autonomous: true
requirements: [INTENT-01]
must_haves:
  truths:
    - "D-08/D-09/D-18: loggedAt remains timestamp authority while nullable mealPeriod represents explicit user meal-category authority only."
    - "D-11/D-12/D-13/D-15: persisted mealPeriod can only come from backend-normalized direct source-text meal-category words, not raw model-only labels or time-of-day words."
    - "D-20: ordinary meal updates preserve existing explicit mealPeriod when the edit omits period changes."
  artifacts:
    - path: "server/lib/meal-period.ts"
      provides: "MealPeriod enum helpers and source-text explicit authority extractor"
      exports: ["MealPeriod", "normalizeMealPeriod", "extractExplicitMealPeriodFromSourceText"]
    - path: "server/db/schema.ts"
      provides: "Nullable meal_transactions.meal_period schema column"
      contains: "mealPeriod"
    - path: "server/services/meal-transactions.ts"
      provides: "Create/read/update service propagation for nullable mealPeriod"
      contains: "mealPeriod"
  key_links:
    - from: "server/services/food-logging.ts"
      to: "server/services/meal-transactions.ts"
      via: "logFood/logGroupedMeal input propagation"
      pattern: "mealPeriod"
    - from: "tests/unit/meal-transactions.test.ts"
      to: "server/db/schema.ts"
      via: "real SQLite transaction assertions"
      pattern: "meal_period"
---

<objective>
Create the persistence and service foundation for explicit meal-period authority.

Purpose: INTENT-01 needs a structured backend fact that remains separate from loggedAt, so later tool, DTO, UI, and correction-candidate plans can project authority without re-deriving it from clock heuristics.
Output: Nullable schema field, pure meal-period helper, transaction/service propagation, and focused real-SQLite unit proof.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-RESEARCH.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-PATTERNS.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-VALIDATION.md
@AGENTS.md
@docs/codex.md
@.codex/skills/nutrition-gen-test/SKILL.md
@.codex/skills/nutrition-verify-change/SKILL.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add explicit meal-period helper and schema field</name>
  <files>server/lib/meal-period.ts, server/db/schema.ts, tests/unit/meal-transactions.test.ts</files>
  <read_first>
    - server/db/schema.ts
    - server/lib/historical-date.ts
    - server/orchestrator/source-text-guard.ts
    - tests/unit/meal-transactions.test.ts
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-PATTERNS.md
  </read_first>
  <behavior>
    - Test first: normalizeMealPeriod accepts only "breakfast", "lunch", "dinner", and "late_night".
    - Test first: extractExplicitMealPeriodFromSourceText maps direct words "早餐/早飯" to "breakfast", "午餐/午飯" to "lunch", "晚餐/晚飯" to "dinner", and "宵夜" to "late_night".
    - Test first: "早上", "中午", and "晚上" return undefined and never become persisted explicit authority.
    - Test first: source text with multiple distinct direct meal periods, such as `午餐是雞腿便當，晚餐是沙拉`, returns undefined so one log call does not manufacture one authority for multiple periods.
  </behavior>
  <action>Create `server/lib/meal-period.ts` with exported `MealPeriod`, `MEAL_PERIODS`, `normalizeMealPeriod(value: unknown): MealPeriod | undefined`, and `extractExplicitMealPeriodFromSourceText(sourceText: string): MealPeriod | undefined`. Use direct source-text meal-category words only per D-11/D-12/D-15; do not reuse `extractHistoricalMealPeriod` because it accepts time-of-day words. If multiple distinct direct meal periods are present in one source string, return undefined to avoid violating D-14's one-log-call/one-meal-period authority rule; Phase 65 does not implement Phase 67 clarification rendering. Add nullable `mealPeriod: text("meal_period", { enum: ["breakfast", "lunch", "dinner", "late_night"] })` to `mealTransactions` in `server/db/schema.ts` per D-08/D-09/D-18. Keep local imports explicit with `.js` where needed.</action>
  <acceptance_criteria>
    - Source assertion: `server/lib/meal-period.ts` exports `MealPeriod`, `normalizeMealPeriod`, and `extractExplicitMealPeriodFromSourceText`.
    - Source assertion: `server/db/schema.ts` contains `mealPeriod` mapped to SQLite column `meal_period` and does not add a non-null/default inferred value.
    - Behavior assertion: unit tests prove direct meal-category words persist as enum authority and time-of-day words return no authority.
    - Behavior assertion: multi-period source text returns no single persisted mealPeriod authority in this helper.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/meal-transactions.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>Meal-period authority helper and nullable schema field exist, with source-text extraction constrained to direct meal-category words only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread mealPeriod through transaction and food logging services</name>
  <files>server/services/meal-transactions.ts, server/services/food-logging.ts, tests/unit/meal-transactions.test.ts, tests/unit/food-logging.test.ts</files>
  <read_first>
    - server/services/meal-transactions.ts
    - server/services/food-logging.ts
    - server/lib/meal-period.ts
    - tests/unit/meal-transactions.test.ts
    - tests/unit/food-logging.test.ts
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
  </read_first>
  <behavior>
    - Test first: createTransaction with `mealPeriod: "lunch"` stores and returns `"lunch"` while preserving the supplied `loggedAt`.
    - Test first: createTransaction without `mealPeriod` stores and returns `null`/absent explicit authority.
    - Test first: updateTransaction that omits period changes keeps the existing transaction-level `mealPeriod`.
  </behavior>
  <action>Extend `CreateMealTransactionInput`, `MealTransactionWriteResult`, transaction row projections, and food logging compatibility entries with nullable `mealPeriod` per D-08/D-09/D-20. Set `meal_transactions.meal_period` during transaction creation only from validated service input. Return existing `mealPeriod` from update/delete/read projections, but do not include it in ordinary update `.set(...)` calls, so numeric/name/image/item edits preserve explicit period authority. Thread the field through `createFoodLoggingService().logFood`, `logGroupedMeal`, `getMealsByDate`, and `updateMeal` without changing `summaryOutcome` behavior.</action>
  <acceptance_criteria>
    - Source assertion: transaction create values include `mealPeriod`, and ordinary update `.set(...)` does not clear or overwrite `mealPeriod`.
    - Behavior assertion: real SQLite service tests show new rows can store `"lunch"`, legacy/no-authority rows remain null, and omitted-period updates preserve the original value.
    - Behavior assertion: food logging compatibility entries expose explicit `mealPeriod` when the transaction has one.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/meal-transactions.test.ts tests/unit/food-logging.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>Food logging and transaction services can write, read, and preserve explicit mealPeriod without changing loggedAt semantics.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service input -> SQLite | Meal-period authority crosses into durable storage. |
| tests/proof -> repo artifacts | Validation evidence must not store raw user or tool payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Tampering | `server/lib/meal-period.ts` | mitigate | D-11/D-12 extractor accepts only direct category words and excludes time-of-day phrases from persisted authority. |
| T-65-02 | Tampering | `server/services/meal-transactions.ts` | mitigate | Store nullable `mealPeriod` only from validated backend input and preserve it on ordinary updates per D-20. |
| T-65-03 | Information Disclosure | tests | mitigate | Unit tests assert enum facts and DB metadata only; do not persist raw prompts, raw tool args, images, session material, or database snapshots per D-27. |
| T-65-SC | Tampering | package installs | accept | No npm/pip/cargo install task exists; use existing yarn scripts only. |
</threat_model>

<verification>
Run targeted unit tests after each task, then `yarn tsc --noEmit` before handing off to migration generation.
</verification>

<success_criteria>
INTENT-01 persistence foundation is complete when `meal_transactions.meal_period` is represented in schema/service types, explicit values can be stored, missing values remain null, and edits that omit period changes preserve explicit authority.
</success_criteria>

<output>
Create `.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-01-SUMMARY.md` when done.
</output>
