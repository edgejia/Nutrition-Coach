---
phase: 65-tool-contract-alignment-and-meal-period-authority
plan: 02
type: execute
wave: 2
depends_on: [65-01]
files_modified:
  - drizzle/0007_*.sql
  - drizzle/meta/0007_snapshot.json
  - drizzle/meta/_journal.json
autonomous: true
requirements: [INTENT-01]
must_haves:
  truths:
    - "D-09/D-18: generated SQL adds nullable meal_period without default, inferred backfill, or destructive rewrite."
    - "The Drizzle migration blocker runs after schema edits and before broad verification."
  artifacts:
    - path: "drizzle/0007_*.sql"
      provides: "Additive nullable SQLite migration for meal_transactions.meal_period"
      contains: "ADD COLUMN meal_period TEXT CHECK"
    - path: "drizzle/meta/0007_snapshot.json"
      provides: "Generated Drizzle snapshot for schema version 0007"
    - path: "drizzle/meta/_journal.json"
      provides: "Journal entry for migration idx 7/tag 0007_*"
  key_links:
    - from: "server/db/schema.ts"
      to: "drizzle/0007_*.sql"
      via: "yarn db:generate"
      pattern: "meal_period"
---

<objective>
Generate and inspect the Drizzle migration for the nullable meal-period authority column.

Purpose: The schema-relevant change must produce additive SQLite SQL before broad Phase 65 verification proceeds.
Output: Generated `0007` SQL, snapshot, and journal entry verified against the expected nullable `meal_period` contract.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-RESEARCH.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-PATTERNS.md
@AGENTS.md
@drizzle/meta/_journal.json
@drizzle.config.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: [BLOCKING] Generate nullable meal_period migration</name>
  <files>drizzle/0007_*.sql, drizzle/meta/0007_snapshot.json, drizzle/meta/_journal.json</files>
  <read_first>
    - server/db/schema.ts
    - drizzle/meta/_journal.json
    - drizzle.config.ts
    - package.json
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-PATTERNS.md
  </read_first>
  <action>Run `yarn db:generate` after Plan 65-01 schema modifications are complete. Inspect the generated `drizzle/0007_*.sql`, `drizzle/meta/0007_snapshot.json`, and `drizzle/meta/_journal.json`. The expected SQL is an additive nullable column on `meal_transactions`: `ALTER TABLE meal_transactions ADD COLUMN meal_period TEXT CHECK (meal_period IN ('breakfast','lunch','dinner','late_night'));`. Do not hand-write migration contents before running the generator. If generated SQL includes a `NOT NULL`, `DEFAULT`, inferred-value backfill, table rebuild/drop/copy, or any change outside nullable `meal_period`, stop and report a manual review blocker before continuing.</action>
  <acceptance_criteria>
    - CLI assertion: `yarn db:generate` exits 0 and creates one new `drizzle/0007_*.sql` plus `drizzle/meta/0007_snapshot.json`.
    - Source assertion: generated SQL contains `ADD COLUMN meal_period TEXT CHECK (meal_period IN ('breakfast','lunch','dinner','late_night'))`.
    - Source assertion: generated SQL does not contain `NOT NULL`, `DEFAULT`, `UPDATE meal_transactions`, `CREATE TABLE __new_meal_transactions`, `DROP TABLE meal_transactions`, or inferred-value backfill.
    - Source assertion: `_journal.json` contains idx `7` and a tag beginning with `0007_`.
  </acceptance_criteria>
  <verify>
    <automated>yarn db:generate && grep -E "ADD COLUMN meal_period TEXT CHECK \\(meal_period IN \\('breakfast','lunch','dinner','late_night'\\)\\)" drizzle/0007_*.sql && ! grep -E "meal_period TEXT NOT NULL|DEFAULT|UPDATE meal_transactions|__new_meal_transactions|DROP TABLE meal_transactions" drizzle/0007_*.sql && node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/meal-transactions.test.ts tests/unit/food-logging.test.ts</automated>
  </verify>
  <done>Generated migration is additive, nullable, and contains no inferred-value backfill or destructive table rewrite.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drizzle schema -> generated SQL | ORM schema changes become durable migration artifacts. |
| generated SQL -> existing SQLite data | Migration can affect existing meal records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-04 | Tampering | `drizzle/0007_*.sql` | mitigate | Block on inspection for additive nullable `meal_period` only; reject backfill, NOT NULL, DEFAULT, or table rewrite. |
| T-65-05 | Denial of Service | migration application | mitigate | Run targeted real-SQLite service tests after generation to catch schema/application mismatch before broad verification. |
| T-65-06 | Information Disclosure | migration/proof output | mitigate | Verification records SQL shape and command status only; no database snapshots or raw user data per D-27. |
| T-65-SC | Tampering | package installs | accept | No package install task exists; `yarn db:generate` uses existing `drizzle-kit`. |
</threat_model>

<verification>
This plan is the phase migration blocker. Do not proceed to broad `yarn test`, `yarn test:integration`, or release gates until it passes or is manually reviewed.
</verification>

<success_criteria>
The migration gate is complete when `0007` SQL and metadata exist and prove nullable explicit-only `meal_period` persistence with no inferred backfill.
</success_criteria>

<output>
Create `.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-02-SUMMARY.md` when done.
</output>
