---
phase: 65-tool-contract-alignment-and-meal-period-authority
plan: 06
type: execute
wave: 5
depends_on: [65-04, 65-05]
files_modified:
  - client/src/types.ts
  - client/src/api.ts
  - client/src/meal-edit-payload.ts
  - tests/unit/api-client.test.ts
  - tests/unit/meal-edit-payload.test.ts
autonomous: true
requirements: [TOOL-03, INTENT-02]
must_haves:
  truths:
    - "D-16/D-17/D-18: client DTOs accept public mealPeriod only as the four-value enum and never add inferredMealPeriod."
    - "D-20/D-21: edit payload builders preserve explicit mealPeriod from rows/receipts without synthesizing fallback authority."
  artifacts:
    - path: "client/src/types.ts"
      provides: "Public MealPeriod, MealEntry, LoggedMealReceipt, and MealEditPayload typing"
    - path: "client/src/api.ts"
      provides: "Transport guard/normalizer for explicit mealPeriod"
    - path: "client/src/meal-edit-payload.ts"
      provides: "Edit payload preservation for explicit mealPeriod"
  key_links:
    - from: "client/src/api.ts"
      to: "client/src/types.ts"
      via: "normalizeMealPeriod guard"
      pattern: "mealPeriod"
    - from: "client/src/meal-edit-payload.ts"
      to: "client edit state"
      via: "payload builders"
      pattern: "mealPeriod"
---

<objective>
Normalize explicit mealPeriod through client transport types and edit payload construction.

Purpose: INTENT-02 requires browser state to carry backend period authority without confusing fallback display inference with persisted facts.
Output: Client enum type, API normalizers, edit payload preservation, and unit proof.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-PATTERNS.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-UI-SPEC.md
@AGENTS.md
@.codex/skills/nutrition-gen-test/SKILL.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add client mealPeriod DTO guards</name>
  <files>client/src/types.ts, client/src/api.ts, tests/unit/api-client.test.ts</files>
  <read_first>
    - client/src/types.ts
    - client/src/api.ts
    - tests/unit/api-client.test.ts
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-UI-SPEC.md
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
  </read_first>
  <behavior>
    - Test first: `normalizeLoggedMealReceipt` preserves `mealPeriod: "lunch"` and drops invalid values.
    - Test first: `getMeals`, `getDaySnapshot`, `normalizeHistoryMeal`, and update-meal response normalization preserve only the four enum values.
    - Test first: invalid or missing mealPeriod is absent/undefined, not coerced to a fallback label.
  </behavior>
  <action>Add `export type MealPeriod = "breakfast" | "lunch" | "dinner" | "late_night"` to `client/src/types.ts` and optional `mealPeriod?: MealPeriod` to `LoggedMealReceipt`, `MealEntry`, and `MealEditPayload`. In `client/src/api.ts`, add a local `normalizeMealPeriod(value: unknown): MealPeriod | undefined` guard accepting exactly those four values. Thread the guard through logged meal receipt normalization, `/api/meals`, `/api/day-snapshot`, `normalizeHistoryMeal`, direct update response normalization, and chat SSE/JSON terminal payload parsing. Do not add `inferredMealPeriod`; do not serialize fallback labels as mealPeriod.</action>
  <acceptance_criteria>
    - Source assertion: `client/src/types.ts` defines `MealPeriod` with exactly the four public enum values.
    - Source assertion: `client/src/api.ts` has a guard that rejects invalid mealPeriod transport values.
    - Behavior assertion: API client unit tests prove explicit values are preserved and invalid/missing values are omitted.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/api-client.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>Client transport can carry explicit mealPeriod safely without exposing inferred authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve mealPeriod in edit payload builders</name>
  <files>client/src/meal-edit-payload.ts, tests/unit/meal-edit-payload.test.ts</files>
  <read_first>
    - client/src/meal-edit-payload.ts
    - client/src/types.ts
    - tests/unit/meal-edit-payload.test.ts
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-UI-SPEC.md
  </read_first>
  <behavior>
    - Test first: `buildHistoryMealEditPayload` copies explicit `mealPeriod: "lunch"` from a `MealEntry`.
    - Test first: `buildReceiptMealEditPayload` copies explicit `mealPeriod: "late_night"` from a `LoggedMealReceipt`.
    - Test first: builders omit mealPeriod when source rows/receipts lack explicit authority.
  </behavior>
  <action>Update `client/src/meal-edit-payload.ts` to copy `mealPeriod` from source `MealEntry` and `LoggedMealReceipt` into `MealEditPayload` when present. Do not infer a period from `loggedAt`, do not create a period picker or correction UI, and do not include `mealPeriod` in ordinary update submit bodies unless an existing code path already serializes full edit payload state for display; Phase 65 only preserves explicit authority in client edit state per D-20/D-21.</action>
  <acceptance_criteria>
    - Behavior assertion: meal edit payload unit tests prove explicit period preservation from history rows and chat receipts.
    - Behavior assertion: missing period stays absent and is not derived from `loggedAt`.
    - Source assertion: no new UI control, picker, toast, or modal is introduced.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/meal-edit-payload.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>Edit payload builders preserve explicit mealPeriod authority for downstream edit state without manufacturing inferred values.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API payload -> browser state | Backend DTO fields become client state and edit payloads. |
| client edit state -> mutation requests | Preserved meal facts can influence future edit flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-17 | Tampering | `client/src/api.ts` | mitigate | Normalize only exact enum values; drop invalid mealPeriod values instead of coercing fallback authority. |
| T-65-18 | Tampering | `client/src/meal-edit-payload.ts` | mitigate | Preserve explicit backend field only; never infer from loggedAt in payload builders. |
| T-65-19 | Information Disclosure | client tests | mitigate | Unit tests assert structured enum fields only and avoid raw chat/tool payload artifacts per D-27. |
| T-65-SC | Tampering | package installs | accept | No package install task exists. |
</threat_model>

<verification>
Run targeted client unit tests and `yarn tsc --noEmit` after TypeScript edits.
</verification>

<success_criteria>
Client DTO propagation is complete when browser transport and edit payload builders preserve explicit backend mealPeriod and reject invalid or inferred public values.
</success_criteria>

<output>
Create `.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-06-SUMMARY.md` when done.
</output>
