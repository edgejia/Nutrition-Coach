---
phase: 65-tool-contract-alignment-and-meal-period-authority
plan: 05
type: execute
wave: 4
depends_on: [65-03, 65-04]
files_modified:
  - server/routes/chat.ts
  - server/services/chat.ts
  - tests/integration/chat-api.test.ts
  - tests/integration/chat-streaming.test.ts
autonomous: true
requirements: [TOOL-03, INTENT-02]
must_haves:
  truths:
    - "D-16/D-17: live and restored loggedMeal receipts carry public mealPeriod when backend authority exists."
    - "D-18/D-27: chat receipt projection does not synthesize inferredMealPeriod and proof remains metadata-only."
  artifacts:
    - path: "server/routes/chat.ts"
      provides: "JSON/SSE terminal loggedMeal mealPeriod projection"
    - path: "server/services/chat.ts"
      provides: "Restored chat receipt mealPeriod reconstruction"
    - path: "tests/integration/chat-api.test.ts"
      provides: "JSON chat receipt regression proof"
    - path: "tests/integration/chat-streaming.test.ts"
      provides: "SSE terminal receipt regression proof"
  key_links:
    - from: "server/orchestrator/tools.ts"
      to: "server/routes/chat.ts"
      via: "ToolExecutionResult.loggedMeal -> projectLoggedMealReceipt"
      pattern: "mealPeriod"
    - from: "server/services/chat.ts"
      to: "chat history receipts"
      via: "mealTransactions.mealPeriod select"
      pattern: "mealPeriod"
---

<objective>
Carry explicit mealPeriod through live and restored chat logged-meal receipts.

Purpose: TOOL-03 and INTENT-02 require successful text/image logging receipts to remain backend-committed mutation facts while projecting the new explicit meal-period authority.
Output: Chat route/service receipt projection and JSON/SSE integration proof.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-PATTERNS.md
@.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-VALIDATION.md
@AGENTS.md
@.codex/skills/nutrition-gen-test/SKILL.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Project mealPeriod in live chat JSON and SSE receipts</name>
  <files>server/routes/chat.ts, tests/integration/chat-api.test.ts, tests/integration/chat-streaming.test.ts</files>
  <read_first>
    - server/routes/chat.ts
    - server/orchestrator/tools.ts
    - tests/integration/chat-api.test.ts
    - tests/integration/chat-streaming.test.ts
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
  </read_first>
  <behavior>
    - Test first: JSON `/api/chat` log_food response includes `loggedMeal.mealPeriod: "lunch"` for explicit lunch authority.
    - Test first: SSE terminal `done` payload includes `loggedMeal.mealPeriod: "lunch"` for the same committed meal fact.
    - Test first: terminal payload still includes `summaryOutcome` exactly as before for committed meal logs.
  </behavior>
  <action>Extend `projectLoggedMealReceipt` in `server/routes/chat.ts` to include `mealPeriod` only when the orchestrator loggedMeal receipt contains a valid public enum value. Apply the same projection to JSON responses, SSE `done`, and SSE `stopped` terminal payloads through the existing single helper. Preserve existing `didLogMeal`, `summaryOutcome`, `dailySummary`, `affectedDate`, upload cleanup, status/chunk/done ordering, and fallback sanitization behavior.</action>
  <acceptance_criteria>
    - Behavior assertion: chat JSON and SSE integration tests both observe `loggedMeal.mealPeriod: "lunch"` for explicit source-text lunch.
    - Behavior assertion: no test expects or receives `inferredMealPeriod`.
    - Behavior assertion: summaryOutcome and committed receipt fields remain present on successful log_food.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/chat-api.test.ts tests/integration/chat-streaming.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>Live chat receipts project explicit mealPeriod consistently across JSON and SSE terminal payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconstruct mealPeriod in stored chat receipts</name>
  <files>server/services/chat.ts, tests/integration/chat-api.test.ts</files>
  <read_first>
    - server/services/chat.ts
    - server/db/schema.ts
    - tests/integration/chat-api.test.ts
    - .planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-CONTEXT.md
  </read_first>
  <behavior>
    - Test first: restored chat history receipt for a current active meal includes `mealPeriod: "lunch"` when the transaction has explicit authority.
    - Test first: stale/deleted receipt identity behavior from Phase 62 remains unchanged.
  </behavior>
  <action>Select `mealTransactions.mealPeriod` in `server/services/chat.ts` receipt reconstruction queries and include optional `mealPeriod` in restored logged meal receipt DTOs. Keep Phase 62 current-active receipt identity rules intact: stale/deleted receipts must still omit edit identity fields as before while preserving display-safe fields. Do not add raw user text, tool args, or database snapshots to persisted chat messages or proof output.</action>
  <acceptance_criteria>
    - Behavior assertion: restored current-active receipt includes explicit `mealPeriod` when persisted.
    - Behavior assertion: stale/deleted receipt tests still prove display-only behavior where applicable.
    - Source assertion: chat service selects only structured meal transaction fields and does not persist raw tool payloads.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/chat-api.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>Stored chat receipt reconstruction carries explicit mealPeriod without weakening stale receipt protections.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend receipt -> chat API/SSE | Meal mutation facts cross into browser-visible chat state. |
| persisted chat history -> restored client receipts | Stored receipt facts are replayed later and may regain edit affordances only when current. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-14 | Tampering | `server/routes/chat.ts` | mitigate | Project only backend loggedMeal mealPeriod, never raw model `meal_period`, and preserve summaryOutcome committed-fact semantics. |
| T-65-15 | Information Disclosure | `server/services/chat.ts` | mitigate | Restore structured meal fields only; do not persist raw prompts, user text, tool args, image data, or session material per D-27. |
| T-65-16 | Elevation of Privilege | restored receipts | mitigate | Keep Phase 62 current-active mealRevisionId rules so stale/deleted receipts remain display-only. |
| T-65-SC | Tampering | package installs | accept | No package install task exists. |
</threat_model>

<verification>
Run targeted chat integration tests and `yarn tsc --noEmit` after TypeScript edits.
</verification>

<success_criteria>
Chat receipt projection is complete when live JSON, SSE terminal payloads, and restored chat history receipts all carry explicit mealPeriod when present and never synthesize inferred public period authority.
</success_criteria>

<output>
Create `.planning/phases/65-tool-contract-alignment-and-meal-period-authority/65-05-SUMMARY.md` when done.
</output>
