---
phase: 74-home-meal-edit-entry-and-existing-edit-contract-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/meal-edit-payload.ts
  - client/src/components/HomeScreen.tsx
  - client/src/app.css
  - tests/unit/meal-edit-payload.test.ts
  - tests/unit/home-dashboard-contract.test.ts
autonomous: true
requirements:
  - HOME-EDIT-01
must_haves:
  truths:
    - "D-01/D-03/D-07: Eligible Home today meal rows activate the existing Meal Edit flow through openMealEdit(payload, \"home\")."
    - "D-06/D-08/D-09: Incomplete Home meal rows render silently read-only and never fabricate edit identity."
    - "D-04: Complete grouped Home meals open Meal Edit but remain governed by the existing grouped-lock branch."
  artifacts:
    - path: "client/src/meal-edit-payload.ts"
      provides: "Nullable Home/History meal edit payload eligibility wrapper."
      exports: ["buildMealEditPayloadIfComplete"]
    - path: "client/src/components/HomeScreen.tsx"
      provides: "Whole-row Home meal edit activation for eligible rows."
      contains: "openMealEdit(editPayload, \"home\")"
    - path: "client/src/app.css"
      provides: "Stable hover/focus styling for interactive Home meal rows."
      contains: "home-sport-meal-row"
    - path: "tests/unit/meal-edit-payload.test.ts"
      provides: "Authority-preserving nullable helper proof."
    - path: "tests/unit/home-dashboard-contract.test.ts"
      provides: "Home source contract for eligible button semantics and ineligible read-only fallback."
  key_links:
    - from: "client/src/components/HomeScreen.tsx"
      to: "client/src/meal-edit-payload.ts"
      via: "buildMealEditPayloadIfComplete(meal, todayDateKey)"
      pattern: "buildMealEditPayloadIfComplete\\(meal, todayDateKey\\)"
    - from: "client/src/components/HomeScreen.tsx"
      to: "client/src/store.ts"
      via: "openMealEdit(editPayload, \"home\")"
      pattern: "openMealEdit\\(editPayload, \"home\"\\)"
---

<objective>
Make Home today meal rows enter the existing revision-safe Meal Edit flow for eligible meals.

Purpose: Phase 74 HOME-EDIT-01 closes the missing Home entry surface without creating a Home-only edit contract. D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-09, D-11, and D-12 govern this plan.
Output: A nullable payload eligibility helper, accessible Home row activation, focused source/unit proof, and styling that preserves the current Home row layout.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-CONTEXT.md
@.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-RESEARCH.md
@.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-PATTERNS.md
@.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-UI-SPEC.md
@.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-VALIDATION.md
@.planning/milestones/v2.3/phases/62-meal-revision-tokens-and-stale-receipt-protection/62-04-SUMMARY.md
@.planning/milestones/v2.5/phases/71-authoritative-dto-validation-expansion/71-02-SUMMARY.md
@client/src/meal-edit-payload.ts
@client/src/components/HomeScreen.tsx
@client/src/components/HistoryScreen.tsx
@client/src/components/MessageBubble.tsx
@client/src/app.css
@tests/unit/meal-edit-payload.test.ts
@tests/unit/home-dashboard-contract.test.ts
</context>

## Artifacts this phase produces

- `buildMealEditPayloadIfComplete(meal, dateKey)` in `client/src/meal-edit-payload.ts`.
- Home `MealRows` eligible-row path that computes `todayDateKey`, calls `buildMealEditPayloadIfComplete(meal, todayDateKey)`, and invokes `openMealEdit(editPayload, "home")`.
- Home ineligible-row fallback that keeps the existing read-only `article.home-sport-meal-row` visual path.
- Interactive Home row CSS branch for hover and focus-visible styling without a chevron, edit icon, disabled copy, or layout shift.
- `tests/unit/meal-edit-payload.test.ts` assertions for complete payloads, missing revision/core authority returning `null`, and grouped item preservation.
- `tests/unit/home-dashboard-contract.test.ts` assertions for native button semantics, `aria-label` copy `編輯 {meal label} {foodName}`, Home origin, payload helper use, grouped routing, and silent ineligible fallback.
- Phase-wide related artifacts produced by later plans: `MealEditScreen` Home back label, matrix source/doc corrections, and single-item edit/delete revalidation.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add nullable Home/History payload eligibility helper</name>
  <files>client/src/meal-edit-payload.ts, tests/unit/meal-edit-payload.test.ts</files>
  <read_first>
    @.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-CONTEXT.md
    @.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-RESEARCH.md
    @.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-PATTERNS.md
    @client/src/meal-edit-payload.ts
    @tests/unit/meal-edit-payload.test.ts
    @.planning/milestones/v2.5/phases/71-authoritative-dto-validation-expansion/71-02-SUMMARY.md
  </read_first>
  <behavior>
    - Complete authoritative `MealEntry` input returns the same `MealEditPayload` as `buildHistoryMealEditPayload`.
    - Missing or blank `mealRevisionId` returns `null`, not a thrown render-time exception.
    - Missing id, foodName, finite nutrition, positive `itemCount`, or `loggedAt` authority returns `null`.
    - Grouped rows preserve normalized `items`, `itemCount`, `imageAssetId`, `imageUrl`, `loggedAt`, and explicit `mealPeriod`.
  </behavior>
  <action>
    Add `buildMealEditPayloadIfComplete(meal: MealEntry, dateKey: string): MealEditPayload | null` next to `buildHistoryMealEditPayload` per D-06, D-07, D-08, and D-09. Implement it as a narrow non-throw wrapper around `buildHistoryMealEditPayload`, returning `null` for the builder's defensive authority/revision failures and not manufacturing fallback ids, revisions, nutrition, item counts, dates, image identity, item details, or meal periods. Keep the existing throwing builder unchanged for History callers.

    Update `tests/unit/meal-edit-payload.test.ts` to import the new helper and assert complete, missing revision, incomplete authority, and grouped-item cases. Keep Node built-in `node:test`, `node:assert/strict`, explicit `.js` imports, and existing fixture style. Cite D-03/D-04/D-06/D-07/D-08/D-09 in test names or assertion comments only if a comment is genuinely useful; do not add broad explanatory comments.
  </action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/meal-edit-payload.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `client/src/meal-edit-payload.ts` exports `buildMealEditPayloadIfComplete`.
    - `tests/unit/meal-edit-payload.test.ts` proves complete rows return payloads, missing revision/core authority returns `null`, and grouped items remain present.
    - The existing `buildHistoryMealEditPayload` throwing behavior remains intact for current History tests.
    - The automated commands in `<verify>` pass.
  </acceptance_criteria>
  <done>Home can ask whether a meal is edit-eligible without render-time exceptions or fallback authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire eligible Home rows to Meal Edit with native row activation</name>
  <files>client/src/components/HomeScreen.tsx, client/src/app.css, tests/unit/home-dashboard-contract.test.ts</files>
  <read_first>
    @.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-CONTEXT.md
    @.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-UI-SPEC.md
    @.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-PATTERNS.md
    @client/src/components/HomeScreen.tsx
    @client/src/components/HistoryScreen.tsx
    @client/src/components/MessageBubble.tsx
    @client/src/app.css
    @tests/unit/home-dashboard-contract.test.ts
  </read_first>
  <behavior>
    - Eligible Home rows render as native `<button type="button">` whole-row controls.
    - Button `aria-label` is `編輯 {getDisplayMealLabel(meal.mealPeriod, meal.loggedAt)} {meal.foodName}`.
    - Clicking an eligible row calls `openMealEdit(editPayload, "home")` with public `mealId` and `mealRevisionId` from `buildMealEditPayloadIfComplete`.
    - Ineligible rows render the existing read-only `article.home-sport-meal-row` with no disabled copy, tooltip, chevron, edit icon, click handler, or keyboard handler.
  </behavior>
  <action>
    Update `HomeScreen.tsx` per D-01, D-02, D-03, D-04, D-07, D-08, D-09, D-11, and D-12. Import `buildMealEditPayloadIfComplete` from `../meal-edit-payload.js`; get `openMealEdit` through `useStore`; derive `todayDateKey` from `dailySummary?.date ?? formatLocalDate(new Date())`; pass `todayDateKey` into `MealRows`; for each meal compute `editPayload = buildMealEditPayloadIfComplete(meal, todayDateKey)`. Render eligible rows as native buttons that preserve the existing thumbnail, meta, title, macro, and calorie content and call `openMealEdit(editPayload, "home")`. Render ineligible rows through the existing article path without visible disabled affordance or explanatory copy. Grouped meals with complete payloads must follow the same button path and rely on `MealEditScreen` `payload.itemCount > 1`.

    Update `client/src/app.css` to make only interactive Home row buttons show pointer, hover, active, and focus-visible affordances matching the existing History row pattern. Preserve stable dimensions and the existing visual hierarchy; do not add a chevron, edit icon, badge, glow, disabled state, or new copy.

    Replace the existing `Home meal rows stay read-only` source-contract test in `tests/unit/home-dashboard-contract.test.ts` with assertions for the new eligible/ineligible split. Keep assertions for `getMealBadge`, `formatMealRowTime`, `getDisplayMealLabel`, `getMealMacroSummary`, calories, and empty-state Chat handoff. Add negative assertions for `SportPlusIcon`, chevron/edit icons, `openSecondaryScreen("mealEdit")`, disabled edit copy, and custom Home edit routes.
  </action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/home-dashboard-contract.test.ts tests/unit/meal-edit-payload.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `HomeScreen.tsx` contains `buildMealEditPayloadIfComplete(meal, todayDateKey)` and `openMealEdit(editPayload, "home")`.
    - Eligible rows use native button semantics; ineligible rows remain read-only articles.
    - Existing Home row content and empty-state Chat routing remain source-tested.
    - No Phase 74 code introduces grouped direct item edit/add/delete, Home post-edit highlights, or Home-specific edit state.
    - The automated commands in `<verify>` pass.
  </acceptance_criteria>
  <done>Home today rows expose revision-safe whole-row Meal Edit entry for complete meals while defensive incomplete rows stay silent read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Home UI -> Zustand edit state | Home passes untrusted client row data into the existing secondary-screen state boundary. |
| Client edit identity -> API writes | Client `mealRevisionId` is UX support only; server `expectedMealRevisionId` checks remain authoritative. |
| Generated docs -> developer/operator decisions | Capability claims can mislead future work if they overstate active handlers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Tampering | `MealEditScreen` save/delete stale protection | mitigate | Do not change server revision behavior in this plan; keep Home routing limited to `MealEditPayload` construction and `openMealEdit(payload, "home")`. Plan 02 revalidates `expectedMealRevisionId`. |
| T-74-02 | Elevation of Privilege | `/api/meals/:id` device ownership | mitigate | No route or service changes in this plan; integration proof in Plan 02 must keep cookie/device-scoped mutation checks green. |
| T-74-03 | Tampering | `buildMealEditPayloadIfComplete` and Home row activation | mitigate | Return `null` for missing public id/revision/core authority and render read-only rows instead of fabricating identity, per D-06 through D-09. |
| T-74-04 | Repudiation / Integrity | Capability matrix and generated docs | mitigate | Plan 03 updates source-of-truth metadata and runs `yarn matrix:check`; this plan prepares real Home handler evidence for the scan. |
| T-74-SC | Tampering | npm/pip/cargo installs | accept | No package installation is planned or allowed in Phase 74; Package Legitimacy Audit says none. |
</threat_model>

<verification>
Plan-local gates:
- `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/meal-edit-payload.test.ts`
- `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/home-dashboard-contract.test.ts tests/unit/meal-edit-payload.test.ts`
- `yarn tsc --noEmit`
</verification>

<success_criteria>
- HOME-EDIT-01 is covered for eligible Home rows using public meal id and revision identity.
- D-01 through D-04 and D-06 through D-12 are implemented without adding deferred grouped direct editing or Home-specific post-edit highlight behavior.
- Ineligible fallback behavior is silent, non-throwing, and source-tested.
</success_criteria>

<output>
Create `.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-01-SUMMARY.md` when done.
</output>
