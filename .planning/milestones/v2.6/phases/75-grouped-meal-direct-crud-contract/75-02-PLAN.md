---
phase: 75-grouped-meal-direct-crud-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 75-01
files_modified:
  - server/routes/meals.ts
autonomous: true
requirements:
  - GROUP-EDIT-01
  - GROUP-EDIT-02
  - GROUP-EDIT-03
  - GROUP-EDIT-04
must_haves:
  truths:
    - "PATCH /api/meals/:id accepts a valid grouped items[] full-list replacement under expectedMealRevisionId."
    - "Scalar update fields and grouped items[] are mutually exclusive request shapes."
    - "Grouped request items are strict public flat rows and are mapped to persistence foodName."
    - "Successful grouped replacement reuses the existing post-commit summaryOutcome, optional dailySummary, and publish path."
  artifacts:
    - path: "server/routes/meals.ts"
      provides: "Strict grouped PATCH parser and route wiring"
      contains: "parseGroupedMealItems"
    - path: "server/routes/meals.ts"
      provides: "Internal parsed body union"
      contains: "GroupedMealUpdateBody"
  key_links:
    - from: "server/routes/meals.ts"
      to: "foodLoggingService.updateMeal"
      via: "grouped items[] branch"
      pattern: "kind: \"items\""
    - from: "server/routes/meals.ts"
      to: "publishDailySummarySafe"
      via: "existing post-commit block"
      pattern: "source: \"meal_mutation\""
---

<objective>
Implement the strict grouped full-list replacement branch inside the existing direct meal PATCH route.

Purpose: Make the red-first Plan 01 contracts pass by extending `PATCH /api/meals/:id` only, preserving D-01 through D-32 without adding item subroutes, schema changes, packages, UI controls, or chat persistence.
Output: Surgical `server/routes/meals.ts` parser and route wiring changes.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/75-grouped-meal-direct-crud-contract/75-CONTEXT.md
@.planning/phases/75-grouped-meal-direct-crud-contract/75-RESEARCH.md
@.planning/phases/75-grouped-meal-direct-crud-contract/75-PATTERNS.md
@.planning/phases/75-grouped-meal-direct-crud-contract/75-VALIDATION.md
@.planning/phases/75-grouped-meal-direct-crud-contract/75-01-SUMMARY.md
@AGENTS.md
@server/routes/meals.ts
@server/services/food-logging.ts
@server/services/meal-transactions.ts
@server/db/schema.ts
@tests/integration/meals-api.test.ts
</context>

## Artifacts This Phase Produces

| Artifact | Owning Plan | Contract |
|---|---:|---|
| `tests/integration/meals-api.test.ts` grouped PATCH success and invalid-body contract tests | 01 | Red-first coverage for `items[]` full-list replacement, invalid grouped payloads, conflict/no-side-effect behavior, and scalar-on-grouped fallback. |
| `server/routes/meals.ts` internal `ScalarMealUpdateBody`, `GroupedMealUpdateBody`, `ParsedMealUpdateBody`, `parseGroupedMealItems()`, and strict grouped branch in `parseMealUpdateBody()` | 02 | Existing `PATCH /api/meals/:id` accepts either scalar update fields or strict grouped `items[]`, never both. |
| `PATCH /api/meals/:id` grouped full-list replacement contract | 02 | Public item shape is `{ name, position, calories, protein, carbs, fat }`; server maps `name` to persistence `foodName`; response remains `affectedDate`, `summaryOutcome`, optional `dailySummary`, and aggregate `meal`. |
| `tests/unit/meal-transactions.test.ts` focused revision-item persistence coverage | 03 | Direct proof that full-list update writes new revision rows in submitted order, preserves duplicate names, supports 1-to-many and many-to-one shape changes, and preserves image identity. |
| Metadata-only final proof commands | 03 | `yarn tsc --noEmit`, `yarn test:integration`, and `yarn test:unit` when unit transaction tests change. |

<tasks>

<task type="auto">
  <name>Task 1: Add strict grouped parser beside scalar parser</name>
  <files>server/routes/meals.ts</files>
  <read_first>
    @server/routes/meals.ts
    @server/services/meal-transactions.ts
    @client/src/types.ts
    @.planning/phases/75-grouped-meal-direct-crud-contract/75-CONTEXT.md
    @.planning/phases/75-grouped-meal-direct-crud-contract/75-PATTERNS.md
  </read_first>
  <action>Replace the scalar-only `MealUpdateBody` shape with internal `ScalarMealUpdateBody`, `GroupedMealUpdateBody`, and `ParsedMealUpdateBody` types. The request body must not gain any public discriminator field per D-02; the internal union may use `kind: "scalar"` and `kind: "items"`. Keep the current scalar parser behavior for bodies without an `items` own-property, except reject scalar bodies that also contain `items` per D-03. Add a grouped parser path selected by an own `items` property. For grouped bodies, accept only top-level `items` and optional `expectedMealRevisionId`; reject top-level scalar fields, `imageAssetId`, `mode`, `operation`, or any other extra key per D-02, D-03, D-23, and D-24. Add `parseGroupedMealItems()` that requires a nonempty array and item objects with exactly `name`, `position`, `calories`, `protein`, `carbs`, and `fat` per D-07, D-09, D-10, and D-21. Each item must trim `name`, reject blank names, require `position` as an integer exactly equal to its zero-based array index, and require finite nonnegative nutrition numbers per D-11 and D-19. Map accepted public `name` to service `foodName` per D-08, preserve array order per D-17, and allow duplicate trimmed names per D-18. Do not add stable item IDs, intent metadata, partial item operations, or sorting per D-13, D-14, D-15, D-16, and D-20.</action>
  <verify>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `server/routes/meals.ts` exports no new public route and imports no new external package.
    - Grouped parser rejects tolerant read-path shapes and returns `null` so the route sends `{ error: "Invalid meal update" }`.
    - Scalar parser behavior remains compatible for existing scalar PATCH tests.
  </acceptance_criteria>
  <done>The direct route has a strict parsed-body union ready to wire into mutation behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire grouped updates through existing post-commit path</name>
  <files>server/routes/meals.ts</files>
  <read_first>
    @server/routes/meals.ts
    @server/services/food-logging.ts
    @server/services/meal-transactions.ts
    @tests/integration/meals-api.test.ts
    @.planning/phases/75-grouped-meal-direct-crud-contract/75-CONTEXT.md
  </read_first>
  <action>In the `app.patch("/api/meals/:id")` handler, branch on the internal parsed `kind`. For `kind: "scalar"`, keep the current `foodLoggingService.getMealMutationGuard()` call, stale-before-grouped ordering, scalar-on-grouped `MEAL_REQUIRES_GROUPED_UPDATE` response, owned image validation, and single-item `foodLoggingService.updateMeal()` call. For `kind: "items"`, skip the scalar grouped-lock guard and image asset validation because grouped writes cannot carry image input per D-24. Call `foodLoggingService.updateMeal(deviceId, id, { expectedMealRevisionId: update.expectedMealRevisionId, items: update.items })` so `mealTransactionsService.updateTransaction()` performs the authoritative expected-revision check per D-31 and full-list revision write per D-04. Reuse the existing catch block, `affectedDateKey` calculation, `buildSummaryOutcomeAfterMealCommit()`, `dailySummaryFromOutcome()`, `publishDailySummarySafe()`, and aggregate response block unchanged per D-05, D-26, D-27, D-28, D-30, D-31, and D-32. Do not create chat receipts, assistant messages, compressed-history mutation outcomes, client UI controls, schema changes, package installs, or item subroutes per D-01, D-25, and D-29.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/meals-api.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Plan 01 grouped success and invalid-body tests pass.
    - Existing scalar PATCH, DELETE, auth, foreign-device, image validation, grouped scalar rejection, stale-revision, and publish envelope tests still pass.
    - Conflict branches return through `sendMealRevisionConflict()` and do not run summary recompute or publish.
  </acceptance_criteria>
  <done>Grouped `items[]` replacement works through the existing direct route and post-commit mutation path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser client -> route parser | Untrusted grouped JSON must be validated before service mutation. |
| Route parser -> transaction service | Parsed public `name` values become persisted `foodName` facts. |
| Route -> asset service | Grouped writes must not accept image mutation input in Phase 75. |
| Route -> realtime publisher | Publish only after successful commit and usable affected-date summary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-75-06 | Tampering | `parseGroupedMealItems()` | mitigate | Exact key allowlist, nonempty array, zero-based positions, finite nonnegative numbers, and nonblank names. |
| T-75-07 | Elevation of Privilege | `resolveGuestSession()` and `foodLoggingService.updateMeal()` | mitigate | Preserve existing signed cookie session resolution and pass only resolved `deviceId` into services. |
| T-75-08 | Tampering | stale `expectedMealRevisionId` | mitigate | Let `updateTransaction()` enforce the expected revision before writes and convert `MealRevisionPreconditionError` with existing 409 shape. |
| T-75-09 | Information Disclosure | route response and realtime envelope | mitigate | Reuse existing aggregate response and `meal_mutation` envelope; do not include item details, summaryOutcome in publish payload, chat receipts, or internal revision item rows. |
| T-75-10 | Repudiation | direct route edits and chat history | accept | Phase 75 intentionally keeps direct route edits out of chat receipts/outcomes per D-29; authoritative revision history remains in meal revisions. |
| T-75-SC | Tampering | package-manager installs | mitigate | No package installs are planned. If execution attempts an npm/pip/cargo install, stop and require package legitimacy audit before continuing. |
</threat_model>

<verification>
Plan 02 is complete when `tests/integration/meals-api.test.ts` passes through the timezone wrapper and `yarn tsc --noEmit` passes.
</verification>

<success_criteria>
- The only production change is `server/routes/meals.ts`.
- Valid grouped full-list replacement works for 1-to-many, many-to-one, and many-to-many payloads.
- Invalid grouped shapes return simple 400, while missing/stale expected revisions return existing 409 bodies for valid grouped payloads.
- Summary recompute and realtime publish remain after successful commit only.
</success_criteria>

<output>
Create `.planning/phases/75-grouped-meal-direct-crud-contract/75-02-SUMMARY.md` when done.
</output>
