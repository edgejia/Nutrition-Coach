---
phase: 75-grouped-meal-direct-crud-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/meals-api.test.ts
autonomous: true
requirements:
  - GROUP-EDIT-01
  - GROUP-EDIT-02
  - GROUP-EDIT-03
  - GROUP-EDIT-04
must_haves:
  truths:
    - "PATCH /api/meals/:id has failing integration coverage for strict grouped items[] replacement before route code changes."
    - "Grouped add/update/delete are expressed only as a resulting nonempty ordered items[] list."
    - "Malformed grouped request bodies are covered with simple 400 Invalid meal update assertions."
    - "Successful grouped replacement coverage expects the existing affectedDate, summaryOutcome, optional dailySummary, and aggregate meal response."
  artifacts:
    - path: "tests/integration/meals-api.test.ts"
      provides: "Red-first Fastify/SQLite grouped PATCH contract coverage"
      contains: "PATCH /api/meals/:id accepts grouped items replacement"
  key_links:
    - from: "tests/integration/meals-api.test.ts"
      to: "PATCH /api/meals/:id"
      via: "app.inject"
      pattern: "method: \"PATCH\""
    - from: "tests/integration/meals-api.test.ts"
      to: "summary/publish side-effect spies"
      via: "services.summaryService.getDailySummary and services.publisher.publishDailySummary"
      pattern: "summaryCalls|publishCalls"
---

<objective>
Create red-first integration contract coverage for the Phase 75 grouped direct CRUD route shape before production route code changes.

Purpose: Lock the public server contract from D-01, D-02, D-03, D-04, D-05, D-07, D-08, D-09, D-10, D-11, D-15, D-17, D-18, D-19, D-20, D-21, D-23, D-24, D-26, D-27, D-28, D-30, D-31, and D-32 so Plan 02 has exact behavior to satisfy.
Output: Failing integration tests in `tests/integration/meals-api.test.ts`; no production files changed in this plan.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-CONTEXT.md
@.planning/phases/74-home-meal-edit-entry-and-existing-edit-contract-review/74-02-SUMMARY.md
@.planning/phases/75-grouped-meal-direct-crud-contract/75-CONTEXT.md
@.planning/phases/75-grouped-meal-direct-crud-contract/75-RESEARCH.md
@.planning/phases/75-grouped-meal-direct-crud-contract/75-PATTERNS.md
@.planning/phases/75-grouped-meal-direct-crud-contract/75-VALIDATION.md
@AGENTS.md
@tests/integration/meals-api.test.ts
@server/routes/meals.ts
@server/services/food-logging.ts
@server/services/meal-transactions.ts
</context>

## Artifacts This Phase Produces

| Artifact | Owning Plan | Contract |
|---|---:|---|
| `tests/integration/meals-api.test.ts` grouped PATCH success and invalid-body contract tests | 01 | Red-first coverage for `items[]` full-list replacement, invalid grouped payloads, conflict/no-side-effect behavior, and scalar-on-grouped fallback. |
| `server/routes/meals.ts` internal `ScalarMealUpdateBody`, `GroupedMealUpdateBody`, `ParsedMealUpdateBody`, `parseGroupedMealItems()`, and strict grouped branch in `parseMealUpdateBody()` | 02 | Existing `PATCH /api/meals/:id` accepts either scalar update fields or strict grouped `items[]`, never both. |
| `PATCH /api/meals/:id` grouped full-list replacement contract | 02 | Public item shape is `{ name, position, calories, protein, carbs, fat }`; server maps `name` to persistence `foodName`; response remains `affectedDate`, `summaryOutcome`, optional `dailySummary`, and aggregate `meal`. |
| `tests/unit/meal-transactions.test.ts` focused revision-item persistence coverage | 03 | Direct proof that full-list update writes new revision rows in submitted order, preserves duplicate names, supports 1-to-many and many-to-one shape changes, and preserves image identity. |
| Metadata-only final proof commands | 03 | `yarn tsc --noEmit`, `yarn test:integration`, and `yarn test:unit` when unit transaction tests change. |

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---:|---|---|
| GOAL | - | Grouped meals can be edited directly through item-level add, update, and delete operations instead of being locked to chat correction. | 01, 02, 03 | COVERED | Route contract tests, implementation, and persistence/side-effect proof cover the server-owned contract. |
| REQ | GROUP-EDIT-01 | Direct item updates for item name, calories, and macros through a validated server contract. | 01, 02, 03 | COVERED | Active requirements and D-07/D-10 lock the Phase 75 item shape to name/nutrition facts; `serving` remains rejected as an unsupported extra write key. |
| REQ | GROUP-EDIT-02 | Direct additions without model-authored committed authority. | 01, 02, 03 | COVERED | 1-to-many replacement test covers add as resulting list; no chat receipt/outcome path is introduced. |
| REQ | GROUP-EDIT-03 | Direct item deletion without whole-meal deletion. | 01, 02, 03 | COVERED | many-to-one replacement and empty-list rejection cover omission as delete and prevent whole-meal delete semantics. |
| REQ | GROUP-EDIT-04 | Revision checks, affected-date freshness, summaryOutcome, and realtime publish behavior. | 01, 02, 03 | COVERED | Existing post-commit route block is reused and conflicts stay side-effect free. |
| RESEARCH | - | Extend existing PATCH route; no new packages, schema, UI, or item subroutes. | 02 | COVERED | Plan 02 modifies `server/routes/meals.ts` only for production behavior. |
| RESEARCH | - | Strict grouped parser rejects aliases, nested nutrition, extra item keys, empty lists, invalid positions, and bad numbers. | 01, 02 | COVERED | Plan 01 adds invalid-body assertions; Plan 02 implements parser. |
| RESEARCH | - | Transaction service already writes complete ordered item lists and preserves image identity. | 03 | COVERED | Plan 03 adds focused transaction proof without schema changes. |
| CONTEXT | D-01 D-02 D-03 D-04 D-05 D-06 | Public route shape and response contract. | 01, 02 | COVERED | D-06 is respected by not requiring `meal.items` in PATCH response. |
| CONTEXT | D-07 D-08 D-09 D-10 D-11 D-12 | Strict public flat item wire shape and zero-based positions. | 01, 02 | COVERED | Tests require zero-based position equal to array index. |
| CONTEXT | D-13 D-14 D-15 D-16 D-17 D-18 | Revision-scoped item identity, no stable IDs or intent metadata, submitted order preserved, duplicate names allowed. | 01, 02, 03 | COVERED | Implementation maps submitted array order directly to service items; unit proof checks persistence. |
| CONTEXT | D-19 D-20 D-21 D-22 D-23 D-24 D-25 | Validation and edge cases. | 01, 02, 03 | COVERED | Empty list is 400, mixed shapes are 400, image is preserved, whole-meal delete remains existing DELETE. |
| CONTEXT | D-26 D-27 D-28 D-29 D-30 D-31 D-32 | Summary, publish, receipt, validation, and conflict response behavior. | 01, 02, 03 | COVERED | Plans assert existing response/conflict bodies and no summary/publish on failure. |

<tasks>

<task type="auto">
  <name>Task 1: Add red grouped replacement success contracts</name>
  <files>tests/integration/meals-api.test.ts</files>
  <read_first>
    @tests/integration/meals-api.test.ts
    @server/routes/meals.ts
    @server/services/food-logging.ts
    @server/services/meal-transactions.ts
    @.planning/phases/75-grouped-meal-direct-crud-contract/75-CONTEXT.md
    @.planning/phases/75-grouped-meal-direct-crud-contract/75-PATTERNS.md
  </read_first>
  <action>Add failing Fastify/SQLite integration tests near the existing `PATCH /api/meals/:id` tests. Cover valid grouped `items[]` replacement for single-to-many addition, many-to-one deletion-by-omission, and many-to-many update/reorder. Request bodies must include `expectedMealRevisionId` and `items` only, with public rows shaped as `{ name, position, calories, protein, carbs, fat }` per D-02, D-04, D-07, D-08, D-11, D-15, D-17, and D-18. Assert 200 responses keep the existing minimum direct PATCH shape per D-05: `affectedDate`, `summaryOutcome`, optional `dailySummary`, and aggregate `meal`; do not assert `meal.items` because D-06 leaves item-detail response expansion outside Phase 75. Assert aggregate `meal.itemCount`, summed calories/protein/carbs/fat, changed `mealRevisionId`, unchanged meal id, and `meal_mutation` publish envelope on successful replacement per D-26, D-27, and D-28.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/meals-api.test.ts (expected nonzero until Plan 02; failures must be limited to the newly added grouped replacement success assertions)</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - New tests fail against the current grouped-lock route because valid `items[]` replacement is not yet implemented.
    - Existing scalar PATCH, DELETE, auth, image, and stale-revision tests are not edited except for local helper reuse.
    - No production files are modified in this plan.
  </acceptance_criteria>
  <done>Integration coverage describes grouped add, update, and delete-by-omission as full-list replacement and is ready for Plan 02 to make green.</done>
</task>

<task type="auto">
  <name>Task 2: Add red invalid grouped body contracts</name>
  <files>tests/integration/meals-api.test.ts</files>
  <read_first>
    @tests/integration/meals-api.test.ts
    @server/routes/meals.ts
    @.planning/phases/75-grouped-meal-direct-crud-contract/75-CONTEXT.md
    @.planning/phases/75-grouped-meal-direct-crud-contract/75-VALIDATION.md
  </read_first>
  <action>Add failing invalid-body integration coverage for grouped writes per D-03, D-09, D-10, D-19, D-20, D-21, D-23, D-24, and D-30. Cases must include: `items: []`; `items[]` plus scalar fields such as `foodName`, calories, macros, or `imageAssetId`; item object with public `foodName`; item object with nested `nutrition`; item object with an extra key such as `serving`; blank `name`; non-finite or negative nutrition; missing required nutrition; and `position` not equal to the zero-based array index. For these validation failures assert status 400 with exactly `{ error: "Invalid meal update" }`, no `summaryOutcome`, no `dailySummary`, zero summary recompute calls, zero realtime publish calls, and the meal row remains at its original revision. Keep scalar-on-grouped `MEAL_REQUIRES_GROUPED_UPDATE` coverage intact for unsupported scalar payloads per D-03 and D-32.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/meals-api.test.ts (expected nonzero until Plan 02; failures must be limited to newly added grouped contract assertions)</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Invalid grouped write assertions expect only simple 400 bodies, not field-level validation detail DTOs.
    - Tests explicitly reject tolerant read-path shapes for grouped writes.
    - Tests do not create UI, schema, chat receipt, or item media requirements.
  </acceptance_criteria>
  <done>Invalid grouped write behavior is pinned before route implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser client -> `PATCH /api/meals/:id` | Untrusted JSON body crosses into server mutation route. |
| Signed guest session -> service `deviceId` | Route must derive ownership from cookies, not raw caller identifiers. |
| Route -> summary/realtime side effects | Side effects must happen only after committed mutation. |
| Test artifacts -> planning proof | Evidence must stay metadata-only and not include private raw payloads beyond synthetic test values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-75-01 | Tampering | `parseMealUpdateBody()` grouped branch | mitigate | Red tests require strict rejection for mixed shapes, aliases, nested nutrition, extra item keys, empty lists, invalid numbers, and bad positions. |
| T-75-02 | Elevation of Privilege | `PATCH /api/meals/:id` ownership | mitigate | Keep existing signed guest-session route setup in read context; do not plan query/header `deviceId` authority. |
| T-75-03 | Tampering | stale revision writes | mitigate | Tests require missing/stale expected revision conflicts to keep existing 409 body and no mutation side effects. |
| T-75-04 | Information Disclosure | realtime summary publish | mitigate | Success tests assert the existing `meal_mutation` envelope omits `summaryOutcome`, `mealId`, and `mealRevisionId`. |
| T-75-05 | Information Disclosure | proof artifacts | mitigate | Use repo-native tests with synthetic meal names only; do not add harness artifacts, raw prompts, provider bodies, session material, image data, or database snapshots. |
| T-75-SC | Tampering | package-manager installs | mitigate | No package installs are planned. If execution attempts an npm/pip/cargo install, stop and require package legitimacy audit before continuing. |
</threat_model>

<verification>
Plan 01 is complete when the new integration contracts exist, `yarn tsc --noEmit` passes, and the direct integration test command fails only for the newly added grouped PATCH assertions that Plan 02 is expected to satisfy.
</verification>

<success_criteria>
- Red tests exist for grouped replacement success paths, invalid grouped bodies, response shape, and side-effect suppression.
- Every assertion uses real Fastify `app.inject()` and the existing `:memory:` SQLite app fixture.
- No production source, schema, client UI, package, or chat persistence files are modified.
</success_criteria>

<output>
Create `.planning/phases/75-grouped-meal-direct-crud-contract/75-01-SUMMARY.md` when done.
</output>
