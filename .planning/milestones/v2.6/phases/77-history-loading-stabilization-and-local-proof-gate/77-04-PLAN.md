---
phase: 77-history-loading-stabilization-and-local-proof-gate
plan: 04
type: execute
wave: 4
depends_on:
  - 77-03
files_modified:
  - client/src/components/HistoryScreen.tsx
  - tests/unit/history-screen-contract.test.ts
  - tests/harness/scenarios/77-history-loading-visual.mjs
  - tests/harness/artifacts/77-history-loading/latest/manifest.json
  - tests/harness/artifacts/77-history-loading/latest/history-cold-week-pending-mobile-390x844.png
  - tests/harness/artifacts/77-history-loading/latest/history-cold-week-loaded-mobile-390x844.png
  - tests/harness/artifacts/77-history-loading/latest/history-fast-date-click-mobile-390x844.png
  - .planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-HUMAN-UAT.md
  - .planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-VERIFICATION.md
autonomous: true
gap_closure: true
requirements:
  - HIST-UX-01
  - PROOF-01
  - PROOF-02
  - PROOF-03
must_haves:
  truths:
    - "GAP-77-UAT-01: Week navigation and date-button clicks do not flash `同步這天紀錄中...` for fast selected-day reloads."
    - "GAP-77-UAT-01: The History meal-list slot stays visually stable during fast week/date changes, without a quick text-card shake."
    - "GAP-77-UAT-01: Longer cold selected-day loads may still show the inline pending copy in the existing meal-list slot."
    - "HIST-UX-01: No page-level `載入這週紀錄中...` loading card returns during week switches."
    - "PROOF-02/PROOF-03: Gap-closure evidence remains local, synthetic, metadata-only, and does not authorize staging or main promotion."
  artifacts:
    - path: "client/src/components/HistoryScreen.tsx"
      provides: "Delayed selected-day inline pending copy for fast reload flicker suppression"
      contains: "DAY_PENDING_COPY_DELAY_MS"
    - path: "tests/unit/history-screen-contract.test.ts"
      provides: "Source contract for delayed pending-copy behavior and stable snapshot authority"
      contains: "DAY_PENDING_COPY_DELAY_MS"
    - path: "tests/harness/scenarios/77-history-loading-visual.mjs"
      provides: "Synthetic mobile proof for long cold loading and fast date-click anti-flicker behavior"
      contains: "history-fast-date-click-mobile-390x844.png"
    - path: "tests/harness/artifacts/77-history-loading/latest/manifest.json"
      provides: "Metadata-only visual proof manifest including fast-reload anti-flicker assertions"
      contains: "metadata-only"
    - path: ".planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-HUMAN-UAT.md"
      provides: "Gap closure status for GAP-77-UAT-01"
      contains: "GAP-77-UAT-01"
    - path: ".planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-VERIFICATION.md"
      provides: "Updated phase verification status after gap closure"
      contains: "promotion_authorized: false"
  key_links:
    - from: "client/src/components/HistoryScreen.tsx"
      to: "tests/unit/history-screen-contract.test.ts"
      via: "source assertions for delayed pending copy"
      pattern: "DAY_PENDING_COPY_DELAY_MS"
    - from: "client/src/components/HistoryScreen.tsx"
      to: "/api/history/days/:date"
      via: "fast selected-day reload resolves before pending copy becomes visible"
      pattern: "loadingDay"
    - from: "tests/harness/scenarios/77-history-loading-visual.mjs"
      to: "tests/harness/artifacts/77-history-loading/latest/manifest.json"
      via: "fast date-click sampling and screenshot metadata"
      pattern: "fastDateClick"
---

<objective>
Close GAP-77-UAT-01 by suppressing fast transient History selected-day pending-copy flicker while preserving the existing longer cold-load inline pending state.

Purpose: Phase 77 already removed the page-level week loading card and made rows/detail/edit snapshot-backed, but human UAT found a remaining quick visual shake when week/date clicks briefly render `同步這天紀錄中...`. This plan adds a narrow delayed-pending contract, implements it in `HistoryScreen`, strengthens synthetic local proof, and updates only Phase 77 gap-closure metadata.
Output: Updated History source contract, `HistoryScreen` pending behavior, regenerated local visual proof artifacts, and Phase 77 UAT/verification status for GAP-77-UAT-01.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-CONTEXT.md
@.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-RESEARCH.md
@.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-PATTERNS.md
@.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-HUMAN-UAT.md
@.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-VERIFICATION.md
@.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-01-PLAN.md
@.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-02-PLAN.md
@.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-03-PLAN.md
@client/src/components/HistoryScreen.tsx
@tests/unit/history-screen-contract.test.ts
@tests/harness/scenarios/77-history-loading-visual.mjs
</context>

## Artifacts this phase produces

| Artifact | Contract |
|---|---|
| `client/src/components/HistoryScreen.tsx` constant `DAY_PENDING_COPY_DELAY_MS` | Defines the minimum selected-day pending duration before `同步這天紀錄中...` may appear; use 180-250ms and keep it local to History. |
| `client/src/components/HistoryScreen.tsx` state `delayedInlineDayPending` | Tracks whether the selected-day pending copy has lasted long enough to render; it resets immediately when the snapshot resolves, selected date changes, or `dayError` appears. |
| `client/src/components/HistoryScreen.tsx` ref `inlineDayPendingTimerRef` | Stores and clears the pending-delay timer so stale timers cannot reveal copy after a fast reload completes. |
| `client/src/components/HistoryScreen.tsx` refined `showInlineDayPending` | Renders `同步這天紀錄中...` only when `selectedDaySnapshotPending`, `loadingDay`, no `dayError`, and `delayedInlineDayPending` are all true. |
| `tests/unit/history-screen-contract.test.ts` source contract `suppresses fast selected-day pending copy while preserving longer cold-load copy` | Requires the delay constant, timer cleanup, delayed boolean, and no immediate `selectedSnapshot === null` pending-copy branch. |
| `tests/harness/scenarios/77-history-loading-visual.mjs` helper `collectFastPendingCopySamples()` | Samples the built UI across animation frames after a fast date click and fails if `同步這天紀錄中...` appears before the fast snapshot resolves. |
| `tests/harness/scenarios/77-history-loading-visual.mjs` manifest assertion `fastDateClick.noTransientInlinePendingCopy` | Records the anti-flicker proof as metadata only. |
| `tests/harness/artifacts/77-history-loading/latest/history-fast-date-click-mobile-390x844.png` | Synthetic mobile screenshot after a fast date click resolves without transient pending-copy flicker. |
| `.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-HUMAN-UAT.md` | Records GAP-77-UAT-01 as closed by local gap-closure proof, while leaving promotion confirmation separate. |
| `.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-VERIFICATION.md` | Updates status from `gaps_found` only after targeted commands, visual proof, TypeScript, build, and local release gate pass. |

## Multi-Source Coverage Audit

| Source | ID | Item | Covered By | Status |
|---|---|---|---|---|
| GOAL | Phase 77 goal | History week switching avoids disruptive loading jumps and closes with focused local proof. | Tasks 1, 2, 3 | COVERED |
| GAP | GAP-77-UAT-01 | Suppress fast History pending-copy flicker on week/date clicks. | Tasks 1, 2, 3 | COVERED |
| REQ | HIST-UX-01 | History week switching keeps a stable layout during cold pending loads and avoids disruptive loading jumps. | Tasks 1, 2, 3 | COVERED |
| REQ | PROOF-01 | Targeted local proof covers History loading behavior. | Task 3 | COVERED |
| REQ | PROOF-02 | Generated evidence remains metadata-only. | Task 3 | COVERED |
| REQ | PROOF-03 | Local closure runs changed-path checks and final release gate before any promotion request. | Task 3 | COVERED |
| RESEARCH | R-HISTORY-PENDING | Keep target context visible, avoid page-level week loading, use inline pending only when helpful. | Tasks 1, 2 | COVERED |
| RESEARCH | R-VISUAL | Use existing local CDP/static-server harness pattern with synthetic evidence and no new dependencies. | Task 3 | COVERED |
| CONTEXT | D-01 through D-08 | Cold week/date target context, inline placeholders, no skeleton/stale rows, trend aggregates display only. | Tasks 1, 2 | COVERED |
| CONTEXT | D-09 through D-15 | Snapshot-backed Day Detail, meal rows, edit activation, empty, pending, and error behavior. | Tasks 1, 2 | COVERED |
| CONTEXT | D-25 through D-33 | Source/unit/browser proof, metadata-only evidence, release gate, and no-promotion boundary. | Task 3 | COVERED |
| CONTEXT | Deferred scope | Monthly goals, hydration, broad motion polish, coaching copy, infrastructure cleanup, staging promotion, and main promotion remain out of scope. | Tasks 1, 2, 3 | COVERED |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add anti-flicker source contract for delayed selected-day pending copy</name>
  <files>tests/unit/history-screen-contract.test.ts</files>
  <read_first>
    @AGENTS.md
    @.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-HUMAN-UAT.md
    @.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-VERIFICATION.md
    @.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-PATTERNS.md
    @client/src/components/HistoryScreen.tsx
    @tests/unit/history-screen-contract.test.ts
  </read_first>
  <behavior>
    - Fast selected-day reloads that resolve before `DAY_PENDING_COPY_DELAY_MS` do not render `同步這天紀錄中...`.
    - Longer cold selected-day loads still render `同步這天紀錄中...` inline after the delay.
    - Pending-copy timers are cleared when the snapshot resolves, `dayError` appears, or the selected date changes.
    - Existing Plan 77 snapshot authority remains: no prior rows, no skeleton rows, no trend-only empty/detail/edit activation, and no page-level `載入這週紀錄中...`.
  </behavior>
  <action>Add a focused source-contract test to `tests/unit/history-screen-contract.test.ts` using the existing `source` string and `escapedPattern()` style. Name it around suppressing fast selected-day pending copy while preserving longer cold-load copy. Require the source to contain `DAY_PENDING_COPY_DELAY_MS`, `delayedInlineDayPending`, `setDelayedInlineDayPending`, `inlineDayPendingTimerRef`, `window.setTimeout`, and `window.clearTimeout`. Require `showInlineDayPending` to depend on `delayedInlineDayPending`, `loadingDay`, `selectedDaySnapshotPending`, and `!dayError`. Reject an immediate branch where `showInlineDayPending` is assigned only from `selectedSnapshot === null`, `selectedDaySnapshotPending`, or `hasSelectedDaySnapshot` without the delayed boolean. Preserve existing assertions that reject `載入這週紀錄中...`, prior snapshot fallbacks, skeleton rows, trend-only empty state, broad cache clears, `activeScreen` gates, and `secondaryScreen` gates. Do not introduce React Testing Library, Jest, Vitest, or package installs.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/history-screen-contract.test.ts (expected nonzero until Task 2; failures must be limited to the new delayed pending-copy contract)</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The new source contract fails against the current immediate `showInlineDayPending` implementation and does not fail unrelated History contracts.
    - The test explicitly protects both sides of the UX: fast reloads suppress transient copy; longer cold loads can still show inline copy.
    - No existing Phase 77 snapshot-backed row/detail/edit contracts are weakened or deleted.
  </acceptance_criteria>
  <done>GAP-77-UAT-01 has an executable source contract before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Delay transient History selected-day pending copy without reopening snapshot authority</name>
  <files>client/src/components/HistoryScreen.tsx</files>
  <read_first>
    @AGENTS.md
    @.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-CONTEXT.md
    @.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-HUMAN-UAT.md
    @client/src/components/HistoryScreen.tsx
    @tests/unit/history-screen-contract.test.ts
    @tests/unit/history-week.test.ts
    @tests/unit/meal-edit-refresh.test.ts
  </read_first>
  <action>In `HistoryScreen.tsx`, implement the delayed selected-day pending-copy behavior for GAP-77-UAT-01. Add `useRef` to the React import if needed. Add local constant `DAY_PENDING_COPY_DELAY_MS` near the other History helpers; use a value between 180 and 250ms so a fast selected-day reload does not flash copy for a single frame. Add `delayedInlineDayPending` state and `inlineDayPendingTimerRef`. Add a `useEffect` keyed by the selected-day pending condition that starts a timer only when `loadingDay` is true, `selectedSnapshot === null`, and `dayError` is null; after the delay, set `delayedInlineDayPending` true. Clear the timer and set the delayed boolean false immediately when the snapshot resolves, the selected date changes, loading ends before the delay, or `dayError` appears. Refine `showInlineDayPending` so `同步這天紀錄中...` renders only when `selectedDaySnapshotPending`, `loadingDay`, `!dayError`, and `delayedInlineDayPending` are all true. Keep `TimelinePanel` mounted and keep the meal count as `--` while `snapshot === null`; do not restore prior rows, disabled rows, skeleton rows, trend-only empty state, trend-only Day Detail, or the page-level `載入這週紀錄中...` card. Preserve `confirmedEmptyDay`, `openConfirmedEmptyDayDetail`, `TimelineRows` from `snapshot.meals`, and the existing scoped `lastMealMutation` refresh/invalidation effect. Do not edit backend routes, services, monthly-goal scope, hydration scope, or promotion workflow.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/history-screen-contract.test.ts tests/unit/history-week.test.ts tests/unit/meal-edit-refresh.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source contracts pass and prove `showInlineDayPending` is delayed rather than directly derived from `selectedSnapshot === null`.
    - A selected-day snapshot that resolves before the delay cannot reveal `同步這天紀錄中...`.
    - A selected-day snapshot that remains pending beyond the delay still shows inline `同步這天紀錄中...`.
    - No stale rows, skeleton rows, trend-only empty state, trend-only Day Detail, or page-level week loading card is introduced.
  </acceptance_criteria>
  <done>History date/week clicks no longer flash transient pending copy on fast selected-day reloads while longer cold loads remain understandable.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate fast-reload visual proof and close the gap metadata</name>
  <files>tests/harness/scenarios/77-history-loading-visual.mjs, tests/harness/artifacts/77-history-loading/latest/manifest.json, tests/harness/artifacts/77-history-loading/latest/history-cold-week-pending-mobile-390x844.png, tests/harness/artifacts/77-history-loading/latest/history-cold-week-loaded-mobile-390x844.png, tests/harness/artifacts/77-history-loading/latest/history-fast-date-click-mobile-390x844.png, .planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-HUMAN-UAT.md, .planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-VERIFICATION.md</files>
  <read_first>
    @AGENTS.md
    @.planning/REQUIREMENTS.md
    @.planning/ROADMAP.md
    @.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-HUMAN-UAT.md
    @.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-VERIFICATION.md
    @tests/harness/scenarios/77-history-loading-visual.mjs
    @tests/unit/history-screen-contract.test.ts
    @docs/adr/0001-metadata-only-llm-failure-localization.md
  </read_first>
  <action>Extend `tests/harness/scenarios/77-history-loading-visual.mjs` to prove the actual GAP-77-UAT-01 fast-reload case in addition to the existing delayed cold-week case. Keep the existing local CDP/static-server pattern, synthetic mocks, safe output directory guard, and no-package approach. Add a fast date-click path with deterministic mocked data where the selected-day response resolves faster than `DAY_PENDING_COPY_DELAY_MS`; add helper `collectFastPendingCopySamples()` or an equivalent in-script sampler that records body text across animation frames for at least 250ms after the date click. Fail the run if any sample includes `同步這天紀錄中...` before the fast snapshot resolves, if target date context disappears, if current-week stale meal labels appear under the selected target date, if `載入這週紀錄中...` appears, if horizontal overflow appears, or if unsafe/unmocked/external API calls occur. Capture `history-fast-date-click-mobile-390x844.png` after the fast reload settles. Regenerate the existing pending and loaded screenshots plus `manifest.json`; do not hand-edit generated screenshots. Ensure the manifest records command/status/viewport/assertion names, relative screenshot paths, deterministic mock categories, `fastDateClick.noTransientInlinePendingCopy`, metadata-only privacy text, and local-only no-promotion policy. Then update `77-HUMAN-UAT.md` to mark GAP-77-UAT-01 closed by local gap-closure proof and update `77-VERIFICATION.md` from `gaps_found` to passed only after all commands are green. Keep evidence metadata-only: do not paste screenshot bytes, raw prompts, user-authored text beyond the already-recorded gap summary, assistant final text, raw tool/provider payloads, image data, real local DB rows, cookies, session material, private logs, device identifiers, or database snapshots. Keep promotion language explicit: local proof and `yarn release:check` do not authorize staging or main promotion; no push, merge, deploy, Railway smoke, staging promotion, or main promotion is performed.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/history-screen-contract.test.ts tests/unit/history-week.test.ts tests/unit/meal-edit-refresh.test.ts</automated>
    <automated>yarn tsc --noEmit</automated>
    <automated>yarn build</automated>
    <automated>node tests/harness/scenarios/77-history-loading-visual.mjs</automated>
    <automated>node -e "const fs=require('fs'); const p='tests/harness/artifacts/77-history-loading/latest/manifest.json'; const m=JSON.parse(fs.readFileSync(p,'utf8')); const s=JSON.stringify(m); if (!s.includes('fastDateClick')) throw new Error('missing fastDateClick proof'); if (!s.includes('noTransientInlinePendingCopy')) throw new Error('missing anti-flicker assertion'); if (!s.includes('metadata-only')) throw new Error('missing metadata-only policy'); const forbidden=['OPENAI_API_KEY','provider body','tool payload','database snapshot','cookie','session material']; for (const token of forbidden) if (s.includes(token)) throw new Error('forbidden token in manifest: '+token);"</automated>
    <automated>yarn release:check</automated>
  </verify>
  <acceptance_criteria>
    - Visual harness fails if fast date-click samples ever observe transient `同步這天紀錄中...` before the fast snapshot resolves.
    - `manifest.json` includes the fast anti-flicker assertion and remains metadata-only.
    - `77-HUMAN-UAT.md` records GAP-77-UAT-01 as closed and preserves no-promotion confirmation as a workflow boundary.
    - `77-VERIFICATION.md` maps HIST-UX-01, PROOF-01, PROOF-02, and PROOF-03 to the gap-closure commands and no-promotion policy.
    - `yarn release:check` is green before verification status is changed to passed.
    - No package installs, backend changes, monthly goals, hydration, broad motion polish, coaching copy, infrastructure cleanup, staging promotion, or main promotion occur.
  </acceptance_criteria>
  <done>GAP-77-UAT-01 is closed with local source, build, browser, metadata, and release-gate proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `/api/history/trends` -> History UI | Trend facts may support aggregate display only; they must not unlock rows, edit identity, empty state, or Day Detail. |
| `/api/history/days/:date` -> selected-day slot | Snapshot resolution controls whether rows, empty state, and pending copy can render. |
| Fast pending UI -> user action | A one-frame pending card can create action ambiguity and perceived layout instability during week/date clicks. |
| Visual harness -> generated artifacts | Browser proof must stay synthetic and metadata-only despite producing screenshots and manifests. |
| Local proof -> release workflow | Green local checks and `yarn release:check` do not authorize push, deploy, staging promotion, or main promotion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-77-04-01 | Tampering | `HistoryScreen.tsx` selected-day facts | mitigate | Preserve snapshot-backed `TimelineRows`, `confirmedEmptyDay`, `openConfirmedEmptyDayDetail`, and `buildHistoryMealEditPayload`; tests reject trend-only or stale-row authority. |
| T-77-04-02 | Repudiation | Fast week/date click UI | mitigate | Add source and visual evidence that fast selected-day reloads do not flash transient pending copy, while longer cold loads still show clear inline pending state. |
| T-77-04-03 | Denial of Service / Integrity | Pending-copy timer | mitigate | Clear `inlineDayPendingTimerRef` on snapshot resolution, date changes, loading completion before delay, and errors so stale timers cannot mutate visible state later. |
| T-77-04-04 | Information Disclosure | `tests/harness/artifacts/77-history-loading/latest/*` and `77-VERIFICATION.md` | mitigate | Use synthetic mocks, metadata-only manifest fields, relative screenshot paths, and forbidden-token checks; do not embed screenshots or raw private payloads in planning docs. |
| T-77-04-05 | Repudiation | Local closure versus production authorization | mitigate | Keep `promotion_authorized: false`, exact no-promotion language, and acceptance criteria forbidding push, merge, deploy, Railway smoke, staging promotion, and main promotion. |
| T-77-04-SC | Tampering | package-manager installs | mitigate | No package installs are planned. If execution attempts npm/pip/cargo install, stop and require a package legitimacy audit first. |
</threat_model>

<verification>
Run these checks for the full gap-closure plan:

- `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/history-screen-contract.test.ts tests/unit/history-week.test.ts tests/unit/meal-edit-refresh.test.ts`
- `yarn tsc --noEmit`
- `yarn build`
- `node tests/harness/scenarios/77-history-loading-visual.mjs`
- `node -e "const fs=require('fs'); const p='tests/harness/artifacts/77-history-loading/latest/manifest.json'; const m=JSON.parse(fs.readFileSync(p,'utf8')); const s=JSON.stringify(m); if (!s.includes('fastDateClick')) throw new Error('missing fastDateClick proof'); if (!s.includes('noTransientInlinePendingCopy')) throw new Error('missing anti-flicker assertion'); if (!s.includes('metadata-only')) throw new Error('missing metadata-only policy'); const forbidden=['OPENAI_API_KEY','provider body','tool payload','database snapshot','cookie','session material']; for (const token of forbidden) if (s.includes(token)) throw new Error('forbidden token in manifest: '+token);"`
- `yarn release:check`
</verification>

<success_criteria>
- Fast History week/date clicks do not show a one-frame `同步這天紀錄中...` card.
- Longer cold History selected-day loads still show inline `同步這天紀錄中...` after the delay.
- Snapshot-backed row, Meal Edit, confirmed empty, Day Detail, and error behavior remains intact.
- The Phase 77 visual harness proves both delayed cold-load and fast-reload anti-flicker behavior with synthetic local data.
- Gap-closure verification remains metadata-only, local-only, and no-promotion.
</success_criteria>

<output>
Create `.planning/phases/77-history-loading-stabilization-and-local-proof-gate/77-04-SUMMARY.md` when done.
</output>
