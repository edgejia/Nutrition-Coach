---
quick_id: 260517-2a8
slug: fix-phase-59-d-04-empty-day-summary-regr
date: 2026-05-16
status: planned
validation: enabled
---

# Fix Phase 59 D-04 Empty-Day Summary Regression

## Goal

Empty-day summary/history replies must preserve deterministic summary semantics like `今天已記錄 0 餐，共 0 kcal。` and must not be converted into the no-mutation meal logging fallback.

## Scope

- Keep Phase 59 closed; do not edit Phase 59 roadmap artifacts.
- Narrowly adjust route/orchestrator normalization so renderer-owned summary/history replies are not downgraded by the no-mutation logging guard.
- Preserve non-summary false-log blocking, fake-meal blocking, aggregate mismatch blocking, daily-total-as-single-meal blocking, and SSE pre-final buffering.
- Update JSON and live SSE empty-day route coverage to assert deterministic 0-meal summary text.

## Plan

1. Add route integration regressions for JSON and live SSE empty-day summary contexts.
2. Change `normalizeRouteFinalReply()` so composed or already renderer-owned summary/history replies bypass the no-mutation meal logging guard after the shared renderer has accepted/rewritten the text.
3. Keep existing guard tests for non-summary and persisted-meal summary claims unchanged.
4. Run targeted renderer/orchestrator/route tests, TypeScript, integration, and `yarn release:check`.

## Plan Check

PASS from `gsd-plan-checker`: no missing plan requirements. The checker specifically required preserving `result.finalReplySource === "renderer"` cases, not only replies composed inside `normalizeRouteFinalReply()`.
