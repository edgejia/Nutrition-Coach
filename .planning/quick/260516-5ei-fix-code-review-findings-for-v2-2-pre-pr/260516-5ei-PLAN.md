---
phase: quick-260516-5ei-fix-code-review-findings-for-v2-2-pre-pr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/chat.ts
  - server/orchestrator/index.ts
  - server/orchestrator/system-prompt.ts
  - server/orchestrator/tools.ts
  - tests/unit/orchestrator.test.ts
  - tests/unit/system-prompt.test.ts
  - tests/unit/tools.test.ts
  - tests/integration/chat-api.test.ts
  - tests/integration/verification-image.test.ts
  - tests/harness/scenarios/image-log-failure.ts
  - tests/harness/scenarios/image-log.ts
autonomous: true
requirements:
  - QUICK-260516-5EI
must_haves:
  truths:
    - "When `didLogMeal` and `didMutateMeal` are both false, model-owned reply text and live SSE chunks cannot claim 已記錄 or 完成記錄."
    - "Reachable prompt copy no longer tells the model to use stale `headline` wording or `保守估算` wording."
    - "Grouped `log_food` calls suppress transient `missing_quantity` when top-level serving metadata contains Chinese serving quantities such as 一份, 半碗, or 兩份, even when source text carries no quantity."
    - "The `image-log-failure` harness asserts live SSE chunk text does not claim logging on failed/no-mutation paths."
    - "The `image-log` harness fails on malformed or empty chunk payloads and proves the final `loggedMeal` receipt shape is non-empty and usable."
    - "Targeted unit/integration tests, `image-log-failure`, `image-log`, `protein-trust`, `yarn test:unit`, `yarn tsc --noEmit`, and `yarn release:check` pass without push, deploy, merge, or promotion."
  artifacts:
    - path: "server/routes/chat.ts"
      provides: "SSE and non-streaming route guard that prevents false logged-copy when no meal mutation occurred"
      contains: "handleStreamingReply"
    - path: "server/orchestrator/index.ts"
      provides: "No-mutation reply guard and local choice recovery rules"
      contains: "detectHallucinatedChoiceFollowUp"
    - path: "server/orchestrator/system-prompt.ts"
      provides: "Current reachable meal logging prompt copy"
      contains: "成功 log_food 回覆契約"
    - path: "server/orchestrator/tools.ts"
      provides: "Grouped serving metadata normalization for quantity evidence"
      contains: "hasGroupedQuantityEvidence"
    - path: "tests/unit/orchestrator.test.ts"
      provides: "Unit regressions for no-mutation replies that must not claim logging"
      contains: "didLogMeal"
    - path: "tests/unit/system-prompt.test.ts"
      provides: "Prompt-copy regression proving stale reachable terms are removed or rewritten"
      contains: "successful log_food reply contract"
    - path: "tests/unit/tools.test.ts"
      provides: "Grouped top-level Chinese serving metadata regression with quantity-free source text"
      contains: "missing_quantity"
    - path: "tests/harness/scenarios/image-log-failure.ts"
      provides: "Live SSE failure-path proof that failed/no-mutation image paths do not claim logging"
      contains: "image-log-failure"
    - path: "tests/harness/scenarios/image-log.ts"
      provides: "Image logging proof that rejects malformed/empty chunks and asserts non-empty receipt shape"
      contains: "image-log"
  key_links:
    - from: "server/routes/chat.ts"
      to: "server/services/chat.ts"
      via: "finalizeAssistantReply persists the same guarded text that SSE emits"
      pattern: "finalizeAssistantReply"
    - from: "server/routes/chat.ts"
      to: "tests/harness/scenarios/image-log-failure.ts"
      via: "real `/api/chat` SSE route exercised through createScenarioApp"
      pattern: "runSubScenario"
    - from: "server/orchestrator/tools.ts"
      to: "server/services/food-logging.ts"
      via: "executeTool log_food -> logGroupedMeal"
      pattern: "logGroupedMeal"
    - from: "tests/harness/scenarios/image-log.ts"
      to: "server/routes/chat.ts"
      via: "real multipart image route with parsed SSE chunks and done payload"
      pattern: "readStreamUntilEvent"
---

<objective>
Fix the v2.2 pre-PR code-review findings for the pre-promotion UAT patch.

Purpose: Close the false logging-copy, stale prompt-copy, serving-metadata, and regression-proof gaps without push, deploy, merge, or promotion.
Output: One surgical patch with production guards, focused tests, harness proof, and release gate verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/quick/260516-4a1-v2-2-pre-promotion-uat-patch-based-on-no/260516-4a1-SUMMARY.md
@server/routes/chat.ts
@server/orchestrator/index.ts
@server/orchestrator/system-prompt.ts
@server/orchestrator/tools.ts
@tests/unit/orchestrator.test.ts
@tests/unit/system-prompt.test.ts
@tests/unit/tools.test.ts
@tests/integration/chat-api.test.ts
@tests/integration/verification-image.test.ts
@tests/harness/scenarios/image-log-failure.ts
@tests/harness/scenarios/image-log.ts
@tests/harness/scenarios/protein-trust.ts

<interfaces>
From `server/routes/chat.ts`: `handleStreamingReply(...)` streams `event: chunk` frames and persists the final assistant reply through `finalizeAssistantReply`. Any no-mutation false-log guard must protect both live chunks and persisted history. The non-streaming branch also writes one `chunk` and one `done` event after `orchestrator.handleMessage(...)`.

From `server/orchestrator/index.ts`: `detectHallucinatedChoiceFollowUp(userMessage, recentMessages)` returns `HALLUCINATED_CHOICE_RECOVERY_REPLY`; that local recovery reply contains 完成記錄 language and must require successful meal-mutation evidence. `buildImageLoggedReply(...)` remains valid only when a log mutation actually succeeded.

From `server/orchestrator/system-prompt.ts`: stale reachable source terms currently include `headline` and `保守估算` in meal logging guidance. Rewrite these to current product language without changing the core contract that replies include 已記錄, meal name, kcal, trusted protein, date when non-today, and conditional uncertainty guidance.

From `server/orchestrator/tools.ts`: `hasGroupedQuantityEvidence(args)` currently recognizes numeric top-level grouped quantity fields. It must also recognize Chinese serving strings in grouped top-level `amount`, `unit`, or `serving_size` such as 一份, 半碗, 兩份 without depending on `context.currentUserMessage`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard no-mutation replies from false logging claims and rewrite stale prompt copy</name>
  <files>server/routes/chat.ts, server/orchestrator/index.ts, server/orchestrator/system-prompt.ts, tests/unit/orchestrator.test.ts, tests/unit/system-prompt.test.ts, tests/integration/chat-api.test.ts</files>
  <behavior>
    - Test 1: A model reply containing 已記錄 or 完成記錄 with `didLogMeal: false` and `didMutateMeal: false` is replaced by safe non-logging fallback copy before any SSE `chunk` is emitted and before assistant history is persisted.
    - Test 2: Local hallucinated choice recovery still works only when prior history proves a successful meal mutation; failed/no-tool histories do not return 完成記錄 copy.
    - Test 3: `buildSystemPrompt(...)` no longer contains `headline` or `保守估算`, while still describing trusted protein, successful logging reply boundaries, conditional uncertainty, and forbidden internal tool names.
  </behavior>
  <action>Add failing assertions first. Cover the route boundary in `tests/integration/chat-api.test.ts` or the closest existing SSE integration helper so live `chunk` text and persisted assistant history are both checked. Then update `server/routes/chat.ts` so no-mutation model-owned replies are buffered or otherwise guarded until the route knows they do not claim logging; apply the same guard to non-streaming model replies before writing `chunk` and before `finalizeAssistantReply`. Preserve mutation-success renderer receipts. In `server/orchestrator/index.ts`, keep `detectHallucinatedChoiceFollowUp` tied to explicit successful mutation evidence and add any missing unit coverage for model-content no-mutation replies. Rewrite stale prompt copy in `server/orchestrator/system-prompt.ts` to current terms such as 可信蛋白 and cautious estimate language; do not weaken the successful log reply contract or internal-name prohibition.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/orchestrator.test.ts tests/unit/system-prompt.test.ts tests/integration/chat-api.test.ts</automated>
    <automated>! rg -n "headline|保守估算" server/orchestrator/system-prompt.ts</automated>
  </verify>
  <done>No route-visible or persisted no-mutation model reply claims 已記錄/完成記錄, successful mutation receipts still use the renderer-owned receipt path, and reachable system prompt text no longer contains stale `headline` or `保守估算` terms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Treat grouped Chinese serving metadata as quantity evidence without source text</name>
  <files>server/orchestrator/tools.ts, tests/unit/tools.test.ts</files>
  <behavior>
    - Test 1: A grouped `log_food` call with `items[]`, top-level `amount: "一份"`, and `currentUserMessage` containing no quantity-bearing text does not set `loggedMeal.quantityUncertaintyReason`.
    - Test 2: The same suppression works for top-level `serving_size: "半碗"` and `unit`/`amount` combinations such as `amount: "兩份"`.
    - Test 3: Existing `missing_quantity` regressions still mark quantity-free grouped text logs as `missing_quantity` when neither item-level nor top-level serving evidence exists.
  </behavior>
  <action>Add isolated regression coverage in `tests/unit/tools.test.ts` near the existing missing quantity tests. The fixture must use top-level grouped serving metadata and a quantity-free `currentUserMessage`; do not rely on item names or source text containing numbers/serving words. Update `server/orchestrator/tools.ts` so grouped top-level `amount`, `unit`, and `serving_size` use the existing Chinese quantity vocabulary from `hasQuantityLikeNumberInText` or a shared helper. Keep `items[]` authoritative for persisted food names and macros; the top-level fields only suppress false `missing_quantity` metadata.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/tools.test.ts</automated>
  </verify>
  <done>Chinese serving strings in grouped top-level metadata suppress `missing_quantity` without using source text, while truly quantity-free grouped logs still receive the uncertainty marker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Strengthen harness proof for failure chunks, malformed chunks, and receipt shape</name>
  <files>tests/harness/scenarios/image-log-failure.ts, tests/harness/scenarios/image-log.ts, tests/integration/verification-image.test.ts</files>
  <behavior>
    - Test 1: `image-log-failure` parses live SSE chunk text for failed/no-mutation sub-scenarios and fails if any chunk claims 已記錄 or 完成記錄.
    - Test 2: `image-log` fails when a `chunk` event payload is malformed, missing `token`, or produces an empty assembled reply.
    - Test 3: `image-log` asserts `done.loggedMeal` exists when `didLogMeal: true` and includes non-empty `foodName`, positive `itemCount`, finite macro fields, a meal identity, and non-empty item details when present.
  </behavior>
  <action>Update `tests/harness/scenarios/image-log-failure.ts` to assemble live `chunk` text from `rawSSE`, fail on malformed chunk JSON, and assert failed/no-mutation chunks plus persisted fallback copy do not match 已記錄 or 完成記錄. Update `tests/harness/scenarios/image-log.ts` so `parseReplyText` rejects malformed chunks and empty token output instead of silently returning empty strings, and add explicit `done.loggedMeal` shape assertions that match `projectLoggedMealReceipt` from `server/routes/chat.ts`. Mirror the new artifact expectations in `tests/integration/verification-image.test.ts`. Do not hand-edit generated artifacts; regenerate through harness commands only.</action>
  <verify>
    <automated>yarn verify:harness -- image-log-failure</automated>
    <automated>yarn verify:harness -- image-log</automated>
    <automated>yarn verify:harness -- protein-trust</automated>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/integration/verification-image.test.ts</automated>
    <automated>yarn test:unit</automated>
    <automated>yarn tsc --noEmit</automated>
    <automated>yarn release:check</automated>
  </verify>
  <done>Harnesses fail on false logging chunks, malformed/empty image-log chunks, and missing/empty loggedMeal receipt shape; all requested gates pass locally with no push, deploy, merge, or promotion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model->route SSE | Untrusted model text crosses into live user-visible `chunk` frames and persisted assistant history. |
| model->tool args | Untrusted `log_food` arguments cross into normalization and meal persistence. |
| harness artifacts | Generated evidence represents product behavior and must not false-pass by ignoring malformed chunks or missing receipts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260516-5EI-01 | Tampering | `server/routes/chat.ts` model-owned reply handling | mitigate | Gate no-mutation model text before SSE emission and persistence; replace false logging claims with safe fallback copy. |
| T-260516-5EI-02 | Information Disclosure | `server/orchestrator/system-prompt.ts` reachable model instructions | mitigate | Remove stale internal-facing `headline` and `保守估算` wording from reachable prompt copy while preserving internal tool-name prohibitions. |
| T-260516-5EI-03 | Repudiation | `tests/harness/scenarios/image-log*.ts` proof artifacts | mitigate | Fail harnesses on malformed chunks, empty live replies, missing loggedMeal receipts, and false logged-copy in failed/no-mutation chunks. |
| T-260516-5EI-04 | Denial of Service | SSE buffering for no-mutation guard | accept | The guard applies only to model-owned no-mutation replies; no new external I/O or unbounded data source is introduced beyond existing response text. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Fix code-review findings for v2.2 pre-PR patch | Covered by Tasks 1-3 |
| REQ | No-mutation replies cannot claim 已記錄/完成記錄 when mutation flags are false | Task 1, Task 3 |
| REQ | Remove or rewrite reachable prompt copy containing `headline` and `保守估算` | Task 1 |
| REQ | Grouped top-level Chinese serving metadata suppresses `missing_quantity` without source text | Task 2 |
| REQ | `image-log-failure` asserts live SSE chunks do not claim logging | Task 3 |
| REQ | Tools test isolates top-level grouped serving metadata with no quantity-bearing source text | Task 2 |
| REQ | `image-log` fails on malformed/empty chunks and asserts non-empty receipt shape | Task 3 |
| CONTEXT | Do not push, deploy, merge, or promote | All tasks are local edit/test only |
| CONTEXT | Verify targeted unit/integration tests, image-log-failure, image-log, protein-trust, yarn test:unit, yarn tsc --noEmit, yarn release:check | Task 1 targeted checks, Task 2 targeted check, Task 3 final gates |
</source_audit>

<verification>
Run the focused commands embedded in each task. Final verification must include:
- `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/orchestrator.test.ts tests/unit/system-prompt.test.ts tests/unit/tools.test.ts tests/integration/chat-api.test.ts tests/integration/verification-image.test.ts`
- `yarn verify:harness -- image-log-failure`
- `yarn verify:harness -- image-log`
- `yarn verify:harness -- protein-trust`
- `yarn test:unit`
- `yarn tsc --noEmit`
- `yarn release:check`

Do not run push, deploy, merge, rebase, fast-forward, staging promotion, or main promotion commands.
</verification>

<success_criteria>
The false-log guard protects live SSE chunks and persisted history whenever `didLogMeal` and `didMutateMeal` are false; stale reachable prompt terms are removed or rewritten; grouped top-level Chinese serving metadata suppresses false `missing_quantity`; harness proof catches false-pass cases; all requested local gates pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260516-5ei-fix-code-review-findings-for-v2-2-pre-pr/260516-5ei-SUMMARY.md`.
</output>
