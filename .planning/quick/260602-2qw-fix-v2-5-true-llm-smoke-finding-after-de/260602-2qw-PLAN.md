---
phase: quick-260602-2qw-fix-v2-5-true-llm-smoke-finding-after-delete
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - server/orchestrator/index.ts
  - server/routes/chat.ts
  - client/src/types.ts
  - client/src/api.ts
  - client/src/store.ts
  - client/src/components/ChatPanel.tsx
  - tests/unit/store.test.ts
  - tests/unit/api-client.test.ts
  - tests/integration/chat-streaming.test.ts
  - .planning/STATE.md
  - .planning/quick/260602-2qw-fix-v2-5-true-llm-smoke-finding-after-de/260602-2qw-SUMMARY.md
autonomous: true
requirements:
  - QUICK-260602-2QW
  - SMOKE-V2.5-DELETE-RECEIPT-INVALIDATION
must_haves:
  truths:
    - "After a live chat delete_meal succeeds, prior in-memory chat receipts for that deleted meal remain visible but no longer open Meal Edit."
    - "The delete confirmation itself remains assistant text only and does not gain a loggedMeal edit affordance."
    - "Reloaded chat history keeps existing stale/deleted receipt behavior: display facts remain visible, mealId/dateKey/mealRevisionId stay omitted for stale or deleted receipts."
    - "The v2.5 state records this as a pre-closeout true-LLM smoke fix."
  artifacts:
    - path: "server/orchestrator/index.ts"
      provides: "Committed delete result exposes a safe client invalidation id separate from compressed-history mutation facts"
      contains: "deletedMealId"
    - path: "server/routes/chat.ts"
      provides: "SSE done/stopped and JSON terminal payloads include deletedMealId only after committed delete_meal success"
      contains: "deletedMealId"
    - path: "client/src/api.ts"
      provides: "ChatReply and sendMessageStream parse deletedMealId from terminal payloads"
      contains: "deletedMealId"
    - path: "client/src/store.ts"
      provides: "Live-session commit path redacts existing matching receipt identity before appending the final delete response"
      contains: "deletedMealId"
    - path: "tests/unit/store.test.ts"
      provides: "Regression coverage that live-session delete commit redacts prior receipts without removing display content"
      contains: "deletedMealId"
    - path: "tests/integration/chat-streaming.test.ts"
      provides: "Route-level delete_meal terminal payload coverage for SSE and JSON live chat paths"
      contains: "deletedMealId"
  key_links:
    - from: "server/orchestrator/index.ts"
      to: "server/routes/chat.ts"
      via: "OrchestratorResult.deletedMealId"
      pattern: "deletedMealId"
    - from: "server/routes/chat.ts"
      to: "client/src/api.ts"
      via: "SSE/JSON terminal payload field"
      pattern: "deletedMealId"
    - from: "client/src/api.ts"
      to: "client/src/components/ChatPanel.tsx"
      via: "StreamCallbacks.onDone/onStopped data"
      pattern: "deletedMealId"
    - from: "client/src/components/ChatPanel.tsx"
      to: "client/src/store.ts"
      via: "commitProvisionalBubble/commitStoppedProvisionalBubble extra"
      pattern: "deletedMealId"
    - from: "client/src/store.ts"
      to: "client/src/meal-edit-payload.ts"
      via: "redacted receipt omits identity so buildReceiptMealEditPayload returns null"
      pattern: "mealId.*mealRevisionId.*dateKey"
---

<objective>
Fix the v2.5 true-LLM smoke finding where a deleted meal's prior live-session chat receipt can still open Meal Edit after delete_meal succeeds.

Purpose: Delete commits must invalidate stale live receipt edit identity immediately, matching the already-protected reload/history behavior.
Output: A small server-to-client invalidation field, client live-session redaction wiring, focused regression tests, and v2.5 pre-closeout state/summary recording.
</objective>

<execution_context>
@/Users/jia/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jia/.codex/get-shit-done/templates/summary.md
@.codex/skills/nutrition-gen-test/SKILL.md
@.codex/skills/nutrition-verify-change/SKILL.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@client/src/components/ChatPanel.tsx
@client/src/store.ts
@client/src/api.ts
@client/src/types.ts
@client/src/components/MessageBubble.tsx
@client/src/meal-edit-payload.ts
@client/src/meal-edit-refresh.ts
@server/orchestrator/index.ts
@server/orchestrator/mutation-effects.ts
@server/routes/chat.ts
@server/services/chat-mutation-outcomes.ts
@tests/unit/store.test.ts
@tests/unit/api-client.test.ts
@tests/unit/chat.test.ts
@tests/integration/chat-streaming.test.ts
</context>

<source_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Fix v2.5 true-LLM smoke finding: after delete_meal succeeds in the live chat session, stale prior meal receipts for the deleted meal become display-only and do not open Meal Edit. | Covered by Tasks 1-2 through route payload, client parser, store redaction, and live-session regression tests. |
| REQ | Add focused regression coverage for the live-session path. | Covered by Task 1 in `tests/unit/store.test.ts`, `tests/unit/api-client.test.ts`, and `tests/integration/chat-streaming.test.ts`. |
| REQ | Preserve reload/history stale receipt behavior. | Covered by Tasks 1-2 by keeping `server/services/chat.ts` behavior unchanged and keeping `tests/unit/chat.test.ts` D-22/D-26 green. |
| REQ | Run the Nutrition Coach verification matrix. | Covered by Task 3: TypeScript, unit, integration, and release pre-closeout gate. |
| REQ | Record this as a v2.5 pre-closeout smoke fix in GSD artifacts/state. | Covered by Task 3 in `.planning/STATE.md` and quick summary. |
| RESEARCH | No research phase requested. | Excluded by user constraint. |
| CONTEXT | No locked D-XX decisions supplied in this quick planning context. Existing state decisions require server-side revision checks remain authoritative and stale receipt protection remains fail-closed. | Covered by avoiding client-only authority changes and preserving server stale-history redaction. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add live delete receipt invalidation regressions</name>
  <files>tests/unit/store.test.ts, tests/unit/api-client.test.ts, tests/integration/chat-streaming.test.ts</files>
  <behavior>
    - Test 1: Given an existing in-memory assistant receipt with mealId/dateKey/mealRevisionId, committing a successful delete chat completion with deletedMealId removes only mealId/dateKey/mealRevisionId from prior matching receipts while preserving foodName, macros, imageAssetId/imageUrl, and visible receipt rendering facts.
    - Test 2: A nonmatching receipt keeps its edit identity after another meal's deletedMealId is committed.
    - Test 3: sendMessageStream passes valid string deletedMealId from event: done and event: stopped to callbacks, and ignores malformed non-string deletedMealId values.
    - Test 4: Existing delete_meal SSE and JSON integration flows return deletedMealId equal to the committed deleted meal id while still omitting loggedMeal, keeping delete confirmations non-editable.
  </behavior>
  <action>Use `nutrition-gen-test` rules: Node built-in `node:test`, real route/app integration patterns, and existing fixtures. Add tests next to the current receipt redaction coverage in `tests/unit/store.test.ts`, stream terminal payload coverage in `tests/unit/api-client.test.ts`, and delete_meal route assertions in `tests/integration/chat-streaming.test.ts`. Do not add browser-only or harness coverage for this quick fix; the false-pass risk is the typed live chat payload and store redaction path.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/store.test.ts tests/unit/api-client.test.ts tests/integration/chat-streaming.test.ts</automated>
  </verify>
  <done>The new tests fail before implementation because deletedMealId is not projected/parsing/wired, while the existing `tests/unit/chat.test.ts` D-22/D-26 reload/history stale receipt test remains the required preservation reference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire committed delete invalidation through chat transport and live store commit</name>
  <files>server/orchestrator/index.ts, server/routes/chat.ts, client/src/types.ts, client/src/api.ts, client/src/store.ts, client/src/components/ChatPanel.tsx</files>
  <behavior>
    - Test 1: Successful delete_meal results carry deletedMealId from the committed deletedMeal snapshot, not from model text, receipt display copy, or compressed-history mutation facts.
    - Test 2: SSE done/stopped and JSON terminal payloads include deletedMealId only when a delete mutation committed; log_food/update_meal/goals paths do not synthesize it.
    - Test 3: ChatPanel passes deletedMealId into the store's final assistant commit path for done and stopped events.
    - Test 4: Store final commit redacts prior matching receipts before appending the delete confirmation, so MessageBubble continues to render the display receipt but buildReceiptMealEditPayload returns null.
  </behavior>
  <action>Add `deletedMealId?: string` to `OrchestratorResult` as a client invalidation field separate from `mutationOutcomeFact`; do not add any id/revision field to `ChatMutationOutcomeFact` or compressed history. Set it only when `mealMutationKind === "delete"` after `deletedMeal` is present. Thread it through `server/routes/chat.ts` for normal SSE done, SSE stopped, SSE catch after committed mutation, JSON streamed-result returns, JSON normal returns, and JSON catch after committed mutation. Add `deletedMealId?: string` to client `ChatReply`, stream callback payloads, and terminal parsing in `client/src/api.ts` with string validation. In `client/src/store.ts`, extend the provisional commit extras with `deletedMealId?: string` and reuse the existing receipt-redaction behavior so matching prior receipts lose `mealId`, `mealRevisionId`, and `dateKey` while all display facts remain. In `client/src/components/ChatPanel.tsx`, destructure and pass deletedMealId from `onDone` and `onStopped`. Keep `MessageBubble.tsx` and `meal-edit-payload.ts` fail-closed behavior unchanged unless tests expose a direct gap.</action>
  <verify>
    <automated>node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/store.test.ts tests/unit/api-client.test.ts tests/integration/chat-streaming.test.ts</automated>
  </verify>
  <done>Live chat delete completions immediately make prior receipts for the deleted meal display-only, delete confirmations remain non-editable text, and persisted reload/history stale receipt behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Run verification matrix and record v2.5 pre-closeout fix</name>
  <files>.planning/STATE.md, .planning/quick/260602-2qw-fix-v2-5-true-llm-smoke-finding-after-de/260602-2qw-SUMMARY.md</files>
  <action>Use `nutrition-verify-change` after implementation to inspect changed files and run the matching local gates. Because this changes TypeScript, unit tests, integration tests, server route/orchestrator code, and client transport/store code, run `yarn tsc --noEmit`, `yarn test:unit`, and `yarn test:integration`; because v2.5 is at pre-closeout, also run `yarn release:check` after the narrower gates pass. Update `.planning/STATE.md` `last_activity` and current-position wording to record this as a v2.5 pre-closeout true-LLM smoke fix without changing milestone completion counts. Create the quick summary file with files changed, tests run, and confirmation that reload/history stale receipt behavior was preserved.</action>
  <verify>
    <automated>yarn tsc --noEmit && yarn test:unit && yarn test:integration && yarn release:check</automated>
  </verify>
  <done>All required local gates pass, `.planning/STATE.md` records the pre-closeout smoke fix, and `260602-2qw-SUMMARY.md` captures the behavioral fix and verification evidence.</done>
</task>

</tasks>

<dependency_graph>
Task 1 creates failing coverage. Task 2 depends on Task 1 and creates the transport/store behavior. Task 3 depends on Tasks 1-2 and records verification plus GSD state.
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM/tool execution -> route terminal payload | Committed server-side mutation facts cross into client-visible chat metadata. |
| Route terminal payload -> browser store | Client consumes typed metadata to invalidate in-memory edit affordances. |
| Browser store -> Meal Edit | Receipt identity controls whether a user can open an edit/delete workflow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260602-2QW-01 | Tampering | `server/orchestrator/index.ts` | mitigate | Derive deletedMealId only from committed `deletedMeal.mealId` after delete_meal succeeds; never from model text or receipt copy. |
| T-260602-2QW-02 | Information Disclosure | `server/services/chat-mutation-outcomes.ts` | mitigate | Keep mutationOutcomeFact/compressed history id-free; deletedMealId is live-client invalidation metadata only and follows the same authenticated chat response boundary as existing loggedMeal mealId for log/update. |
| T-260602-2QW-03 | Elevation of Privilege | `client/src/store.ts` and `MessageBubble.tsx` | mitigate | Redaction removes edit identity so `buildReceiptMealEditPayload()` returns null and MessageBubble omits button role, aria edit label, click handler, keyboard handler, tabIndex, and chevron for stale receipts. |
| T-260602-2QW-04 | Repudiation | `server/services/chat.ts` persisted history | mitigate | Do not alter history rehydration; keep D-22/D-26 tests proving stale/deleted reload receipts omit mealId/dateKey/mealRevisionId. |
| T-260602-2QW-SC | Tampering | package installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Use the Nutrition Coach verification matrix:
- Targeted red/green: `node scripts/run-node-with-tz.mjs --import tsx --test tests/unit/store.test.ts tests/unit/api-client.test.ts tests/integration/chat-streaming.test.ts`
- TypeScript gate: `yarn tsc --noEmit`
- Unit suite: `yarn test:unit`
- Integration suite: `yarn test:integration`
- v2.5 pre-closeout release gate: `yarn release:check`
</verification>

<success_criteria>
- A successful live chat delete_meal response carries a valid deletedMealId.
- The browser immediately redacts prior matching in-memory receipt edit identity after done/stopped completion.
- Prior matching receipt display facts remain visible, including foodName, nutrition, and image fields.
- Nonmatching receipts remain editable.
- Delete confirmation messages still omit loggedMeal and render as assistant text only.
- Reload/history stale and deleted receipts remain display-only via existing server-side stale receipt behavior.
- `.planning/STATE.md` and the quick summary record this as a v2.5 pre-closeout true-LLM smoke fix.
</success_criteria>

<output>
Create `.planning/quick/260602-2qw-fix-v2-5-true-llm-smoke-finding-after-de/260602-2qw-SUMMARY.md` when done.
</output>
